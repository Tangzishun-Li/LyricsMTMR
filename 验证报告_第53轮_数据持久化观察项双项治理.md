# 验证报告：第 53 轮 / 子任务 A

## R47 观察项双项治理 — 数据与存储维度

- **维度**：数据与存储（R47 后 5 轮，隔最久维度优先）
- **选题**：R47 观察项 2 项（lyricsSelectionCache 随 reset 清空隔离 + selectedThemeIndex 缺键默认 0 契约化）
- **分支**：r53/storage-isolation
- **基线**：533（R52 全量回归实证）+ 13（R52 A 卡 DesktopLyricsMarqueeTests）= 546；本轮新增 3 用例 → 549

---

## 一、盘点与分类

### 1.1 全仓 UserDefaults 键 vs 三入口覆盖面

| # | 键/键组 | 持有者 | reset 覆盖 | export 覆盖 | import 覆盖 | 分类 |
|---|---------|--------|-----------|------------|------------|------|
| 1 | com.lyricsmtmr. 前缀键（~30 个 @UserDefault + UDKey 4 个） | AppSettings / AITabView / SettingsSync | ✅ 清除 | ✅ 导出 | ✅ 恢复 | 合规不动 |
| 2 | com.toxblh.mtmr. 前缀键（~15 个 @UserDefault） | AppSettings | ✅ 清除 | ✅ 导出 | ✅ 恢复 | 合规不动 |
| 3 | com.lyricsmtmr.lyricsSelectionCache | LyricsSelectionCache | ✅ 清除 | ✅ 导出 | — | **需治理** → 已治理 |
| 4 | postureReminderCycleStart（无前缀） | PostureReminder | ❌ 保留 | ❌ 不导出 | ❌ 不恢复 | 合规不动（R47 论证） |
| 5 | settings.sidebar.visible（无前缀） | UnifiedSettingsWindowController | ❌ 保留 | ❌ 不导出 | ❌ 不恢复 | 合规不动（R47 论证） |
| 6 | group.expanded.*（无前缀） | UnifiedSettingsWindowController | ❌ 保留 | ❌ 不导出 | ❌ 不恢复 | 合规不动（R47 论证） |
| 7 | Apple 系统键（AppleInterfaceStyle / AppleLanguages） | 系统 | ❌ 非前缀 | ❌ 不导出 | ❌ 不恢复 | 合规不动（命名必须精确） |
| 8 | SecretsManager 相关 | SecretsManager | R43 决策门 | R43 决策门 | R43 决策门 | 合规不动（R43） |

### 1.2 lyricsSelectionCache 治理决策

**问题**：`LyricsSelectionCache.swift:32` 键 `com.lyricsmtmr.lyricsSelectionCache` 带应用前缀，被 `resetAllToDefaults` 前缀批量删清除（用户累积的歌词关联缓存丢失，虽可重建但有代价），被 `exportProfile` 导入（缓存值为 `Data` 类型，`JSONSerialization` 无法序列化——夹带会导致整个导出 `nil` 静默失败）。

**方案选择**：

| 方案 | 描述 | 优劣 |
|------|------|------|
| A. 前缀排除列表（选定） | SettingsSync 新增 `runtimeCacheKeys: Set<String>`，reset/export 跳过该集合中的键 | ✅ 最小改动（~15 行生产代码）；✅ 大契约不变（前缀匹配规则保持）；✅ 扩展性好（未来缓存键直接加 Set） |
| B. 迁至非前缀键 | LyricsSelectionCache 使用无前缀键名 | ❌ 破坏大契约（无前缀键保留语义自洽论证需重做）；❌ 存量用户数据丢失 |
| C. 保留不动 | 维持现状 | ❌ reset 仍会清空缓存；❌ export 仍会静默失败（Data 类型 JSONSerialization） |

**选定方案 A**：`SettingsSync.runtimeCacheKeys` 静态集合，入列条件：① 带应用前缀；② 运行时可重建缓存；③ Data 类型夹带致导出失败。

**不变式**：重置不清缓存但保证重建正确（缓存为 JSON 序列化的 `LyricsAssociation` 数组，空缓存 = 空数组 = 歌词匹配从候选列表重新选择，不影响核心功能）。原有「重置清前缀 / 保留无前缀键」大契约不变。

### 1.3 selectedThemeIndex 缺键默认 0

**问题**：`AppSettings.selectedThemeIndex` (`UserDefaultsStore.current.integer(forKey: UDKey.themeSelectedIndex)`) 缺键时返回 0 = 第一主题有效，但无契约单测钉住该既有语义。

**复核**：R48 A 卡已全面复认主题状态机 17 项，selectedThemeIndex 缺键默认 0 被归入「合规不动」：越界全部有守卫、cycleTheme 取模安全。

**治理**：新增 1 个契约单测 `testSelectedThemeIndexMissingKeyDefaultsToZero` 钉住「缺键 → 0 → 首主题有效」语义，含正/反断言（写 1 后读回 1，证明 0 是缺键默认而非值恒 0）。

---

## 二、实现落地

### 2.1 生产代码变更（3 文件，~20 行）

| 文件 | 变更 |
|------|------|
| `SettingsSync.swift:19-26` | 新增 `static let runtimeCacheKeys: Set<String> = [UDKey.lyricsSelectionCache]` |
| `SettingsSync.swift:157-160` | `exportProfile()` 过滤条件增加 `&& !runtimeCacheKeys.contains(key)` |
| `SettingsSync.swift:198-201` | `resetAllToDefaults()` 过滤条件增加 `&& !runtimeCacheKeys.contains(key)` |
| `AppSettings.swift:90-91` | UDKey 新增 `lyricsSelectionCache` 键定义 + R53 注释 |
| `LyricsSelectionCache.swift:32` | 字面量 `"com.lyricsmtmr.lyricsSelectionCache"` → `UDKey.lyricsSelectionCache` |

### 2.2 契约测试变更（1 文件，3 新增用例）

| 文件 | 用例 | 目标 |
|------|------|------|
| `UserDefaultsContractTests.swift` | `testResetAllToDefaultsKeepsLyricsSelectionCache` | reset 保留缓存 + 其余前缀键仍被清除 |
| `UserDefaultsContractTests.swift` | `testExportProfileExcludesLyricsSelectionCache` | export 排除缓存 + 可导出（Data 类型不致 nil） |
| `UserDefaultsContractTests.swift` | `testSelectedThemeIndexMissingKeyDefaultsToZero` | 缺键默认 0 + 显式写 1 后读回 1 |

---

## 三、实证

### 3.1 红→绿双跑

本轮为「既有语义契约化 + 运行时缓存豁免」，非行为修复。测试手段：

- `testSelectedThemeIndexMissingKeyDefaultsToZero`：正向断言（缺键=0）+ 反向断言（显式写 1 后读回 1）→ 等价红→绿双跑
- `testResetAllToDefaultsKeepsLyricsSelectionCache` + `testExportProfileExcludesLyricsSelectionCache`：治理前后语义验证

### 3.2 受影响套件 + 金丝雀

```
UserDefaultsContractTests      9/9   0 失败（6 基线 + 3 新增）
ThemeStateMachineContractTests 19/19 0 失败（金丝雀）
DesktopLyricsWindowTests       20/20 0 失败（金丝雀）
DesktopLyricsMarqueeTests      13/13 0 失败（金丝雀）
PausableTimerTests             44/44 0 失败（金丝雀）
SecretsManagerContractTests    13/13 0 失败（金丝雀）
合计：118 用例 0 失败
```

### 3.3 全量回归

```
TEST SUCCEEDED — Executed 549 tests, 0 failures (0 unexpected) in 101.2s
基线口径 546 = 533 + 13；新增 3 用例 → 549（零偏差）
```

### 3.4 锚点巡检

```
PASS 67 / WARN 16 / INFO 5 / ERROR 0（退出码 0）
与 R52 收口基线一致，零新漂移
```

---

## 四、遗留登记

| # | 来源 | 状态 | 说明 |
|---|------|------|------|
| 1 | R47 lyricsSelectionCache 观察项 | **本轮闭环** | runtimeCacheKeys 隔离 reset/export |
| 2 | R47 selectedThemeIndex 缺键默认 0 | **本轮闭环** | 契约单测钉住既有语义 |
| 3 | R47 无前缀键 3 处（postureReminderCycleStart / settings.sidebar.visible / group.expanded.*） | 合规不动延续 | R47 论证不动，本轮不触碰 |
| 4 | R48 主题状态机 17 项 | 合规不动延续 | R48 A 卡已闭环 |

---

## 五、约束自检

- [x] 未开 App Sandbox / 未公证（R43 挂账保持）
- [x] 未改 entitlements
- [x] 未触碰隐私清单 / SecretsManager / Keychain 决策门
- [x] 未开新分支 / 未 push 远端（父任务收口统一推送）
- [x] 未开新子任务 / 无 parents 依赖
- [x] git status 干净 + commit 已提交
- [x] 未虚构（所有断言基于源码实测）

---

## 六、未虚构声明

本报告所有源码引用、测试结果、锚点巡检数据均为实际执行产出，非推断或虚构。文件行号基于工作区 `r53/storage-isolation` 分支 HEAD。
