# 设置窗口内存"回不去"问题——调查与修复报告（t_5e363548）

> 报告日期：2026-08-12。任务：内存泄漏（用户报告：不开设置 35MB → 开设置 135MB →
> 关设置只回到 80MB，下不去）。基于 main=b405839（37 项优化已合并）实测。

## 一、结论速览

1. **OPT-1（关窗即释放）是有效的**：设置窗口关闭后，`UnifiedSettingsWindowController`、
   `NSHostingView<SettingsRootView>`、SettingsTabCache 在 heap 中全部消失（0 存活实例），
   CoreAnimation 从 136MB 降到 2.2MB。**不存在经典的强引用泄漏**（`leaks` 全量扫描仅 992B，
   其中每轮泄漏一个 32 字节的窗口标题字符串 + CFNetwork PAC 小循环）。
2. **但存在"每轮开窗 +~10MB、线性累积、无平台"的真实问题**：实测同一实例连开 8 次设置
   （每次只开默认 tab、不浏览），关窗后 footprint 48→60→70→85→90→94→99→103MB。
   用户看到的"80MB"只是中途采样点——按此速率 20+ 次开窗即回到优化前 300MB 量级。
3. **根因**：每次关窗销毁、开窗重建整棵窗口/视图树。框架缓存（CoreSVG 的 SF Symbols
   解析缓存 ~1-2MB/轮、SwiftUI 渲染缓存、objc method cache）按窗口实例去重失败，
   加上 malloc zone 中不可归还的碎片页（~7MB/轮），共同构成每轮净增长。
   `malloc_zone_pressure_relief` 实测返回 0（macOS 15 默认 zone 不归还）；
   内存警告兜底（OPT-8）实测不掉 footprint（缓存本就小）。
4. **修复**：关窗 = 隐藏复用（不销毁），重开零重建零增长；闲置 1 小时（或系统内存
   压力）才整体释放，内存回到基线。隐藏期间 Deck 动画由 OPT-2 的 isVisible 机制暂停，
   不会出现优化前的幽灵渲染（已实测隐藏后无 DisplayLink 采样）。

## 二、实测数据（全部 2026-08-12 实测，Debug 构建 = 当前 main）

### 2.1 基线对照（同构建，全新实例 vs 用户实例）

| 指标 | 全新实例（未开设置） | 用户实例（开过关设置） |
|---|---|---|
| phys_footprint | 22-24 MB | 80 MB（peak 139） |
| live heap 节点 | 43,923 | 158,528 |
| live heap 总量 | ~6.9 MB | ~26.6 MB |
| CoreAnimation | — | 2.2 MB（优化前 136 MB） |
| 设置窗口对象 | — | **0 存活**（heap 实锤） |

### 2.2 开/关循环累积实验（实验脚本 /tmp/mem_experiment2.sh、3.sh）

| 轮次 | 开窗+10s | 关窗+6s | 关窗+30s |
|---|---|---|---|
| 1 | 57 MB | 48 MB | 50 MB |
| 2 | 69 MB | 60 MB | 62 MB |
| 3 | 79 MB | 70 MB | 71 MB |
| 4 | 98 MB | 85 MB | — |
| 5 | 103 MB | 90 MB | — |
| 6 | 108 MB | 94 MB | — |
| 7 | 111 MB | 99 MB | — |
| 8 | 118 MB | 103 MB | — |

线性增长 ~10MB/轮，8 轮无收敛迹象。关窗后 120s 内无自然衰减。

### 2.3 残留构成（heap/vmmap 归因）

| 类别 | 增量（3 轮 vs 全新） | 可回收性 |
|---|---|---|
| 匿名缓冲（SwiftUI/CoreSVG 内部） | +5.5 MB | 框架级，不可干预 |
| CoreSVG SF Symbols 解析缓存 | +0.9 MB/3轮（随窗口实例增长） | 框架级 |
| malloc zone 碎片/滞留页 | ~7 MB/轮 | `pressure_relief`=0，不可归还 |
| CFString / 字体 / objc method cache 等 | ~1 MB | 框架级，微小 |

### 2.4 已排除项

- leaks 全量：992 B（每轮 1 个 "设置" 标题字符串 32B + CFNetwork PAC 循环）。
- `malloc_zone_pressure_relief(zone, 0/SIZE_MAX)`：返回 0，无效。
- OPT-8 内存警告路径：URLCache/CoverCache/歌词 LRU 本就小，footprint 不动。
- NSImage 命名缓存 / 自定义图片集：无（全部 SF Symbols）。
- 时间衰减：关窗后 30s/60s/120s 均无下降。

## 三、修复设计（窗口复用 + 闲置 GC）

### 3.1 方案对比

| 方案 | 关窗后内存 | 多次开关后 | 说明 |
|---|---|---|---|
| 现状（每轮销毁重建） | 48-50 MB | 48→60→70→85→… 无平台 | 10MB/轮线性累积 |
| 永久复用（不释放） | ~50 MB | 恒 50 MB | 重开零延迟，但永不回落 |
| **复用 + 闲置 GC（本修复）** | ~50 MB（活跃期） | 恒 ~50 MB | 闲置 1h 后回落基线 ~35MB；内存压力即时释放 |

### 3.2 改动点

1. **UnifiedSettingsWindowController.swift**
   - `windowShouldClose` 不再放行关闭：一律 `hideWindow()`（orderOut 隐藏）后返回 false；
     未保存编辑器修改的确认弹窗逻辑保留（保存/不保存 → 隐藏；取消 → 保持可见）。
   - 新增 `hideWindow()`：同步 isVisible=false（停 Deck 动画，OPT-2）、收起 Dock 图标
     （DockVisibilityManager）、orderOut、回调 `onWindowHidden`。
   - `DockVisibilityManager.track()` 改为幂等（先清旧 observer），重复开窗不叠加通知令牌。
2. **AppDelegate.swift**
   - `openSettings`：开窗即取消待执行的闲置 GC；窗口已存在则直接复用（重挂 Dock 跟踪）。
   - 新增 `scheduleSettingsWindowGC()`：关窗后 1 小时（`settingsWindowIdleGCSeconds=3600`）
     释放控制器，整树归还系统。1h 取值理由：每次重建永久代价 ~10MB，窗口应活过典型
     开/关工作流；真正不用了（1h）才释放。
   - `applicationDidReceiveMemoryWarning`：隐藏中的设置窗口整树立即释放
     （`releaseSettingsWindowIfHidden`，可见时不打扰用户）。

### 3.3 为什么不会重演幽灵窗口

幽灵窗口三要素全部不成立：① 窗口隐藏时 Deck 漂移动画按 isVisible 暂停（OPT-2，隐藏
后无 DisplayLink 采样）；② 21 tab 懒加载 + LRU(4)（OPT-4）；③ blur 静态化（OPT-3）。
窗口存活 ≠ 渲染。

## 四、验证结果（修复版构建实测，2026-08-12 14:10）

验证方法：临时加入 DEBUG 自测钩子（env `LYRICSMTMR_SETTINGS_CYCLE_TEST=5`），App 进程
内自行执行 5 轮开/关设置并打印每步 phys_footprint（进程内 task_info 不受外部调试器
卡死影响——当时本机 Xcode 工具链因内存打满 + WindowServer stuck 全部挂起），随后触发
内存警告路径并重开一次。验证后钩子已移除，交付物为纯修复。

| 里程碑 | 修复前（每轮销毁重建） | 修复后（复用 + GC） |
|---|---|---|
| 基线 | 23 MB | 21.3 MB |
| 关窗后 第1轮 | 48 MB | 52.1 MB |
| 关窗后 第2轮 | 60 MB | 51.9 MB |
| 关窗后 第3轮 | 70 MB | 51.9 MB |
| 关窗后 第4轮 | 85 MB | 51.9 MB |
| 关窗后 第5轮 | 90 MB | 52.0 MB |
| 5 轮后 | （继续线性涨，8 轮 103MB） | **51.9 MB 全平** |
| 内存警告后 | 无变化 | **38.2 MB**（隐藏窗口整树释放） |
| 释放后重开 | — | 54.8 MB（正常重建，无崩溃） |

结论：每轮 +10MB 线性累积被彻底消除（5 轮零增长，波动 ±0.2MB）；内存警告路径立即
回落 13.7MB（接近基线）；重开功能正常。窗口隐藏期间无幽灵渲染（OPT-2 的 isVisible
暂停机制，此前已实测隐藏后无 DisplayLink 采样）。

## 五、遗留与建议

- malloc zone 碎片是 macOS 分配器行为，任何应用都无法主动归还（`pressure_relief` 实测
  返回 0；压力下系统自行回收）。复用方案把「每次重建的永久代价」降为「每次 GC 后重建
  才付一次」；若想进一步减少，只能调大 `settingsWindowIdleGCSeconds`。
- 复用后 CoreSVG 符号缓存不再按窗口实例增长（同 UI 不重复解析），长期稳定性建议在
  下次回归时用同一自测钩子复测 10+ 轮确认无漂移。
- 本机 8GB 内存偏紧（实测 7.5G/8G 占用 + 3GB compressor 时 WindowServer 也会 stuck、
  Xcode 分析工具集体挂起）；用户若常开设置可考虑把设置窗口的 LRU 容量从 4 提到 6-8，
  减少重度浏览时的重建次数（会相应增加开窗态内存，需权衡）。
