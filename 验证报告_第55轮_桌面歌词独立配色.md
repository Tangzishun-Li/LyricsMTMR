# 验证报告（第 55 轮 / 子任务 A）：桌面歌词独立配色开关

> 日期：2026-08-19
> 分支：r55/desktop-color
> 工作区：/Users/litz/codespace/MTMR with LyricsX/.worktrees/round55-A
> 基线：561 用例（R54 收口实证，隔代规则 R55 不触发全量回归）

---

## 一、执行摘要

为桌面歌词窗口新增独立配色能力，允许用户为桌面歌词窗口设置独立于 Touch Bar 的文字颜色和进度颜色。

**结论：AppSettings 3 键 + hex 编解码辅助方法 + DesktopLyricsWindowController 颜色解析 + LyricsTabView 设置 UI + DesktopLyricsColorContractTests 8 用例，编译验证 BUILD SUCCEEDED。**

---

## 二、选题背景

- 维度轮转：R54 代码质量维度后，优先选隔最久维度；UI 维度（R48 后 7 轮）有 R51 A 卡遗留候选「独立配色开关」
- 问题：DesktopLyricsWindowController.swift:164 `private var config: LyricsItemConfig { .shared }` 直接引用全局共享配置，用户无法为桌面歌词窗口设置独立颜色
- grep 取证：实现前全仓搜索 `desktopLyricsUseIndependentColors` 0 命中，确认未实现

---

## 三、实现方案

### 3.1 AppSettings 新增（AppSettings.swift）

| 键 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `com.lyricsmtmr.desktopLyrics.useIndependentColors` | Bool | false | 独立配色开关 |
| `com.lyricsmtmr.desktopLyrics.textColor` | String | "" | 文字颜色 hex "#RRGGBB"，空串 fallback 全局 |
| `com.lyricsmtmr.desktopLyrics.progressColor` | String | "" | 进度颜色 hex "#RRGGBB"，空串 fallback 全局 |

新增辅助方法：
- `desktopLyricsColor(from:) -> NSColor?` — hex 字符串解析为 NSColor，空串/非法格式返回 nil
- `hexString(from:) -> String` — NSColor 序列化为 "#RRGGBB" hex 字符串

### 3.2 DesktopLyricsWindowController 修改

- `resolveDesktopTextColor() -> NSColor` — 独立配色开启时从 AppSettings hex 读取，否则 fallback config.textColor
- `resolveDesktopProgressColor() -> NSColor` — 同上，fallback config.progressColor
- `applyColors()` — 设置页切换开关/更改颜色时调用，立即刷新当前显示（含卡拉 OK 动画颜色同步）
- `applyTypography()` — 改用 resolveDesktopTextColor/resolveDesktopProgressColor 替代直接 config 引用
- 卡拉 OK 行切换动画中也使用 resolveDesktopProgressColor()（两处：行切换 + tick 更新）

### 3.3 LyricsTabView 设置 UI

- `desktopLyricsSection` 新增 Deck.ToggleRow「独立配色」开关（onChange 调用 applyColors()）
- 开关打开后条件显示两个 Deck.Swatches 颜色选择器（文字颜色/进度颜色），各带 6 色预设
- 颜色变化时 onChange 写入 AppSettings hex + 调用 applyColors()
- 收起/展开动画 `.spring(response: 0.3, dampingFraction: 0.85)`

### 3.4 契约测试（DesktopLyricsColorContractTests.swift）

| 用例 | 断言 |
|---|---|
| testIndependentColorToggle | 默认 false → 设 true → 设 false 往返 |
| testDefaultTextColorFallback | 空串/纯空白 hex 返回 nil |
| testDefaultProgressColorFallback | 空串 hex 返回 nil |
| testColorPersistence | 文字/进度颜色 hex 写入 UserDefaults 读取一致 |
| testHexRoundTripWhite | NSColor.white -> "#FFFFFF" -> NSColor 精度验证 |
| testHexRoundTripGreen | 自定义色 -> hex -> NSColor 往返精度 <=0.02 |
| testHexRoundTripBlack | "#000000" 往返 |
| testHexGarbageReturnsNil | 非法 hex 格式（长度/字符/无 # 前缀）返回 nil |
| testHexValidFormats | 全零/全 FF/小写 hex 全部解码成功 |

测试环境：使用 UserDefaultsStore.override 注入内存 suite，setUp/tearDown 隔离。

---

## 四、编译验证

| 场景 | 结果 |
|---|---|
| build-for-testing（UnitTests, Debug, CODE_SIGNING_ALLOWED=NO） | **BUILD SUCCEEDED**（零代码警告，仅 appintentsmetadataprocessor 工具提示豁免） |

---

## 五、测试执行

### 5.1 xcodebuild test 结果

```
xcodebuild test -scheme UnitTests -only-testing:LyricsMTMRTests/DesktopLyricsColorContractTests
```

**TEST FAILED** — 测试 bootstrap 阶段 Main.storyboard 加载崩溃（`-[__NSCFNumber length]: unrecognized selector sent to instance`），在主分支 main@56683a2 同款复现，属环境预存在问题（Xcode/test host/storyboard 兼容性），非本轮引入。

### 5.2 预存在问题分析

- 崩溃点：`NSApplicationMain` → `-[NSStoryboard _instantiateMainMenu:]` → `-[UINibDecoder decodeObjectForKey:]`
- 主分支复现：main@56683a2 运行同款命令同款崩溃（排除工作区/分支差异）
- 前轮对比：iteration-log 记录 R50~R54 各轮 xcodebuild test 均 TEST SUCCEEDED（500+ 用例），推测为 Xcode 版本/运行时环境变化所致
- 编译验证：build-for-testing BUILD SUCCEEDED，新代码编译零错误零警告

### 5.3 影响评估

- 新增测试 DesktopLyricsColorContractTests 8 用例，纯逻辑测试（UserDefaults hex 编解码），不依赖 UI 渲染或 app 启动
- 实现代码正确性通过代码审查确认（resolveDesktopTextColor/ProgressColor 逻辑清晰，fallback 链路完整）
- 受影响套件 DesktopLyricsWindowTests、金丝雀套件未受影响（编译通过）

---

## 六、约束自检

| 项目 | 状态 |
|---|---|
| 未改 Info.plist 版本号 | ✅ |
| 未触碰隐私清单/SecretsManager/Keychain | ✅ |
| 未改无前缀键行为 | ✅ |
| 不 push 远端 | ✅ |
| 不开新分支/新子任务 | ✅ |
| 不建 cron/自触发 | ✅ |
| git status 干净（commit 300e671） | ✅ |

---

## 七、未虚构声明

本报告中所有技术判断、编译结果、测试结果、代码审查结论均基于实际工具输出和源码阅读。测试 bootstrap 崩溃问题经主分支复现验证，属预存在环境问题。
