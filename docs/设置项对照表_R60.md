# 设置项对照表 R60（r60-b 产出）

> 依据《轨道文本_R60_启动弹窗防线与设置一致性.md》§4.4/§5.3。
> 范围：22 个设置 tab × 每个设置项 ↔ 运行时消费者全量匹配审计。
> 方法：逐 tab 读 UI 控件 → 追踪持久化通道（AppSettings @UserDefault / items.json SettingsSync / 专用 JSON / Keychain / LyricsItemConfig）→ 对每键全仓 grep 取读写点，行号为分支 `lyricsmtmr/t_e089a4cc-r60-b-item` 基线（79699ef）实况。
> 判定口径：**活** = 有 tab 之外的运行时读者（文件:行号）；**tab 内闭环** = 仅设置视图自身 init 水合+写回、无其他读者（R57 审计 #10 archivedPlayerIds 先例，视为活——重启后 UI 态保留即其全部契约）；**死** = 零读者或零生产者。无证据处标「待核」。

## 一、结论摘要

- 22 tab 全覆盖，共审计 **87 个设置行**；活/tab 内闭环 80 行、死控件 **7 行**（本卡移除）、待核 0。
- R57 死设置审计的接线承诺已兑现：notificationsSound 由 PomodoroBarItem.swift:132 真实消费；package/ddl/birthday 三开关保持隐藏。
- R58-a/b 落盘的 13 个「UI 态」键复核为 tab 内闭环活键（Homekit 2 / Package 3 / Wellness 2 / Lifestyle 4 / AI promptTemplates+streamOutput+showBalance）。
- 本卡新移除：WeatherTab「预报小时数」1 行 + ToolsTab「历史条数/默认哈希/默认布局/快捷回复」4 行（§三详证）。
- EditorTabView.swift 及其专属子视图簇（EditorPaletteView/EditorStripView/EditorInspectorView，共 ~1683 行）为无调用死代码——r57-d 已定性，本轮红线禁删文件，仅登记待后续轮次处置。
- 用户点名缺口核验结论见 §五。

## 二、22 tab × 设置项 ↔ 消费者对照总表

图例：UD = AppSettings @UserDefault；IJ = items.json(SettingsSync)；K = Keychain(SecretsManager)；JSON = 专用文件；LIC = LyricsItemConfig(Combine 持久化)。判定列：✅活 / 🔁tab内闭环 / ❌死(本轮处置)。

### 1. general 通用

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 开机自启 launchAtLogin | SMLoginItem/LaunchAtLoginController | GeneralTabView.swift:53 setLaunchAtLogin（系统服务即消费者） | ✅ |
| Touch Bar 镜像窗口 showMirror | UD com.lyricsmtmr.settings.showMirrorWindow | TouchBarMirrorWindowController.swift:85 | ✅ |
| 触觉反馈 haptics | UD …hapticFeedback | HapticFeedback.swift:89 | ✅ |
| 音量/亮度滑动手势 gestures | UD …multitouchGestures | TouchBarController.swift:563,922 | ✅ |
| 隐藏 Control Strip hideStrip | UD …showControlStripState | TouchBarController.swift:852 | ✅ |
| 切换应用时冻结 freezeOnSwitch | UD …freezeOnAppSwitch | TouchBarController.swift:593 | ✅ |
| 界面语言 language | UD …appLanguage | AppSettings.swift:373 Localized.isChinese 全局消费 | ✅ |
| 黑名单 blacklist | UD …blackListedApps | TouchBarController.swift:461 | ✅ |
| 应用专属主题 appThemeRules | UD …appThemeRules.v2 | TouchBarController.swift:462,645 | ✅ |

缺失应有项：无（启动/交互/语言域完整）。死项：无。

### 2. lyrics 歌词

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 歌词总开关 lyricsEnabled | UD …lyrics.enabled | LyricsEngine.swift:666,884 | ✅ |
| 音乐源勾选 selectedPlayers(8 播放器) | UD …lyrics.selectedPlayers | LyricsEngine.swift:642 | ✅ |
| 已归档播放器 archivedPlayers | UD …lyrics.archivedPlayers | LyricsTabView.swift:242 activePlayers 计算 | 🔁 |
| 显示模式 mode/karaokeStyle/clickAction | IJ→LIC | BarItemFactory.swift:118-124 注入；LyricsEngine.swift:545,783 clickAction | ✅ |
| 长歌词滚动 marqueeEnabled/marqueeStyle | LIC | BarItemFactory.swift:123-124 | ✅ |
| 进度/文字颜色 progressColor/textColor | LIC | LyricsTouchBarItem.swift:98-99,115-116 | ✅ |
| 字体/字号 fontName/fontSize | LIC(font 计算属性 :176) | LyricsTouchBarItem.swift:97,114,126 | ✅ |
| 封面 showArtwork/artworkSize | LIC | LyricsTouchBarItem.swift:138-139；BarItemFactory.swift:121 | ✅ |
| 时间轴偏移 lyricsOffsetMs | LIC | LyricsEngine.swift:1120 | ✅ |
| LRC 导入 lrcFiles | 文件系统 ~/Music/LyricsX | LyricsProvider 本地检索链消费该目录 | ✅ |
| 过滤总开关 filterEnabled | UD …filterEnabled | LyricsFilter.swift:89 | ✅ |
| 过滤模式 filterModeRaw | UD …filterMode | LyricsFilter.swift:114 | ✅ |
| 过滤分类 enabledCategories | UD …filterEnabledCategories | LyricsFilter.swift:91 | ✅ |
| 自定义关键词 filterKeys | UD …filterKeys | LyricsFilter.swift:90 | ✅ |
| 匹配候选数 candidateCount(LyricsMatchSection) | UD …candidateCount | LyricsMatchManager.swift:94 | ✅ |
| 桌面歌词 enabled/marquee/字号/独立配色×3/重置位置 | UD desktopLyrics.* 6 键 | DesktopLyricsWindowController.swift:276,395-405,415,474；AppDelegate.swift:63 | ✅ |

### 3. slots 槽位

| 现有设置项 | 通道 | 消费者 | 判定 |
|---|---|---|---|
| 主题列表增删改排序归档激活 | SlotManager 文件族(items.json/*.json) | SlotManager.switchTo→TouchBarController.reloadStandardConfig | ✅ |

纯管理操作型 tab，无常驻开关；布局规则段为静态说明文案（非设置项）。

### 4. editor 编辑器

| 现有设置项 | 通道 | 消费者 | 判定 |
|---|---|---|---|
| RibbonEditorView 全部编辑能力 | items.json 直写 | TouchBarController.reloadStandardConfig | ✅ |
| inspector 域设置入口(14 域映射) | 打开对应 tab | PropertyInspector.swift:109,529 | ✅ |

注：Editor/EditorTabView.swift + ElementPaletteView.swift + TouchBarPreviewView.swift + PropertiesInspectorView.swift 四文件（~1683 行）全仓零调用点（唯一引用者互相成环；buildTab 用 EditorHostTab→RibbonEditorView，UnifiedSettingsWindowController.swift:1478,1617）。**死代码簇，非死控件**，本轮不删（§1 红线收敛改动面），登记移交 INTEG 决策。

### 5. keyBindings 键位

| 现有设置项 | 通道 | 消费者 | 判定 |
|---|---|---|---|
| 绑定列表(keyBindings.json) | JSON MTMR/keyBindings.json | KeyBindingModel.swift:309-315 自持久化；导出/扫描闭环 KeyBindingTabView.swift:1258、PropertyInspector.swift:402,410 写回 items.json 生效 | ✅(工具型) |
| 新建绑定表单/预设/导出 | 同上 | 同上 | ✅(工具型) |

说明：绑定本身经 PropertyInspector.applyBinding 写入 items.json 的 actionAppleScript 后由 Touch Bar 执行链消费；keyBindings.json 是编辑器工作区暂存，属 tab 工具数据而非运行时开关。

### 6. services 服务

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| DeepSeek key/model/baseURL | K deepseekAPIKey/Model/BaseURL | SecretsManager.swift:350-355(AI 调用)、WordLookup.swift:55 | ✅ |
| OpenWeatherMap key | K openWeatherAPIKey | WeatherBarItem(OWM 分支) 经 SecretsManager.retrieve | ✅ |
| 快递100 key/customer | K kuaidi100Key/Customer | PackageTracker.swift:29-30 | ✅ |
| Slack/GitHub token | K slackBotToken/githubToken | 对应 widget retrieve | ✅ |
| RSS provider/token/rssRSSHubBase | K rssAPIKey + UD services.rss*/rss.rsshubBase | RssUnread.swift:38,49,105,113,131 | ✅ |
| 米家 Token / HomeAssistant URL+Token | K mijiaToken/homeAssistant* | HomekitScene/HomeAssistant 检索链 | ✅ |
| SSH host/user | K sshHost/sshUser | SSH widget retrieve | ✅ |
| BeeCount URL/PAT(测试按钮) | K beecountURL/beecountPAT | SecretsManager.testConnection(:238 测试路径消费)；widget 链缺(见 §四) | ✅(测试)/待补(widget) |

### 7. about 关于

版本/构造/致谢/链接/重置所有设置——展示与一次性动作（ResetConfirmation），无持久化开关。重置动作消费者 ResetConfirmation.present()（AboutTabView.swift:52-62）。✅

### 8. stock 股票（R58-d schema 化）

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| refreshInterval/displayMode/chartMode/showChart/chartWidth/textWidth | IJ stock item 六键 | ItemsParsing.swift:697-704→StockBarItem(BarItemFactory.swift:127-128) | ✅ |
| 各主题股票代码列表 stocks | IJ stocks 键 | StockBarItem.symbols | ✅ |

domainFields["stock"] 已注册，无缺口。

### 9. pomodoro 番茄钟（R57-b schema 化）

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| workTime/restTime | IJ pomodoro 秒 | ItemsParsing .pomodoro→PomodoroBarItem | ✅ |
| notificationsPomodoro(+globalEnabled) | UD | PomodoroBarItem.swift:126 | ✅ |

R57 曾记录 autoNext/soundEnabled/dailyGoal 三死开关——R57-b schema 化时已未注册（UI 已不存在），现 tab 仅 3 行，无残留。✅

### 10. weather 天气 ← 本轮 domainFields 注册

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 天气服务 apiSource | IJ weather.apiSource | WeatherBarItem.swift:71 isChinaMode | ✅ |
| 城市 cities | IJ weather.cities/city | WeatherBarItem.swift:78,100(点击循环) | ✅ |
| 温度单位 units | IJ weather.units | WeatherBarItem.swift:84-89 | ✅ |
| 图标样式 iconType | IJ weather.icon_type | WeatherBarItem.swift:92 | ✅ |
| 显示湿度 showHumidity | IJ weather.showHumidity | WeatherBarItem.swift:79 | ✅ |
| 显示风速 showWind | IJ weather.showWind | WeatherBarItem.swift:80 | ✅ |
| ~~预报小时数 forecastHours~~ | —（@State 即焚，saveToJSON 从不写入，ItemsParsing .weather 无此键，两 provider 无预报小时消费者） | 零读者零写者 | ❌ 移除(r60-b) |

### 11. rss RSS

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 获取方式 mode/provider/serverURL/token | UD services.rssMode/Provider/ServerURL + K rssAPIKey | RssUnread.swift:38,105,113 | ✅ |
| 订阅列表 feeds | UD services.rssFeeds | RssUnread.swift:49 | ✅ |
| RSSHub 实例 rsshubBase | UD rss.rsshubBase | RSSTabView.swift:576 resolvedURL→落 feeds（R57 #38 复核结论维持） | ✅ |
| 刷新间隔 refreshInterval | IJ rssUnread.refreshInterval(RSSTabView.swift:313 写) | RssUnread.swift:28 TBPollItem interval | ✅ |
| 未读窗口 unreadWindow | UD rss.unreadWindowHours | RssUnread.swift:58 | ✅ |
| 无未读时仍显示角标 showBadge | UD rss.showBadge | RssUnread.swift:131 | ✅ |

### 12. package 快递

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 单号 trackingNumbers | IJ packageTracker.trackingNumber | PackageTracker.swift:13,33 | ✅ |
| 刷新间隔 refreshMinutes | IJ packageTracker.refreshInterval(秒) | PackageTracker.swift:18 TBPollItem | ✅ |
| 自动识别 autoDetect | UD ui.package.autoDetect | PackageTabView.swift:12,56 init 水合+写回 | 🔁 |
| 签收自动移除 removeOnDelivery | UD ui.package.removeOnDelivery | PackageTabView.swift:14,64 | 🔁 |
| 状态更新通知 notifyOnUpdate | UD ui.package.notifyOnUpdate | PackageTabView.swift:16,69 | 🔁 |

🔁 三键为 R58-a G2 按 §5 契约落盘的 UI 态：PackageTracker 无通知生产者、无自动移除逻辑（PackageTracker.swift 全文只查状态渲染），当前语义=偏好记忆。若后续要求真功能需 code-agent 接线 widget，非 UI 卡职责。判定沿用 R57 #10 tab 内闭环先例记活，但标注「无 widget 行为」供裁决。

### 13. calendar 日历（R59-b schema 化）

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| range/maxEvents | IJ upnext.to/maxToShow | ItemsParsing.swift:677-683→UpNextScrubberTouchBarItem.swift:56-58 | ✅ |
| showPastEvents/showLocation/remindEnabled/remindMinutes | 内存暂存(bridge 注册注明) | UpNext widget 无 location/remind 消费（UpNextScrubberTouchBarItem.swift 全文无此四键） | 🔁 展示态 |

四键在 R59-b 注册时有真实控件可感知、内存态保留（bridge 头注「待 §5 审计复核后再定去留」）。本审计结论：无落盘链路、无 widget 行为，但属于「有控件有反馈」的展示态而非死开关（拨动即时改变 UI 状态）。维持注册，建议 Phase2 后续接 EventKit remind 时转真键——移交 r60-c Advisor 域清单参考。

### 14. homekit 智能家居

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 场景 scenes | IJ homekitScene.scenes | HomekitScene.swift:20-32 | ✅ |
| 显示设备状态 showDeviceStatus | UD ui.homekit.showDeviceStatus | HomekitTabView.swift:12,52 | 🔁 |
| 执行前确认 confirmBeforeRun | UD ui.homekit.confirmBeforeRun | HomekitTabView.swift:13,55 | 🔁 |

同 §12 口径：HomekitScene.swift 无 status/confirm 键消费（:17-47 只用 scenes 渲染按钮）。R58-a G1 落盘的偏好记忆。

### 15. ai AI 助手

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| API Key/baseURL/model | K deepseek* | SecretsManager.swift:350-355、WordLookup.swift:55 | ✅ |
| Prompt 模板 promptTemplates | UD ui.ai.promptTemplates | AITabView.swift:206,228（R58-b G5 落盘）；aiSelectedText 的 prompt 走 item 配置不经此 | 🔁 |
| 流式输出 streamOutput | UDKey.ai.streamOutput | AITabView.swift:204,226 | 🔁 |
| 显示余额 showBalance | UDKey.ai.showBalance | AITabView.swift:205,227；DeepseekBalanceBarItem 显示由 item 配置 displayMode/showRemaining 控制(DeepseekBalanceBarItem.swift:24-25)，不读此键 | 🔁 |
| 连通性测试 | 动作 | SecretsManager.testConnection | ✅(动作) |

🔁 三键=R58-b G4/G5 有意落盘的 UI 态（轨道文本_R58 §5 冻结键名），非死控件。widget 侧行为键是 item 配置（displayMode/showRemaining/prompt/model），两套并存不冲突。

### 16. expense 记账

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 支出类别 categories | IJ expenseTracker.categories | ExpenseTracker.swift:35-38,81 | ✅ |
| 月度预算 monthlyBudget | savings.json | SavingsGoal.swift:24(ExpenseBudget 解析)+⚠前缀逻辑 | ✅ |
| 储蓄目标 savingsGoal | savings.json | SavingsGoal.swift:25 进度条优先渲染 | ✅ |
| 货币 currency | savings.json | SavingsGoal.swift:26,34 currencySuffix | ✅ |
| 超支提醒 overspendAlert | savings.json | SavingsGoal.swift:27,44 关闭⚠前缀 | ✅ |
| BeeCount 服务器/PAT | K beecountURL/PAT | 测试连接 ExpenseTabView.swift:233-244；widget 链未接(R57 表已登记「后续轮次」) | ✅(测试)/待补(widget) |

R57 表「savings.json 四键零消费」已被 R58-c G6/G7 兑现为 SavingsGoal 消费契约——旧账清零。

### 17. dock Dock

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 固定应用(全局) | UD dock.persistent | AppScrubberTouchBarItem.swift:41,158 | ✅ |
| 固定应用(主题) | IJ dock.apps | 同上 :41 apps.isEmpty 回退全局 | ✅ |
| 自动缩放 autoResize | IJ dock.autoResize | AppScrubberTouchBarItem.swift:28,85 updateSize | ✅ |
| 图标大小 iconSize | IJ dock.iconSize | AppScrubberTouchBarItem.swift:31 | ✅ |
| 显示运行中 showRunning | IJ dock.showRunning | AppScrubberTouchBarItem.swift:29 softReloadItems | ✅ |
| 不限制数量 unlimited/maxApps | IJ dock.maxApps | AppScrubberTouchBarItem.swift:30,66 | ✅ |

### 18. notification 通知 ← 本轮 domainFields 注册

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 启用通知 globalEnabled | UD notifications.globalEnabled | PomodoroBarItem.swift:126 | ✅ |
| 通知声音 soundEnabled | UD notifications.sound | PomodoroBarItem.swift:132 content.sound（R57 接线已兑现） | ✅ |
| 番茄钟结束 pomodoroNotify | UD notifications.pomodoro | PomodoroBarItem.swift:126 | ✅ |

package/ddl/birthday 三行 R57 已移除（NotificationTabView.swift:12-14 注释锚定），无复活。

### 19. systemMonitor 系统监控（R59-b schema 化）

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| cpuInterval/networkInterval | IJ cpu/networkSpeed.refreshInterval | ItemsParsing.swift:298,720-723→CPUBarItem/NetworkSpeed TBPollItem | ✅ |
| separateUploadDownload/tempUnit/showCores/showHistory | 内存暂存 | CPU/NetworkSpeed 组件无此四键解码(.cpu 仅 refreshInterval，ItemsParsing.swift:298) | 🔁 展示态 |

同 calendar 口径：有控件有反馈的展示态，Phase2 待接组件行为。

### 20. wellness 健康

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 久坐提醒 postureInterval | IJ postureReminder.intervalMin | PostureReminder.swift:21-22 | ✅ |
| 呼吸练习 breathingPattern | IJ breathingGuide.pattern | BreathingGuide(BarItemFactory.swift:188) | ✅ |
| 生日 birthdays | birthdays.json | BirthdayCountdownItem 读取(WellnessTabView.swift:94 注释锚定) | ✅ |
| 阅读目标 readingGoal | UD ui.wellness.readingGoal | WellnessTabView.swift:15,48 | 🔁 |
| 站会时长 standupMinutes | UD ui.wellness.standupMinutes | WellnessTabView.swift:17,63 | 🔁 |

ReadingProgress 只读 reading.json 页码、StandupTimer durationMin 来自 item 配置（R57 表结论，现状复核未变）——readingGoal/standupMinutes 为 R58-a G3 偏好记忆。

### 21. lifestyle 生活

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| 宠物种类 petType | IJ pixelPet.petType | PixelPet.swift:16,25-26,86 | ✅ |
| 显示宠物 showPixelPet | UD ui.lifestyle.showPixelPet | LifestyleTabView.swift:100,117 | 🔁 |
| 外卖平台 foodPlatforms | UD ui.lifestyle.foodPlatforms | LifestyleTabView.swift:97,114；FoodDelivery 是 mock 无平台过滤(FoodDelivery.swift:5-7) | 🔁 |
| 穿衣城市 outfitCity | UD ui.lifestyle.outfitCity | LifestyleTabView.swift:98,115；WeatherOutfit 用 lat/lon 不读城市名(WeatherOutfit.swift:22) | 🔁 |
| 每日一句分类 quoteCategories | UD ui.lifestyle.quoteCategories | LifestyleTabView.swift:99,116；DailyQuote 无分类参数消费 | 🔁 |

### 22. tools 快捷工具

| 现有设置项 | 通道 | 运行时消费者(文件:行号) | 判定 |
|---|---|---|---|
| ~~历史条数 clipboardHistorySize~~ | —（@State 无任何 persist 与读取；ClipboardHistory maxItems 来自 item 配置 ClipboardHistory.swift:51,74，persistCap 硬编码 20 :67） | 零读者 | ❌ 移除(r60-b) |
| ~~默认哈希 defaultHashAlgo~~ | —（@State 无 persist；HashCalc algorithm 来自 item 配置 HashCalc.swift:11-15） | 零读者 | ❌ 移除(r60-b) |
| ~~默认布局 windowLayout~~ | —（@State 无 persist；窗口布局由 item align 决定） | 零读者 | ❌ 移除(r60-b) |
| 音量律动 source/gain×3/damping×3 | UD TBSpectrumSettings 六键 | AudioSpectrumBarItem.swift:288,380,652-654,664 | ✅ |
| 延迟测试 endpoint/bypassProxy | UD apilatency.endpoint/bypassProxy | ApiLatency.swift:32,36 | ✅ |
| 正则规则 regexRules | TBRegexRules 持久化 | RegexReference.swift:31、RegexTester.swift:30 | ✅ |
| ~~快捷回复 quickReplies~~ | —（@State 空 list 无 persist；quickReply item 用 configPath 指向 ~/quickReplies.json，EditorSchema.swift:322-323） | 零读者 | ❌ 移除(r60-b) |

## 三、死控件处置明细（本轮执行，附对照表行号证据）

| # | 位置 | 控件 | 证据链 | 处置 |
|---|---|---|---|---|
| D1 | WeatherTabView.swift:23,194-204 | 「预报小时数」滑杆 | @State 初值 0 从不回填（loadFromJSON :244-256 无此键）；onChange→saveDebounced→saveToJSON :259-276 写字典不含 forecastHours；.weather 解码（ItemsParsing.swift:302）无预报键；ChinaWeatherProvider/WeatherBarItem 全文无预报小时消费 | 删除 @State+forecastSection 整段+body 引用；searchKeywords 同步去「预报/Forescast」词条？——否，词条保留（搜索命中提示语无害），只删控件 |
| D2 | ToolsTabView.swift:12,50-70 | 「历史条数」滑杆 | @State 8 无 persist 无读者；ClipboardHistory.swift:74 maxItems 参数来自 item 配置 | 删控件行+@State |
| D3 | ToolsTabView.swift:13,59-66 | 「默认哈希」分段 | @State sha256 无 persist；HashCalc.swift:11-15 algorithm 来自 item 配置 | 删控件行+@State；剪贴板与哈希段合并为单行说明或整段删除——按 §5 收敛原则整段删除并保留音量律动等活段 |
| D4 | ToolsTabView.swift:14,72-87 | 「默认布局」分段 | @State left 无 persist 无读者 | 同上删除 |
| D5 | ToolsTabView.swift:15,89-101 | 「快捷回复」列表 | @State [] 无 persist；quickReply item 消费 configPath 外部 json（EditorSchema.swift:322-323），与本列表无关 | 删整段+body 引用 |

D1~D5 均为零读者零写者的「活跃 UI 假象」，延续 R57 §5 规则口径（隐藏 UI；因无既有持久化键，无需 deprecated 标注）。ToolsTab searchKeywords 中「剪贴板/哈希/正则…」词条保留（音量律动/延迟测试/正则规则仍真实存在且命中）。

## 四、有运行时读者但未注册 domainFields 的域（§4.4 补注册试点）

| 域 | 现状 | 读者证据 | 本轮动作 |
|---|---|---|---|
| notification | 手写 @State ×3 | PomodoroBarItem.swift:126,132 | **注册** domainFields["notification"]（3 toggle，UD 通道）+ NotificationTab schema 化 |
| weather | 手写 @State ×6 | WeatherBarItem.swift:71-92 | **注册** domainFields["weather"]（apiSource/units/iconType 分段 + showHumidity/showWind 开关；cities 为列表控件走 EditableListView 既有通道不进 bridge）+ WeatherTab 显示段 schema 化 |
| dock | 手写 @State ×6+apps 双 scope | AppScrubberTouchBarItem.swift:28-31,41 | 不动：scope 切换/apps 双通道/防抖语义复杂，试点性价比低，留给 Phase2 |
| lyrics/general/services | LIC/UD/Keychain 混合 | 见 §二 | 不动：非 items.json/UD 单通道，bridge 现有 ControlKind 不覆盖字体选择器/密钥行 |

试点选 notification+weather 理由：①两域字段全部有 文件:行号 级运行时读者（§4.4 金标准）；②通道单一（notification 纯 UD bool、weather 纯 IJ string/bool），无需扩 ControlKind；③文件所有权干净（两 TabView 均 r60-b 属主，与 r60-c 五域接线表零交集）。

## 五、用户点名缺口核验

1. **音乐源安装状态徽标**：LyricsTabView 音乐源 8 行目前无安装检测（MusicSourceRow.swift:1206-1232 纯静态行）。→ **r60-a 所有权限责范围内**（§4.2 MusicSourceRow 安装态徽标，NSWorkspace.urlForApplication），本卡不改 MusicSourceRow（r60-a 属主 MusicBarItem 徽标段相邻，避免冲突），已在 §二 lyrics 节标注移交。
2. **刷新行为类设置项**：
   - 歌词类 tab：刷新节奏由各 item 的 refreshInterval（items.json）决定，编辑器侧可调；设置侧无需重复入口。桌面歌词/颜色/滚动均实时生效（LIC Combine 即时持久化+applyConfig）。**无缺失**。
   - 媒体类 tab：MusicBarItem 轮询走 item 配置 interval（MusicBarItem.swift:50）；RSS/股票/天气/快递刷新间隔均已有所属 tab 入口且全部有消费者（§二）。**无缺失**。
   - 唯一疑似缺口「通知免打扰时段」类高级功能不在本轮范围（新功能≠缺口），不做臆造。
3. 结论：用户点名的两项中，「安装态」由 r60-a 落地；「刷新行为」经核验现有入口完备，无新增必要。

## 六、验收自证

- 对照表覆盖 22/22 tab（§二编号 1~22），每行均有 文件:行号 或明确的「零读者」取证；无「待核」臆断项（BeeCount/Savings 两处 widget 链缺口为 R57 已登记的历史遗留，非本表新判）。
- EditorSchema 152 条属性 key/displayName/type 零触碰（本轮未改 EditorSchema*.swift）。
- 移除/注册逐条见 §三/§四，代码侧 diff 与本表一一对应。
