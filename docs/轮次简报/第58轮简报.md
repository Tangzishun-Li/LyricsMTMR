# 第 58 轮简报

> 一页人话，面向用户。本轮主题：设置里「拨了开关重启就忘」的 UI 状态全部接上持久化（G1~G5），记账 widget 真正消费自己写出的预算数据（G6）并接入 BeeCount 今日收支（G7），外加股票组件设置页 schema 化试点。

## 本轮主题

UI 态持久化收尾 + 记账 widget 消费闭环 + 设置 schema 化第二批试点。（R57 审计发现的两项遗留——@State 即烬开关、savings.json 四键无消费者——本轮全部闭环。）

## 做了什么（4 张执行卡）

1. **Homekit / Package / Wellness 三页 7 个开关落盘（a 卡 G1~G3）**——每个开关改值即时写回 UserDefaults，启动时水合恢复；缺键冷启动取契约默认值，注入非默认值杀进程重启逐项保留（双路径实证）。
2. **Lifestyle 四开关+城市输入、AI 提示词模板列表落盘（b 卡 G4/G5）**——防抖写盘；promptTemplates 选 [String] 整存整取（整行编辑器语义）。
3. **记账 widget 消费闭环（c 卡 G6/G7）**——SavingsGoalItem 真正读取 savings.json 四键：本月已存/目标进度条、月预算超支 ⚠ 前缀（可关闭）、币种后缀（CNY→¥ 等）；BeeCount 凭据可用时副行追加今日收支摘要，凭据缺失/网络失败一律静默回退原显示，绝不拖垮记账主显示。
4. **股票组件设置页 schema 化试点（d 卡 Phase2）**——stock 域 6 字段注册进 SettingsSchemaBridge 共享桥接层（首批 .slider/.segmented 控件扩展），StockTabView 显示段改为配置驱动渲染；EditorSchema 零改动（97 类型/152 属性不变），为剩余设置页批量迁移探路。

## 关键数据

- 合并链完整：先补并 r57-W3 分支（h/i/j 减脂链此前只在分支上，本轮 fast-forward 进链）再按序合并 a→b→c→d
- 冲突 7 处全部按预案「保双方」解清：轨道文本 §8 日志行 ×3 + pbxproj ×4（W3 测试文件 vs c 卡新测试文件），`^<<<<<<<` 全仓 0 残留
- 每卡合并后增量构建 BUILD SUCCEEDED ×4 + 整体构建通过
- 受影响套件 16 用例 0 失败（ExpenseBudgetContractTests 5 新增 + DeadSettingContractTests 5 + SettingsTabCacheTests 6）
- 锚点巡检 PASS 60 / WARN 23 / INFO 5 / ERROR 0（连续第 39 轮）；REGISTRY 195 行（W3 重写带出 record 级位移 7 处，known 值已按惯例修正登记）
- 工程版本 0.57/482 → 0.58/483
- 空间释放：删除 r58 四张执行卡 + r57 第三波残留共 8 个 worktree 与分支

## 遗留决策点（需用户确认）

- **§5 契约三处分歧（a 卡标注，待裁决契约或 UI 修订）**：
  ① package.notifyOnUpdate 契约默认 false vs UI 旧硬编码 true（已按契约切为 false）；
  ② wellness.readingGoal 契约 Int 页/天，但滑杆单位文案仍是「分」（落盘值正确，文案待改）；
  ③ wellness.standupMinutes 契约缺省 45，超出滑杆范围 5...30（首拖前以超程值显示）。
- BeeCount 链路 GUI 端到端实测归入真机冒烟系列挂账（单测与静默回退逻辑已覆盖）。
