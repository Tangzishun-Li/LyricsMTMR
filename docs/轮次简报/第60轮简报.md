# 第 60 轮简报

> 一页人话，面向用户。本轮主题：两道防线 + 一笔总账——启动时不再被 AppleScript 权限弹窗突袭、改设置后 Touch Bar 能自动跟着变（变不了的会告诉你）、22 个设置页全部和真实功能对了一遍账。

## 本轮主题

启动 TCC 弹窗防线 + 设置热更新与需刷新提示 + 设置项全量审计（3 张执行卡，仅 2 处工程文件登记冲突按惯例保双方）。

## 做了什么（3 张执行卡）

1. **启动 TCC 弹窗防线（a 卡）**——自定义 AppleScript 按钮里引用了外部应用（如 Spotify）的示例脚本，以前应用一启动就触发系统权限弹窗；现在守卫会在首次自动执行前改为占位「▶」等你点一下才放行，点完之后正常自循环，默认预设里的三组外部示例块也已清理。顺带修好两个前轮遗留缺陷（脚本描述误返回、放行时误清用户动作），音乐源列表给未安装的应用加了灰字「未安装」徽标。
2. **设置全量审计+死控件处置（b 卡）**——交付 22 个 tab × 87 个设置项的对照表（每行都有 文件:行号 级运行时证据，零「待核」）；移除 5 处谁也不读的死控件（天气预报小时数、工具页历史条数/默认哈希/默认布局/快捷回复）；通知和天气两个域补注册进 schema 渲染桥（§4.4 试点），编辑器 schema 152 条零触碰。
3. **设置热更新与需刷新提示（c 卡）**——番茄钟/股票/系统监视器/日历等能安全热更新的设置域，保存后约半秒内 Touch Bar 自动变化（内部去抖合窗，不闪屏）；不能安全热更新的域保存后会显示横幅提示并附「立即刷新」按钮；顺带修复 General 黑名单缓存不同步的真实缺口。

## 关键数据

- 合并链完整：a(ee70b1a)→b(294912c)→c(8cd69ae) 按序合并，**三卡文件交集为空**（a∩c 仅工程登记文件各自追加行，保双方解清）
- 每卡增量构建 BUILD SUCCEEDED ×3 + 整体构建通过
- 受影响套件定向 **243 用例 0 失败**（TCCGuard 12 新增 + RefreshAdvisor 10 新增 + 注册表/工厂/契约等邻接回归）
- 全量回归本轮不触发（隔代规则：R59 已跑，R60 跳过，R61 候选）
- 锚点巡检 PASS 60 / WARN 23 / INFO 5 / ERROR 0（连续第 41 轮）；REGISTRY 197 行
- 工程版本 0.59/484 → **0.60/485**

## 遗留决策点（需用户确认）

- 编辑器四文件 ~1683 行死代码簇（r57-d 定性、r60-b 复核仍零调用）：建议后续开专项卡处置，删除或接线需要您拍板方向
- remindEnabled/remindMinutes 展示态复核延续挂账（待 §5 审计口径定夺）
- 真机观感两项新增待验：TCC 首次「▶」点按放行、Banner 去抖合窗演示
