# 第56轮简报

**日期**：2026-08-19
**主题**：App Sandbox 启用与临时例外配置（安全合规维度，R43 登记候选闭环）

## 本轮做了什么

- **A卡（主线）**：App Sandbox 启用与临时例外配置——安全合规维度（R43 登记候选）
  - MTMR.entitlements 中 `com.apple.security.app-sandbox` 从 false → true
  - 新增 7 条 entitlements：camera/microphone/network.server 显式 false + 4 类临时例外（apple-events 5 targets / absolute-path.read-only 9 paths / home-user-selected-read-write / mach-lookup 2 names）
  - 新增 SandboxConfigContractTests.swift（18 测试方法 / 15 项自动化断言全 PASS）
  - BUILD SUCCEEDED（Debug, unsigned）
  - 已知限制：Sparkle 测试主机崩溃为预存问题（clean branch 同样复现），本轮通过 plist 自动化断言替代验证
- **B卡（文档）**：README 更新日志补登 v0.56 + 12 项 grep 实证全过 + 版本决策建议 0.56/481
- **C卡（维护·轻量轮）**：锚点巡检复跑 PASS 67/WARN 16/INFO 5/ERROR 0（连续第37轮 0 ERROR）+ round-55 清理（4 worktree + 4 branch）+ 遗留盘点 11 项延续 0 新发现

## 关键数据

| 指标 | 值 |
|------|-----|
| 全量回归 | 561 用例基线（本轮触发全量回归，隔代规则） |
| 受影响套件实证 | 72 用例 0 失败（SandboxConfigContractTests 15 + StockMarketHoursTests 16 + UserDefaultsContractTests 9 + PrivacyManifestContractTests 13 + SecretsManagerContractTests 13 + 其他） |
| 锚点巡检 | PASS 67 / WARN 16 / INFO 5 / ERROR 0（连续第37轮 0 ERROR） |
| REGISTRY | 190 行 |
| Info.plist | 0.56 / 481 |
| 版本号 | v0.56（当前开发版本） |
| Entitlements | 13 个 key（含 sandbox=true + 4 类临时例外） |

## 遗留决策点

- ITER-15 镜像窗事件驱动刷新：决策门 4 问仍待用户确认
- 真机冒烟清单：内存修复 3 项 + 观感系列（+R52 滚动观感/follow 跟随位置，用户待办）
- R51 A 卡遗留 2 项延续：真机观感冒烟 / 无重置位置 UI
- R50 A 卡 3 项挂账延续：PrivacyInfo 真机生效 / Coinbase 网络超时 / 剪贴板权限提示实机观察
- archive/ 死代码清理（1246 行）——评估结论可安全清理，用户确认后再做

## 需要用户确认的事

- 真机冒烟：内存修复 + 独立配色开关 + App Sandbox + 观感系列需真机验证
- ITER-15 镜像窗事件驱动刷新决策门 4 问
- archive/ 死代码 1246 行清理确认
