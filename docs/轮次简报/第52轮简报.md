# 第 52 轮简报（功能/优化迭代第 40 轮）

> 一页人话版。全量实证记录见 iteration-log.md「第 52 轮」段；index.md 为轮次索引。

## 本轮主题
R51 遗留候选闭环：桌面歌词窗口长行 marquee（前端体验/UI 维度，接 R51 A 卡遗留 4 项第 1 项）。

## 做了什么
- **A 卡（实现）桌面歌词窗口长行 marquee**：超过面板宽度的歌词行不再直接截断——有 timetag 的卡拉 OK 行用 follow 跟随滚动（正在演唱的字保持在可视区 65%，逐字高亮不脱位），无 timetag 的长行循环 marquee（30fps，仅条件性运行非常驻）；行切换归位、隐藏/占位/开关即停即启、卡内裁剪。设置页歌词 Tab 新增「长行滚动」开关（默认开）。D11 遗留「滚动与逐字高亮共用当前行标签」评估闭环。
- **B 卡（文档）**：README 更新日志补登 v0.52 + 12 项现状核对全过 + 版本决策建议 0.52/477（收口已落地）。
- **C 卡（维护·轻量轮）**：锚点巡检第 32 轮连续 0 ERROR（REGISTRY 178）；round-51 清理（4 worktree + 4 分支全清零残留）；遗留盘点新增发现 1 项（issue #1 被维护者关闭）。

## 关键数据
- 用例：新增 13（DesktopLyricsMarqueeTests，红 5 failures→绿 13/13 双跑断言零放宽）；受影响套件+金丝雀 109 用例 0 失败；全量回归 533 已在本轮分解前实证（隔代规则触发，xcresulttool passedTests=533/failed=0）
- 锚点巡检：PASS 67 / WARN 16 / INFO 5 / ERROR 0，连续第 32 轮 0 ERROR（REGISTRY 178 行）
- 版本：Info.plist 0.52/477（收口落地），README v0.52（当前开发版本）
- 合并：C→A→B 三次合并提交（89d169b/4484c8c/097926e），冲突全清零残留；报告归档 logs/第52轮/ 4 份

## 遗留决策点（挂账）
- ITER-15 镜像窗事件驱动刷新：决策门 4 问仍待用户确认
- 真机冒烟清单（内存修复 3 项 + 观感系列，+本轮滚动观感/跟随位置）：用户待办
- R50 A 卡 3 项挂账：PrivacyInfo 真机生效 / Coinbase 网络超时 / 剪贴板权限提示实机观察
- R51 A 卡遗留 4 项：长行截断无 marquee **本轮闭环**；真机观感冒烟 / 独立配色开关 / 无重置位置 UI 延续
- 本轮新增 0 项；issue #1 已闭环（2026-08-17 维护者关闭，自第 8 轮挂账 44+ 轮）

## 需要用户确认的事
- 长行 marquee 已合入 main（默认开启，设置页歌词 Tab 可关）。真机观感（滚动流畅度、follow 跟随位置）待用户冒烟反馈。
- ITER-15 决策门 4 问长期挂账，等用户确认是否值得做镜像窗事件驱动刷新。
