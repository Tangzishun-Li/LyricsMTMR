# 第 57 轮简报

> 一页人话，面向用户。本轮为双波 INTEG 收口：第一波「设置体系统一治理」4 卡 + 第二波「性能减脂」3 卡。

## 本轮主题

设置体系治理（把设置里「开关拨了没反应」的死键清干净）+ 性能减脂（把看不见的空转开销省下来）。

## 做了什么（7 张执行卡全部合并）

**设置治理四件：**
1. **死设置审计与接线**——38 个持久化键 × 21 个设置页全查一遍。真该生效的补上线（通知声音），没用的从界面摘掉（3 个无生产者开关），1 个被冤枉的还了清白（RSSHub 地址其实有消费者）。清单归档 logs/第57轮/。
2. **设置页 schema 化第一步**——新建共享桥接层，番茄钟设置页改为配置驱动渲染，顺手删了 5 个纯摆设控件。
3. **侧栏重排**——5 组 22 页平铺收敛为常用/数据/更多设置（默认折叠）3 组 + 关于；搜索关键词全覆盖，折叠组点中自动展开。
4. **设置⇄编辑器双向跳转**——设置里能右键「在编辑器中打开」，编辑器里能一键跳回对应域的设置页。

**性能减脂三件：**
5. **时钟刷新降频**——分钟级显示的时钟从每秒刷 1 次降到每 30 秒 1 次（带秒的模板不受影响）。
6. **浮层关了就停表**——呼吸引导(20Hz)/站起计时/阅读计时三个浮层关闭后定时器立即停转，不再后台空转。
7. **歌词动画可见性守卫**——桌面歌词被隐藏时不再启动动画定时器，恢复可见自动续播。

## 关键数据

- 合并链完整：7 commit 全部落在本分支；pbxproj 冲突 ×4 处按预案「保双方」解清，0 残留
- 每卡合并后增量构建 BUILD SUCCEEDED ×7 + 整体构建通过
- 受影响套件 184 用例 0 失败
- 锚点巡检 PASS 67 / WARN 16 / ERROR 0（连续第 38 轮）
- 工程版本 0.56/481 → 0.57/482
- 空间释放：删除 r57 七张执行卡的 worktree + 分支

## 遗留决策点（需用户确认）

- **@State 即烬开关 ~20 处**：不少设置页开关是临时状态、重启即丢——要不要 R58 统一立卡治理？
- **Expense savings.json 四键无消费者**：同上，建议 R58 处置。
- ITER-15 镜像窗事件驱动刷新：仍在等「使用场景 4 问」的用户答复（延续挂账）。

## 简报目录

- [index](index.md)
