# 第 53 轮简报（功能/优化迭代第 41 轮）

> 一页人话版。全量实证记录见 iteration-log.md「第 53 轮」段；index.md 为轮次索引。

## 本轮主题
R47 观察项双项治理：数据与存储维度（lyricsSelectionCache reset/export 隔离 + selectedThemeIndex 缺键默认 0 契约化）。

## 做了什么
- **A 卡（实现/优化）R47 观察项双项治理**：
  - lyricsSelectionCache 隔离：SettingsSync 新增 `runtimeCacheKeys` 排除列表，`resetAllToDefaults` 和 `exportProfile` 跳过运行时缓存键，大契约（前缀批量删）不变。发现 `Data` 类型会导致 exportProfile 返回 nil（JSONSerialization 无法序列化 Data）。
  - selectedThemeIndex 缺键默认 0 契约化：新增 3 契约单测（6→9 用例），正/反断言钉住缺键=0→首主题有效语义。
  - 全量 549 用例 0 失败实证（533 基线+13 R52 marquee+3 新增零偏差，101.2s）。
- **B 卡（文档）**：README 更新日志补登 v0.53（第 52 轮 marquee 条目）+ 12 项现状核对全过 + 版本决策建议 0.53/478（收口已落地）。纯文档轮零 Swift 改动。
- **C 卡（维护·轻量轮）**：锚点巡检第 33 轮连续 0 ERROR（REGISTRY 184）；round-52 遗留清理复核确认 0 残留；遗留盘点 6 项逐项确认新增发现 0 项。

## 关键数据
- 用例：549 用例 0 失败（533 基线+13 R52 marquee+3 R53 新增）；受影响套件 118 用例 0 失败
- 锚点巡检：PASS 67 / WARN 16 / INFO 5 / ERROR 0，连续第 33 轮 0 ERROR（REGISTRY 184 行）
- 版本：Info.plist 0.53/478（收口落地），README v0.53（当前开发版本）
- 合并：B+C（fast-forward + 冲突解决 2 处）→ A（冲突解决 2 处），3 子卡全部合入父分支

## 遗留决策点（挂账）
- ITER-15 镜像窗事件驱动刷新：决策门 4 问仍待用户确认
- 真机冒烟清单（内存修复 3 项 + 观感系列 + marquee 真机观感）：用户待办
- R50 A 卡 3 项挂账：PrivacyInfo 真机生效 / Coinbase 网络超时 / 剪贴板权限提示实机观察
- R51 A 卡遗留 3 项延续：真机观感冒烟 / 独立配色开关 / 无重置位置 UI
- R47 A 卡 3 项：2 项本轮闭环（lyricsSelectionCache + selectedThemeIndex），无前缀键 3 处延续不动
- 本轮新增 0 项；issue #1 已闭环

## 需要用户确认的事
- R47 观察项双项治理已合入 main（reset/export 不再清歌词缓存，selectedThemeIndex 契约化）。
- ITER-15 决策门 4 问长期挂账，等用户确认是否值得做镜像窗事件驱动刷新。
