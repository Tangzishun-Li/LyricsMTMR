# 第 51 轮简报（功能/优化迭代第 39 轮）

> 一页人话版。全量实证记录见 iteration-log.md「第 51 轮」段；index.md 为轮次索引。

## 本轮主题
歌词产品空白面补全：桌面歌词窗口 MVP（前端体验/UI 维度，R45 后隔 5 轮）。

## 做了什么
- **A 卡（实现）桌面歌词窗口 MVP**：无边框/透明/置顶/可拖拽的非激活悬浮窗（NSPanel），三行竖排 = 前/后 1 行上下文 + 当前行卡拉 OK 逐字高亮；无歌词三态占位；位置记忆（拖拽落盘，程序化移动不覆盖）；设置页歌词 Tab 新增「桌面歌词」开关 + 字号滑块；AppDelegate 生命周期接线（默认关，退出清理）。纯逻辑抽 3 组（进度映射/行布局/可见性状态机），LyricsEngine 改调同一纯函数防漂移。
- **B 卡（文档）**：README 更新日志补登 v0.51 + 12 项现状核对全过 + 版本决策建议 0.51/476（收口已落地）。
- **C 卡（维护·轻量轮）**：锚点巡检第 30 轮连续 0 ERROR；round-50 清理（4 孤儿目录 + 4 分支，删除前 0 ahead 零丢失）；遗留盘点新增 0 项。

## 关键数据
- 用例：新增 20（DesktopLyricsWindowTests，红 7 failures→绿 20/20 双跑断言零放宽）；受影响套件+金丝雀 81 用例 0 失败；全量回归 533 由第 52 轮承担（隔代规则）
- 锚点巡检：PASS 67 / WARN 16 / INFO 5 / ERROR 0，连续第 31 轮 0 ERROR（REGISTRY 176 行）
- 版本：Info.plist 0.51/476（收口落地），README v0.51（当前开发版本）
- 合并：C→A→B 三次合并提交（dd750f1/706cdec/1bb26a3），冲突全清零残留；报告归档 logs/第51轮/ 4 份

## 遗留决策点（挂账）
- ITER-15 镜像窗事件驱动刷新：决策门 4 问仍待用户确认
- 真机冒烟清单（内存修复 3 项 + 观感系列）：用户待办
- R50 A 卡 3 项挂账：PrivacyInfo 真机生效 / Coinbase 网络超时 / 剪贴板权限提示实机观察
- 本轮新增 4 项：桌面长行截断无 marquee（候选）/ 真机观感冒烟（NSPanel 层级透明多屏）/ 桌面窗口独立配色开关 / 无重置位置 UI

## 需要用户确认的事
- 桌面歌词窗口 MVP 已合入 main（默认关闭，设置页歌词 Tab 开启）。真机/日常观感（非激活层级、透明、多屏行为）待用户冒烟反馈。
- ITER-15 决策门 4 问长期挂账，等用户确认是否值得做镜像窗事件驱动刷新。
