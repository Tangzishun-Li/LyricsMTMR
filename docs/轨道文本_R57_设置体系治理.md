# R57 轨道文本 — 设置体系统一治理（权威规范 v1）

> 本文件是第 57 轮所有子卡的**唯一对接规范**。子卡 body 只写三行（必读哪节/我的文件/我的验收），细节一律回本文件。
> 正文修订权归父编排者+INTEG；子卡只许在文末追加日志行。

## §1 规模红线

- 禁改：`LyricsRendering/`、歌词引擎、`items.json` 配置格式、`ItemsParsing.swift` 解析行为
- 禁新增第三方依赖；Swift only；每卡改动面 ≤ 15 文件
- 用户红线：**存量用户的 UserDefaults 数据无损**——死键一律「接线消费 or 隐藏开关」，禁止删除键定义
- 测试纪律（用户令）：少测试快迭代。每卡新增契约测试 ≤1 文件；**不跑全量回归**（隔代规则 R56 已触发，R57 豁免）；验收以 `xcodebuild build SUCCEEDED` 为准

## §2 环境事实（已核实 2026-08-23）

- 仓库根：`/Users/litz/codespace/MTMR with LyricsX`（无尾空格）；worktree 由看板派发到 `.worktrees/<task-id>/`
- 构建：`cd LyricsMTMR && xcodebuild -project LyricsMTMR.xcodeproj -scheme MTMR -configuration Debug CODE_SIGNING_ALLOWED=NO build -derivedDataPath .build/DerivedData 2>&1 | tail -5`
- GitHub 需代理：`export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890`
- 设置窗口骨架：`LyricsMTMR/MTMR/Preferences/UnifiedSettingsWindowController.swift`
  - `SettingsTab` enum（22 case）/`SettingsGroup`（5 组）位于 :346-500
  - Tab 视图缓存 LRU 位于 :1076+；搜索匹配 :1307+
- 编辑器 schema：`LyricsMTMR/MTMR/Preferences/Editor/EditorSchema.swift`（97 类型/152 ItemProperty/`sectionedProperties`/`dependsOn`/`validation` 基建齐全）
- 编辑器渲染器：`PropertyInspector.swift`（905 行，schema 驱动控件族已可用）
- 运行时分发：`ItemsParsing.swift`（字典驱动 decode 注册表 :593+，113-case 工厂在 `BarItemFactory.swift`）
- 持久化：`App/AppSettings.swift` `@UserDefault` 属性包装器；键注册表 `UDKey`

## §3 调研结论（父卡 grep 实证，2026-08-23）

| # | 问题 | 证据 |
|---|------|------|
| F1 | 死设置：4 个通知开关只写不读 | 全 app 仅 `PomodoroBarItem.swift:126`（global+pomodoro）与 DnDBarItem 发通知；`notificationsSound/Package/DDL/Birthday` 零运行时读 |
| F2 | 死设置：`AppSettings.rssRSSHubBase` 只写不读 | RSSTabView.swift:531,545 写入；运行时零读 |
| F3 | 两套元数据：设置 tab 手写 UI 与 EditorSchema 完全独立 | 22 tab ≈20K 行手写布局；schema 152 属性仅服务编辑器 inspector |
| F4 | item 级设置无处安放 | 152 属性无法逐个开 tab；schema 渲染基建已有但未接入设置窗口 |
| F5 | 疑似域错位待查 | StockTabView 的图表设置 vs stock widget 参数（stocks/displayMode/showChart）关系需 AST 审计 |

## §4 目录所有权表（每文件唯一属主；别人文件只读）

| 属主 | 文件 | 动作 |
|------|------|------|
| A 卡 | `App/AppSettings.swift` | 允许增删属性行/补注释；禁止重排既有行 |
| A 卡 | `Preferences/{NotificationTabView,RSSTabView,AITabView,ExpenseTabView,LifestyleTabView,WellnessTabView,WeatherTabView,GeneralTabView,PomodoroTabView,SystemMonitorTabView,ToolsTabView,DockTabView,HomekitTabView,CalendarTabView,PackageTabView}.swift` | 死开关处理（见 §5 规则） |
| A 卡 | `MTMRTests/DeadSettingContractTests.swift` | 新建（唯一允许的新测试文件） |
| B 卡 | `Preferences/Components/SettingsSchemaBridge.swift` | 新建（共享层本体） |
| B 卡 | `Preferences/Editor/EditorSchema.swift` | 允许把静态元数据迁往 bridge 后转发引用；**禁止改 152 条属性的 key/displayName/type 值** |
| B 卡 | `Preferences/PomodoroTabView.swift` | 试点改造（schema 渲染替换手写段）※与 A 卡互斥：A 若先动 pomodoro 则 B 落 rebase 后再改 |
| C 卡 | `Preferences/UnifiedSettingsWindowController.swift` | SettingsTab/SettingsGroup enum 段 + NavItem/tabContainer 相关 |
| C 卡 | 被合并/降级 tab 的对应 *TabView.swift 文件级移动 | 仅限 C 卡（若与 A 所有权重叠，A 先合、C rebase） |
| D 卡 | `Preferences/Editor/EditorTabView.swift`、`Preferences/Editor/PropertyInspector.swift` | 双向跳转 UI |
| D 卡 | `UnifiedSettingsWindowController.swift` | **仅限**追加 `@Published var pendingNavigation` 及其 handler（与 C 的 enum 段不同区域，冲突由 INTEG 收口） |
| INTEG | 一切冲突 | 勤合并当场解决 |

## §5 死设置处理规则（F1/F2 类问题的唯一正确动作）

1. 语义上应当生效的开关 → **接线**：在合理的消费点补读（例：DDL 提醒若无发送方，则标注「无生产者」走规则 2）
2. 无生产者/无意义 → **从 tab 隐藏开关 UI** + AppSettings 属性标 `@available(*, deprecated)` 注释保留键（存量数据无损）
3. 每个处理决定记入 `docs/R57_死设置审计清单.md`（A 卡产出，含 键名/证据 文件:行号/处置）
4. 完成判据：清单内每键 grep 复核「不再出现只写不读的活跃 UI」

## §6 API 契约（冻结到字段级，前后卡并行不打架）

```swift
// B 卡产出 — SettingsSchemaBridge.swift（签名冻结）
enum SettingsSchema {
    /// 设置窗口侧可用的 item 属性元数据（转发 EditorSchema.items，未来演进为设置侧独立扩展点）
    static func properties(forType type: String) -> [ItemProperty]
    static func sections(forType type: String) -> [(name: String, props: [ItemProperty])]
    /// 域级全局设置描述（tab 顶部的非 item 设置），Phase2 扩展用
    static var domainSummaries: [String: String]  // type -> 一句话说明
}

// D 卡产出 — 跳转通知（Notification.Name 冻结）
extension Notification.Name {
    /// 设置窗口 → 编辑器：定位并选中某 item。userInfo: ["type": String, "index": Int?]
    static let settingsNavigateToItem = Notification.Name("com.lyricsmtmr.settings.navigateToItem")
    /// 编辑器 → 设置窗口：打开某域 tab。userInfo: ["tab": String]  // SettingsTab.rawValue
    static let editorRequestOpenSettings = Notification.Name("com.lyricsmtmr.editor.openSettings")
}
```

## §7 验收总则

- 每卡：`xcodebuild … build` 末行 `BUILD SUCCEEDED`；自测自证后 `kanban_complete`（附 1-3 句轨迹素材），**不 block review-required**
- A 卡额外：审计清单覆盖全部 21 个 *TabView（About 除外）+ AppSettings 全部 37 个 @UserDefault
- B 卡额外：试点 tab（pomodoro）渲染结果与改造前字段一一对应（截图或 diff 说明）
- C 卡额外：合并后 tab 总数与分组写入卡 comment；搜索索引（searchKeywords）同步更新
- D 卡额外：两条跳转方向各有 ≥1 处真实入口按钮
- 报告归档：`logs/第57轮/`（INTEG 收口时 git mv）
- 版本号：收口卡统一 Info.plist 0.56/481 → 0.57/482（执行卡勿动）
- **新建 .swift 文件必须登记进 Xcode 工程**（objectVersion 54 无文件系统同步组）：
  `python3 LyricsMTMR/Scripts/add_files.py Preferences:Components/Foo.swift Tests:FooTests.swift`
  （幂等、UUID 按文件名确定；App 文件用组前缀，测试文件用 `Tests:` 前缀。多卡都在段落末尾追加 → pbxproj 合并冲突保留双方即可）

## §8 迭代节奏

- 一次整合勤合并：INTEG 在全部执行卡 done 后立即逐个 merge（冲突当场解）
- 子卡完成即 comment 轨迹素材；禁止子卡互相等待（§4 所有权冲突场景除外，后合者 rebase）
- 追加式轨道更新日志（子卡只在此追加一行）：

```
R57-open | 2026-08-23 | 父卡建立本轨道文本 | base: 7c3a46f (tag pre-r57-settings-audit)
```
