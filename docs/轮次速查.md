# LyricsMTMR 迭代速查表（racecard）

> 父任务开工**先扫这里**：上一轮做了什么、候选在哪、哪些已论证不动——防止重复迭代。
> 维护：每轮收口时更新（父任务收口动作，与简报同步）。最近 10 轮滚动保留，更早轮次见 iteration-log.md 摘要段。
> 最后更新：R59 收口后（契约裁决落 UI + SchemaBridge Phase2 两域 + 桌面歌词位置守护，Info.plist 0.59/484，main 已推送）

## 最近 10 轮（防重复做）

| 轮 | 主题（维度） | 一句话结果 |
|----|--------------|-----------|
| R59 | 契约裁决落 UI + SchemaBridge Phase2 两域 + 桌面歌词位置守护（UI/前端体验） | a/b/c 三卡零冲突全并：a §5 三处分歧落 UI(readingGoal 页/天 5...100+水合钳制/standupMinutes 5...90/notifyOnUpdate 默认关闭副标题)+b systemMonitor/calendar 两域 schema 驱动渲染(domainFields 各 6 字段)+c 重置窗口位置 UI+DesktopLyricsFrameGuard 屏外回退守卫；受影响套件 64 用例 0 失败；全量回归触发 641 用例 0 断言失败；Info.plist 0.59/484；锚点第 40 轮 0 ERROR；REGISTRY 196 |
| R58 | UI 态持久化 + 记账消费闭环 + Schema 试点二（数据存储/UI） | a/b/c/d 四卡全并：G1~G5 十六键落盘(Homekit/Package/Wellness/Lifestyle/AI)+G6 SavingsGoalItem 消费 savings.json 四键+G7 BeeCount 今日收支摘要静默回退+d 卡 stock 域 schema 驱动试点；16 用例 0 失败；Info.plist 0.58/483；锚点第 39 轮 0 ERROR；REGISTRY 195 |
| R57 | 设置体系统一治理 + 性能减脂（双波 INTEG） | A 死设置审计接线(38键处置+sound接线+3开关隐藏)+B SettingsSchemaBridge 共享层+C 侧栏 5组22页收敛 3组折叠+D 设置⇄编辑器双向跳转+E 时钟精度分档(分钟格式1s→30s)+F popover 停表(呼吸/站起/阅读离屏停转)+G 歌词动画可见性守卫；184 用例 0 失败；Info.plist 0.57/482；锚点第 38 轮 0 ERROR；REGISTRY 194 |
| R57·W3 | 性能减脂第三波收口（h/i/j INTEG） | h 镜像窗事件驱动刷新(0.1s 兜底轮询→1s 脏检查心跳+attributedTitle/image didSet 事件化)+i TBPopoverItem 浮层关闭免全量重建(还配置+原对象重 present，PopoverDismissTests 6 新用例)+j archive/ 死代码清理(16 文件净删 -1344 行)；受影响套件 75 用例 0 失败；版本不动 0.57/482 |
| R56 | App Sandbox 启用与临时例外配置（安全合规） | R43 登记候选闭环：entitlements sandbox false→true + 7 条 entitlements（camera/mic/network.server 显式 false + 4 类临时例外）+ SandboxConfigContractTests 15 断言全 PASS；72 用例 0 失败；Info.plist 0.56/481；锚点第 37 轮 0 ERROR；REGISTRY 190 |
| R55 | 桌面歌词独立配色开关（UI） | R51 遗留候选闭环：AppSettings 3 键+hex 编解码/Toggle+Swatches/8 contract tests；BUILD SUCCEEDED；Info.plist 0.55/480；锚点第 36 轮 0 ERROR；REGISTRY 190 |
| R54 | 构建性能分析与编译优化（代码质量） | clean build 48s/incremental 7.6~22.4s/SwiftUI 类型检查 56.3s 瓶颈定位/编译选项已最优/archive/ 死代码 1246 行可清理；561 用例 0 失败；Info.plist 0.54/479；锚点第 35 轮 0 ERROR |
| R53 | R47 观察项双项治理（数据存储） | lyricsSelectionCache reset/export 隔离 + selectedThemeIndex 缺键默认 0 契约化（3 新增单测 6→9）；549 用例 0 失败；Info.plist 0.53/478；锚点第 33 轮 0 ERROR；R47 观察项 2 项本轮闭环 |
| R52 | 桌面歌词窗口长行 marquee（前端体验/UI） | R51 遗留候选闭环：有 timetag follow 跟随（演唱字保持可视 65%）+ 无 timetag 循环 marquee 30fps；DesktopLyricsMarqueeTests 13 红→绿；109 用例 0 失败；全量回归 533 分解前实证（隔代触发）；Info.plist 0.52/477；锚点第 32 轮 0 ERROR；issue #1 维护者关闭（挂账 44+ 轮闭环） |
| R51 | 桌面歌词窗口 MVP（前端体验/UI） | 歌词产品空白面补全：NSPanel 非激活悬浮窗三行卡拉OK逐字高亮 + 设置开关/字号/位置记忆；DesktopLyricsWindowTests 20 红→绿；81 用例 0 失败；Info.plist 0.51/476；锚点第 31 轮 0 ERROR |
| R50 | 隐私清单补建与敏感数据面审计（安全合规） | PrivacyInfo.xcprivacy 补建 + pbxproj 注册 + PrivacyManifestContractTests 13；513 用例 0 失败；Info.plist 0.50/475；锚点巡检第 29 轮 0 ERROR |
| R49 | 编译警告复检清零与工程规范（代码质量） | 1 新警告根因修复 + R41 修复面 10 条零复发；500 用例 0 失败 |
| R48 | 主题系统状态机一致性审计（UI） | ThemeStateMachineContractTests 19 用例；3 项真实问题根因修复；500 用例 0 失败 |

## 已登记候选（未做，可接主线）

- 真机冒烟系列（用户待办，不占 worker）：内存修复 3 项（连续开关设置窗口 8+ 次/隐藏 1h 释放回基线/Dock 图标显隐）、隐私指示灯与恢复观感、GPS 灯观感（R27 登记）、marquee 恢复相位、netstat 恢复首秒（R8~R50 延续挂账）、BeeCount widget GUI 端到端实测（R58 G7 链路，单测+静默回退已覆盖）
- issue #1 已闭环（2026-08-17 维护者手动关闭 COMPLETED，R8 挂账 44+ 轮后状态变化，R52 C 卡新增发现）
- ITER-14：2026-11 国办 2027 节假日通知核对（时间驱动置顶待办，R55 第 45 次核验完成，下次 R60 第 46 次）
- 分发合规空白面：App Sandbox 已启用 R56 闭环；未公证（需 Apple Developer 账号 + notarytool，后续轮次候选）
- 歌词产品面续面候选（R51/R52/R55 连续三轮后）：重置窗口位置 UI+屏外回退守卫 R59-c 闭环 / 真机观感冒烟（+R52 滚动观感/follow 跟随位置，挂真机冒烟系列）；独立配色开关 R55 闭环
- R43 决策门：Keychain 翻转（双 DEVELOPMENT_TEAM 跨配置 ACL 风险，默认不翻转）
- SchemaBridge Phase2 剩余 tab 推广（R58-d 试点、R59-b 消化 systemMonitor/calendar 两域后余量减少）：按 SettingsSchemaBridge.swift 头注释路径逐 tab 迁移——候选顺序建议 General/Notifications/About 之外的纯展示段优先；每 tab 迁移须附字段对照表（§7 d 卡验收惯例）+ EditorSchema 计数不变断言
- §5 契约三处分歧（R58-a 标注）：R59-a 已闭环——readingGoal 单位改页(5...100 步1+水合钳制)/standupMinutes 扩 5...90 使缺省 45 可达/notifyOnUpdate 加「默认关闭」副标题，三处与契约逐字一致
- remindEnabled/remindMinutes 展示态复核（R59-b 登记）：改造前即无运行时读者，暂保留内存态注册+bridge 注释标注，待 §5 审计口径定夺隐藏或接线
- ITER-15 镜像窗事件驱动刷新：R57 第三波已闭环（0.1s 兜底轮询 → 1s 脏检查心跳 + 布局事件化即时同步）；「使用场景 4 问」用户答复仍挂账（仅影响后续真机观测）
- ITER-16 浮层关闭免全量重建：R57 第三波已闭环（dismissOverlay 最小逆操作替代 reloadPreset 全量重建，PopoverDismissTests 6 用例守约）
- archive/ 死代码清理：R57 第三波已闭环（三重复核后删除 16 文件，净删 -1344 行 / ~96KB）
- @State 即烬开关 G1~G5（R57 遗留）：R58 已闭环——Homekit/Package/Wellness/Lifestyle/AI 五页 12 键落盘（a/b 两卡）
- Expense savings.json 四键无消费者 G6/G7（R57 遗留）：R58 已闭环——SavingsGoalItem 四键消费 + BeeCount 今日收支摘要（c 卡）
- §5 契约分歧落 UI：R59 已闭环（a 卡，见候选段闭环行）

## 已论证不动（勿重开）

- 无前缀键 3 处（postureReminderCycleStart / settings.sidebar.visible / group.expanded.*）：改前缀会引入重置清 UI 状态/导出夹带噪声（R47 A 卡论证，不动）
- Coinbase 直连超时：依赖系统代理，已有失败自动重试 + ⚠ 显示（无需再改）
- 全量回归节奏：隔代规则（R54 已触发实证 561 → R55 不触发 → R56 触发），每轮都跑全量是反模式
- appintentsmetadataprocessor 工具提示 + xcodebuild destination 提示：非代码警告，清零需引入无功能依赖属行为变更（R49/R50 豁免复认）

## 维度轮转参考（近 10 轮覆盖）

安全合规 ×3（R43/50/56）· 前端体验 ×3（R45/51/52）· UI ×2（R48/55）· 代码质量 ×2（R49/54）· 数据存储 ×2（R47/53）· 后端服务 ×2（R44/46）· 设置治理+性能（R57 双波）· UI+前端体验混合（R59）→ 下一主线优先选隔最久/未覆盖维度（后端服务 R46 后最长，但候选已闭环），且必须从候选登记段取未做项（grep 取证「还没做」才开卡）。
