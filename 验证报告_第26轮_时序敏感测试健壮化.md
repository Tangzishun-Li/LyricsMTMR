# 验证报告_第26轮_时序敏感测试健壮化

- 轮次：第 26 轮 / 子任务 A（实现/代码质量）
- 分支：r26/test-robustness（基于 main@d5b1248）
- 任务：t_30d0fb44 —— 全量回归 flaky 7 用例根因修复（PausableTimerTests / PollingPauseTests 负载敏感断言）
- 日期：2026-08-13
- 结论：**根因并非负载时序，而是测试宿主 app 真实 TouchBarController 单例对进程级全局隐藏态的翻转污染**。隔离后 5 轮全量实证 214 用例 0 失败 0 意外（含 2 轮系统负载 6.5~23.5 环境复跑），时序断言健壮化改动全部生效。

---

## 一、根因分析

### 1.1 表象与误判

第 26 轮分解前全量回归（214 用例基线）实测 6 轮中 2 轮失败，每次恰 7 用例，全部集中在：

| 文件 | 用例 |
|---|---|
| PausableTimerTests | testUpNextItemPauseFreezesPollAndResumeRefreshes / testWeatherBarItemDeinitStopsLocationAndReleases / testWeatherBarItemLocationPauseBroadcastIsIdempotent / testWeatherBarItemLocationPauseStopsResumeRestartsAndRefreshes / testYandexWeatherBarItemLocationPauseResume |
| PollingPauseTests | testDeinitStopsScheduling / testMetricPopoverItemPauseStopsCompute / testPollItemPauseStopsComputeAndResumeRestarts / testRapidPauseResumeDoesNotDoubleSchedule |

父任务当时的判断是「负载升高 → Timer/DispatchSourceTimer 触发延迟 → 固定 sleep + 紧时序断言误报」（失败发生在其他板 worker 重负载构建期间，load avg 6.3；负载回落后连续 4 轮全绿）。本卡验证运行（负载仅 ~2.0）即以**同签名同集合**复现 22 个断言失败，且失败断言多为**同步断言**（`XCTAssertEqual(counts.startCount, 1)` 期望 1 实得 0，紧跟在构造之后，与任何 sleep/等待无关）——时序假设不成立，可证伪。

### 1.2 真正根因：真实 app 级共享状态污染（第 22 轮教训同族）

失败签名「同步断言 0≠1」的唯一一致解释是 **round-23 的 init 隐藏态播种生效**：Widget 构造时 `TouchBarVisibilityState.shared.isBarHidden == true`，于是

- WeatherBarItem / YandexWeatherBarItem：`locationPauseGate`/`pollGate` 播种暂停 → init 不调 startLocationUpdates（startCount==0）、init fetch 被 guard 拦截；
- UpNextScrubberTouchBarItem：`pollGate` 播种暂停 → init 的 updateView 零 EventKit 查询（queryCount==0）；
- TBPollItem / TBMetricPopoverItem：`_isPaused` 播种暂停 → 首个 cycle 零调度（computeCount 恒 0）。

谁把全局隐藏态置 true 且不复位？追查链条：

1. 任意 suite 触碰 `TouchBarController.shared`（本跑中最早为 AppThemeRulesTests:89-106，14:29:54）→ 单例 init 注册 NSWorkspace `didLaunchApplication / didTerminateApplication / didActivateApplication` 三个通知观察者（TouchBarController.swift:373-375）；
2. 测试运行期间任意 app 启动/退出/激活事件 → `activeApplicationChanged` → `updateActiveApp()`（:484）；
3. 测试宿主 app 在 TEST_HOST 下从不加载 preset，`touchBar` 为空 → 走 `prepareTouchBar() → touchBarContainsAnyItems() == false → dismissTouchBar()`（:538-543）→ `TouchBarVisibilityState.shared.setBarHidden(true)`（:764）——**永久置位**，直到下一次 present；
4. 此后创建的一切 widget 均按隐藏态播种 → 上述 9 用例全部 0 计数失败。

本跑实证时序：14:30:46 testSchedulerWidgetsPauseBroadcastIsIdempotent 创建 UpNext 时 queryCount==1（状态可见，断言过）→ 14:30:53 testUpNextItem… 创建时 queryCount==0（状态已隐藏，断言败）——翻转发生在两个用例之间，期间测试代码无任何 setBarHidden(true) 调用（grep 实证仅 GlobalHiddenStateTests / testNoiseMeterHiddenRebuildSkipsCapture:1171（defer 复位）/ RegistryReconciliationTests:176（defer 复位）三处写者，且均复位）。因此翻转只能来自宿主 app 的真实控制器收到 NSWorkspace 事件。

### 1.3 为什么此前表现为「负载相关」

激活/启动/终止事件落在套件运行窗口内是纯环境概率：负载升高 → 套件跑得更久 → 事件落在窗口内的概率上升；事件落点决定哪几个用例受害（恰 7 个 = 翻转点之后的 widget 用例集合）。负载回落后的 4 轮全绿 = 事件恰好未在窗口内出现。CI（GitHub Actions 共享 runner）同样受此机制影响——任何 app 生命周期事件都会触发。

### 1.4 与第 22 轮教训的关系

第 22 轮「测试间共享真实 scheduler 首触发污染」隔离的是真实 NSBackgroundActivityScheduler 首触发对计数断言的污染（counting double init 后 invalidate）；本轮是同一族问题的另一实例：**真实 app 单例（TouchBarController）通过 NSWorkspace 事件把进程级全局状态（TouchBarVisibilityState）翻转到测试无法预期的值**。两者共同教训：托管测试宿主 app 的真实单例/调度器状态必须由测试侧显式隔离。

---

## 二、修复方案

**测试侧隔离（生产代码零改动）**：在 PausableTimerTests 与 PollingPauseTests 增加 `setUp()`，每用例执行前 `TouchBarVisibilityState.shared.setBarHidden(false)` 复位全局隐藏态——与 GlobalHiddenStateTests 既有 setUp/tearDown 模式完全一致，保证每用例从确定可见态开始，宿主任意事件翻转无法泄漏进用例断言窗口。

- PausableTimerTests.swift:60-77（setUp 注释含完整机制说明）
- PollingPauseTests.swift:30-42（setUp 注释指向 PausableTimerTests 详述）

等价性：生产代码零改动；测试启动态与 round-23 前行为逐字节一致（初始态=可见）；唯一设置该状态的既有用例 testNoiseMeterHiddenRebuildSkipsCapture 显式 setBarHidden(true) 在 setUp 之后执行，语义不受影响。

---

## 三、时序断言健壮化改动清单（逐用例）

原则：① 固定 sleep 排空 → 条件等待（waitUntil / waitForFrozenValue）；② 「sleep 后断言绝对计数」→「冻结窗口内计数不变」（先条件证明已冻结，再断言不再动）；③ firstDelay 紧时序 → 间隔粒度双调度证明；④ dealloc 断言 → 条件等待 dealloc；⑤ 全部 timeout 1.0~2.0 → 3.0 宽松条件等待；⑥ 断言语义只强不弱。

### 3.1 PausableTimerTests.swift（@testable 主线程托管）

| 位置 | 原写法 | 新写法 | 语义保持点 |
|---|---|---|---|
| :143 testTimerPauseStopsFiresAndResumeRestarts | pause 后 pump 0.3s 排空再断言 | waitUntil `currentInterval == 0`（直接证明 teardown 落地）后 pump 1.0 断言冻结 | 冻结断言不变；teardown 证明更强（直接探针而非猜窗口） |
| :168 testTimerPauseBeforeFirstFireNeverFires | setPaused(true) 后直接 pump 1.2 断言 0 | 先 waitUntil teardown 落地再 pump 断言 0 | 绝对 0 断言保留；前置条件更严 |
| :185 testTimerRapidPauseResumeDoesNotDoubleSchedule | hammer 后 pump 0.6；`firstDelay >= 0.3`（首跳延迟下界） | hammer 后 waitUntil teardown；恢复后先等首跳，再断言 0.3s 观察窗内计数不变（双调度会在一间隔内投递双火）+ 1.3s 内增量 ≤4（节奏不倍增） | 「不双调度」改由间隔粒度证明：单调度相邻火 ≥0.4s，双调度会在一间隔内投递两次——0.3s 窗口内无第二跳即证伪双调度；负载只可能推迟火，不可能制造窗口内第二跳，杜绝误报 |
| :236 testTimerImmediateFireOnResume | pause 后 pump 0.5 排空；resume 后 waitUntil(0.6) + `<1.0s` 界 | waitUntil teardown；resume 后 waitUntil(1.2) + `<1.2s` 界 | 即时火语义：1.2s 界 = 间隔本身，即时火落在间隔内的 hop 中必然先于定期火 |
| :273 testTimerRescheduleChangesCadence | pump 0.3 后断言 `currentInterval == 1.2` | waitUntil `currentInterval == 1.2`（直接证明新间隔已装）后 0.7s 观察窗断言旧节奏不火 | 0.7s 窗 < 1.2s 新间隔、> 0.4s 旧间隔：窗口语义更精确（先证安装再观察） |
| :298 testTimerDeinitStopsFires | 原样（weak 断言 + pump 1.2） | timeout 2.0→3.0 | 无 |
| :351 testCPUItemPauseStopsChainAndResumeRestarts | Thread.sleep 1.0 排空 | waitForFrozenValue(stableWindow 1.0)（整间隔冻结证明链已死）后 pump 1.5 断言冻结 | 冻结断言不变；「链死在下一个 hop 门检」的异步性由冻结等待吸收 |
| :414 testDarkModeItemPauseStopsRefreshAndResumeRefreshes | pump 0.5 排空 | waitForFrozenValue(1.0) | 同上 |
| :457 testTimeItemPauseStopsUpdatesAndResumeRefreshes | pump 0.5 排空 | waitForFrozenValue(1.0) | 同上 |
| :496 testMusicItemPauseStopsChainAndResumeRestarts | pump 0.6 排空 | waitForFrozenValue(1.0) | 同上 |
| :536 testBrightnessItemPauseStopsRefresh | pump 0.5 排空 | waitForFrozenValue(1.0) | 同上 |
| :556 testPausableTimerCommonModeFires | pump 0.5 排空 | waitUntil `currentInterval == 0` | 同上（直接探针） |
| :578 testTwoPausableTimersSameOwnerPauseIndependently | pump 0.5 后立即断言 B 仍在跳 | 先 waitUntil `currentInterval == 0`（A 确死），B 改为 waitUntil 新跳（条件等待，负载下只是更迟） | B 继续跳断言由「固定窗内必跳」改为「条件等待必跳」+ A 冻结窗口保持 1.0 |
| :659/:685/:719 AudioSpectrum 3 用例 | pump 0.3 排空 tick / waitUntil(1.0) | waitForFrozenValue(1.0) / waitUntil(3.0) | 25fps 显示 timer 冻结断言保留 |
| :755/:775/:793/:813 Currency/Weather/Yandex/UpNext 4 用例 | waitUntil(1.0/2.0) | waitUntil(3.0) | 语义不变 |
| :831 testSchedulerWidgetsPauseBroadcastIsIdempotent | waitUntil(1.0) | waitUntil(3.0) | 语义不变 |
| :1025/:1049/:1090 Weather/Yandex 定位 3 用例 | waitUntil(1.0) | waitUntil(3.0)（stop/start 条件等待） | 语义不变 |
| :1135 testWeatherBarItemDeinitStopsLocationAndReleases | waitUntil(1.0) stopCount + `XCTAssertNil(weakItem)` | waitUntil(3.0) stopCount + **waitUntil(3.0) `weakItem == nil`** | dealloc 由「固定窗口后已发生」改为「条件等待发生」——负载下 deinit 仅推迟不消失；首行同步断言 startCount==1 保留（隔离后确定性成立） |
| :1186/:1208/:1230 Shell/AppleScript/脚本幂等 | pump 0.3+0.6 排空 / waitUntil(2.0) | waitForFrozenValue(1.0) / waitUntil(3.0) | 冻结断言保留 |

### 3.2 PollingPauseTests.swift（后台队列轮询）

| 位置 | 原写法 | 新写法 | 语义保持点 |
|---|---|---|---|
| :39 setUp | 无 | setBarHidden(false) 复位 | 全局隐藏态隔离（见 §二） |
| :65 waitForFrozenValue | 无 | 新增 helper：值连续 stableWindow 不变即冻结 | 与 PausableTimerTests 同构（Thread.sleep 轮询版本） |
| :122 testPollItemPauseStopsComputeAndResumeRestarts | sleep 0.3 排空 + sleep 1.0 断言冻结 | waitForFrozenValue(1.0) 后再 sleep 1.0 断言冻结 | 冻结断言保留；「已冻结」先被证明 |
| :144 testMetricPopoverItemPauseStopsCompute | 同上 | 同上 | 同上 |
| :163 testPauseBeforeFirstCycleNeverComputes | sleep 1.2 后断言 0 | waitForFrozenValue(1.0) 后断言 0 | 绝对 0 断言保留 |
| :180 testRapidPauseResumeDoesNotDoubleSchedule | sleep 1.0 排空 + `firstDelay >= 0.3` | waitForFrozenValue(1.0) ×2 + 间隔粒度证明（0.3s 窗无第二跳 + 1.3s 增量 ≤4） | 与 3.1 :185 同构论证 |
| :228 testDeinitStopsScheduling | `XCTAssertNil` + sleep 1.2 + `XCTAssertNil` | waitUntil(3.0) `weakItem == nil` | 「dealloc 且不复活」由条件等待证明；「不复活」隐含在 weak nil 后无引用可调度 |

### 3.3 共享 scheduler 首触发污染残留评估（第 22 轮教训复查）

逐双打核查：CountingCurrencyItem:1319 / CountingWeatherItem:1352 / CountingYandexItem:1385 / CountingUpNextItem:1442 / CountingWeatherLocationItem:967 / CountingYandexLocationItem:989 **全部 init 后 `activity.invalidate()`**，真实 NSBackgroundActivityScheduler 首触发（不遵守 interval 下限）隔离到位，无残留。本轮新增 setUp 复位与第 22 轮 invalidate 正交互补，覆盖「真实 scheduler 首触发」与「真实控制器全局态翻转」两条污染路径。

---

## 四、语义等价性论证

1. **冻结语义**：「pause 后计数不再变化」全部保留为显式断言；排空方式从「猜固定窗口」改为「证明已冻结（整间隔无火）再断言窗口内不变」。若被测实现真在 pause 后继续火，冻结等待会持续看到变化、3.0s 超时 → 冻结证明失败 → 用例仍红（不会漏报）。负载只影响「何时开始冻结」，不影响「是否冻结」。
2. **双调度语义**：原 `firstDelay >= 0.3` 断言的是「恢复后首跳不早于 0.3s」——负载下首跳延迟被误判为双调度；新断言「恢复后首跳之后 0.3s 内无第二跳 + 1.3s 内增量 ≤4（单调度 3 跳 + 1 容差）」证明的是「两个调度器实例不会在一间隔内投递双火」。双调度时两实例相位差 < 0.3s 的窗口必然出现（首跳对即暴露），健康单调度负载下火只推迟不提前，0.3s 窗永不误报。
3. **immediateFireOnResume**：界从 1.0s 放宽到 1.2s（= 间隔本身），「即时火先于定期火一个间隔」的判定窗口与间隔绑定，负载语义不变。
4. **dealloc**：`XCTAssertNil`（固定时刻快照）→ waitUntil nil（条件等待），弱引用为 nil 的证明强度不变，且不再受「deinit 在负载下推迟」影响。
5. **绝对计数断言**（testUpNext queryCount==1、weather startCount==1、pause-before-first 的 0）全部保留，由 setUp 隔离保证确定性成立——隔离前它们的失败正是污染证据（§1.2），隔离后 5 轮无一误报。

---

## 五、复跑实证

| 轮次 | 环境 | 结果 |
|---|---|---|
| run1 修复前基线（干净 derivedData，14:29） | 负载 ~2.0 | **FAILED**：PausableTimerTests 18 断言失败（5 用例）+ PollingPauseTests 4 用例失败，全部 0 计数签名 → 定位污染根因 |
| run2 修复后（增量 derivedData，14:44） | 负载 ~2 | **214 用例 0 失败 0 意外** |
| run3 修复后（**rm -rf /tmp/LyricsMTMR-dd-* 后全新 derivedDataPath /tmp/LyricsMTMR-dd-r26a-test**，14:47） | 负载 ~2 | **214 用例 0 失败 0 意外**（验收口径：金丝雀 StockMarketHoursTests 锚点全绿 + WidgetLeakTests 8 全绿） |
| run4 负载复跑第 1 轮（14:50） | **3 个并行 xcodebuild 重构建制造负载，load avg 23.5 / 18.2 / 9.5** | **214 用例 0 失败 0 意外** |
| run5 负载复跑第 2 轮（14:52） | **负载回落中 load avg 6.5 / 13.8 / 8.7**（与父任务失败时 6.3 同量级） | **214 用例 0 失败 0 意外** |

日志：/tmp/r26a-test-run{1,2,3-clean,4-load,5-load}.log（每次 214 用例全量执行）。

---

## 六、遗留登记（不擅改生产）

1. **生产代码观察（非本轮范围）**：`TouchBarController.updateActiveApp()` 在 bar 为空（TEST_HOST / 无 preset）时对任意 NSWorkspace 生命周期事件执行 `dismissTouchBar()` 并永久置位全局隐藏态——测试宿主下即「事件即污染」。生产语义（空配置 dismiss）合理，但建议后续评估：仅在有实际 touchBar 内容时翻转状态，或 dismiss 幂等检查。已按任务约定 comment 上报父任务，不擅改。
2. 真机冒烟延续挂账（第 8/17/18/19/20/21/22/23/24/25 轮同口径，依赖 Touch Bar 真机）。
3. 时序用例健壮化后仍保留的最小真实依赖：TBPausableTimer/TBPollItem 真实 runloop/后台队列调度（被测对象本身），无新增真机依赖；weather/upnext 用例仍全部使用注入假源（IUpNextSource / 计数 override），零 CoreLocation/EventKit/网络。

---

## 七、改动文件清单

- LyricsMTMR/MTMRTests/PausableTimerTests.swift（+282/-114 含注释：setUp 隔离 + waitForFrozenValue helper + 全部时序断言健壮化）
- LyricsMTMR/MTMRTests/PollingPauseTests.swift（+130/-42 含注释：setUp 隔离 + waitForFrozenValue helper + 全部时序断言健壮化）
- 生产代码：零改动（git diff 实证仅上述两测试文件）
