# 核对报告：内存修复文档 ↔ 代码一致性 + issue 状态复核（第 9 轮 / 子任务 C）

> 核对日期：2026-08-12。执行：merge-agent（只读核对，未改任何代码）。
> 核对对象：提交 2d2d681（fix/t_5e363548-settings-window-reuse，文档+日志）声明的修复内容
> ↔ 28e65b6 合并实际带入 main 的代码（AppDelegate.swift +65 / UnifiedSettingsWindowController.swift +40）。
> 基线：main@59a0d24（本工作区分支 r9/issue 即基于此，两份 Swift 文件与 28e65b6 逐字节一致，无后续改动）。

## 一、结论速览

**1. 代码一致性：基本完全一致（6/6 项主张与代码实现吻合），发现 1 处代码内部注释陈旧（低危，非报告↔代码出入）。**
提交 2d2d681 声明的全部修复项——windowShouldClose 改隐藏复用、hideWindow()、DockVisibilityManager.track 幂等、
AppDelegate 闲置 GC 1h、内存压力即时释放——在 main 中均有对应实现，行为与文档描述一致。
**「代码随 28e65b6 合并已入 main」的声明属实**：git diff 28e65b6..HEAD 为空，main@59a0d24 即携带该修复。

**2. issue/PR 状态：全部符合预期。** issue #1 OPEN（1 条 comment = 第 8 轮回复，无新动态）；issue #40 OPEN（backlog，0 评论）；open PR 0。

**3. 待用户确认事项清单（第 9 轮版）见第五节。**

## 二、一致性核对：逐项对照（2d2d681 主张 ↔ main 代码）

| # | 文档/提交信息主张 | main 代码实现 | 结论 |
|---|---|---|---|
| 1 | windowShouldClose 不再放行关闭：一律 hideWindow()（orderOut 隐藏）后返回 false；未保存修改确认弹窗保留（保存/不保存→隐藏；取消→保持可见） | `UnifiedSettingsWindowController.swift` windowShouldClose：无未保存修改 → hideWindow() + return false；有未保存修改 → 弹窗三分支（保存/不保存 → hideWindow()；取消 → break 保持可见），所有路径 return false | ✅ 完全一致 |
| 2 | 新增 hideWindow()：同步 isVisible=false（停 Deck 动画 OPT-2）、收起 Dock 图标（DockVisibilityManager）、orderOut、回调 onWindowHidden | hideWindow() { SettingsWindowState.shared.isVisible = false; DockVisibilityManager.shared.handleSettingsWindowClosed(); window?.orderOut(nil); onWindowHidden?() } | ✅ 完全一致 |
| 3 | DockVisibilityManager.track() 改为幂等（先清旧 observer），重复开窗不叠加通知令牌 | track(window:) 首行调用 removeObservers()，再 setupObservers；handleSettingsWindowClosed() 亦清 observer 并置 .accessory | ✅ 完全一致 |
| 4 | AppDelegate 闲置 GC：隐藏 1h（settingsWindowIdleGCSeconds=3600）后整树释放，内存回落基线；重开取消 | `static let settingsWindowIdleGCSeconds: TimeInterval = 3600`；scheduleSettingsWindowGC() cancel 旧任务 → asyncAfter 3600s → unifiedSettingsController = nil；openSettings 入口即 cancel 待执行 GC | ✅ 完全一致（含取值 1h） |
| 5 | 内存压力时隐藏窗口立即释放（releaseSettingsWindowIfHidden，可见时不打扰用户） | applicationDidReceiveMemoryWarning 调用 releaseSettingsWindowIfHidden()；该方法 cancel GC 且仅当 `window?.isVisible != true` 才置 nil | ✅ 完全一致 |
| 6 | openSettings：开窗即取消闲置 GC；窗口已存在则直接复用（重挂 Dock 跟踪） | openSettings cancel 待执行 GC；controller 为 nil 才新建并挂 onWindowWillClose/onWindowHidden；每次开窗 DockVisibilityManager.shared.track(window:) 重挂 | ✅ 完全一致 |
| 7 | onWindowWillClose 语义调整为「真关闭路径（app 终止）释放」 | onWindowWillClose 回调保留，注释明确 release on real close (app termination path) | ✅ 一致 |
| 8 | 幽灵窗口三要素不成立：隐藏时 isVisible 暂停动画（OPT-2） | hideWindow() 同步 SettingsWindowState.shared.isVisible = false；Deck 动画（Equalizer 等）经 @ObservedObject windowState 观察暂停 | ✅ 一致 |
| 9 | 「代码随 28e65b6 合并已入 main，本提交补文档与日志」 | main@59a0d24 中两份 Swift 文件 = 28e65b6 版本（diff 为空）；2d2d681 仅动 iteration-log.md + 报告 | ✅ 声明属实 |

**验证数据声明核对**（报告 §四，2026-08-12 Debug 构建）：修复后 5 轮关窗态 52.1/51.9/51.9/51.9/52.0MB 全平、
内存警告后 38.2MB、重开 54.8MB、60 单测全绿——均为实测声明，单测可复核（见第四节复核记录）。

## 三、差异清单（发现的问题）

| 编号 | 位置 | 内容 | 严重度 | 影响与建议 |
|---|---|---|---|---|
| D1 | `UnifiedSettingsWindowController.swift:113-114`（onWindowHidden 的文档注释） | 注释写「闲置 GC（**默认 10 分钟**未再开则整体释放）」，而实际 `settingsWindowIdleGCSeconds = 3600`（**1 小时**）；提交信息与报告均写 1h | 低（纯注释陈旧，实现正确） | 误导后续维护者（以为 10 分钟释放，实际 1h）。建议在下次代码改动时顺手把注释改为「1 小时」（本次只读核对，未改代码） |
| D2 | 报告 §2.1 vs §四 基线数字 | §2.1 写全新实例 22-24 MB，§四 验证表写「修复前基线 23 MB / 修复后 21.3 MB」 | 可忽略 | 不同测量时刻（基线对照 vs 修复版实测）的正常波动，非矛盾 |
| D3 | 报告 §3.2 未提及 onWindowWillClose 保留 | 文档只描述 hideWindow 路径，代码保留了 onWindowWillClose（真关闭/app 终止释放） | 可忽略 | 代码更完整，文档无冲突 |

**结论：报告与代码无实质出入；差异清单仅 1 处低危注释陈旧（D1）。**

## 四、issue / PR 状态复核（gh CLI，2026-08-12）

| 对象 | 预期 | 实测 | 结论 |
|---|---|---|---|
| issue #1「Why Apple why?」 | OPEN、1 条 comment=第 8 轮回复、无新动态 | OPEN；1 条 comment（Tangzishun-Li，2026-08-12T05:42:36Z，内容为 MediaRemote 权限收紧说明=第 8 轮回复）；updatedAt 与 comment 时间一致，无新动态 | ✅ 符合 |
| issue #40「[Feature Request] Per-app bar switching（按软件切换 Touch Bar 布局）」 | OPEN（backlog） | OPEN；0 评论；created 2026-08-12T05:42:18Z（第 8 轮建立） | ✅ 符合 |
| open PR | 0 | 0 | ✅ 符合 |

**单测复核**：`make test`（UnitTests scheme，Debug）于 2026-08-12 14:36 完成——**Executed 60 tests, 0 failures**，
与提交声明的「60 单测全绿」一致（xcodebuild TEST SUCCEEDED）。

## 五、待用户确认事项清单（第 9 轮版）

1. **issue #1 关闭**：第 8 轮回复已发（保持 OPEN，说明根因=macOS 15.4+ MediaRemote entitlements 收紧），**待用户在 15.7 真机验证**后关闭 issue（验证通过即关；另开 backlog 承接「按软件切换 bar」已由 #40 承担）。
2. **ITER-15 使用场景 4 问（决策门）**：镜像窗默认关闭（AppSettings.showMirrorWindow=false），是否常驻 / 用途 / 快照实时性要求 / 电量敏感度——用户答复后决定「事件驱动+轮询兜底」是否实施。
3. **README 补 MediaRemote 风险说明**：建议后续补（说明 15.4+ 起第三方 App 无法直接读取 QQ音乐/网易云 now playing），不强制。
4. **【本轮新增】2d2d681 文档并入 main 决策依据**：代码已随 28e65b6 入 main 且经本报告核对一致；2d2d681 含 iteration-log 补记 + 完整修复报告，建议父任务收口时将其合并入 main 以闭环文档。
5. **【可选/低优先】D1 注释修正**：控制器内「10 分钟」→「1 小时」注释陈旧，可在下次代码改动时顺手修正。

## 六、风险点

- **行为风险（低）**：闲置 GC 1h 与内存压力即时释放两条释放路径并存，若窗口隐藏后 1h 内被系统终止进程（而非收到内存警告），窗口树不释放——但这是预期设计（1h 才释放），非缺陷。
- **验证风险（低）**：修复报告中的内存数据（52.1/51.9…、38.2、54.8MB）为作者当日实测，本核对仅复核代码实现与文档一致性，未复现内存实验（需真机 Debug 构建 + 自测钩子，超出只读核对范围）。
- **文档风险（已记录）**：D1 注释陈旧若不修，未来维护者可能误以为 GC 时长为 10 分钟。
