# 验证报告_第44轮_网络请求健壮性审计与治理

- 轮次：第 44 轮（功能/优化迭代第 32 轮）/ 子任务 A（实现/优化）
- 维度：后端服务（网络请求健壮性：超时策略 / 阻塞 / 错误面）
- 分支：r44/network（worktree .worktrees/round44-A，基于父分支预建头 3179dd3）
- 前置基线：父任务分解前全量回归 462 用例 0 失败实证通过（97.1s 口径，TEST SUCCEEDED）
- 历史铺垫：R40 已审计 URLSession 捕获链（27 处 completion 强/弱引用）；R43 已审计 20 个 APIService 生命周期（存储介质/传输方式/泄漏面/ATS 例外域）

---

## 一、审计分类表

### 1.1 同步信号量调用（阻塞面）——全仓 6 处

| # | 站点 | 位置 | 请求超时 | 等待上界 | 调用上下文 | 分类 |
|---|---|---|---|---|---|---|
| S1 | `TBNet.get` | WidgetKit.swift:881 | 8s 默认（调用方显式 5/10） | timeout+1 | ApiLatencyItem.compute（5s）/ RSSDirectCounter.unreadCount（10s） | 轮询链同步阻塞 |
| S2 | `TBNet.json` | WidgetKit.swift:900 | 8s 默认 | 9s | WeatherOutfit / DailyQuote / SlackUnread / RssUnread provider / CiPipeline / BilibiliFeed（均 compute()） | 轮询链同步阻塞 |
| S3 | `TBNet.postJSON` | WidgetKit.swift:906 | 20s 默认 | 21s | AiSelectedText / WordLookup / HomekitScene（用户触发动作） | 用户动作同步阻塞（有界） |
| S4 | `ApiLatency.directGet` | ApiLatency.swift:79 | 5s | 6s | ApiLatencyItem.compute（直连会话） | 轮询链同步阻塞 |
| S5 | `PackageTracker.formPost` | PackageTracker.swift:77 | 8s | 9s | PackageTrackerItem.compute（快递100） | 轮询链同步阻塞 |
| S6 | `ApiTester.performRequest` | ApiTester.swift:75 | 10s | **12s（与请求超时不匹配）** | 用户点击（global 队列，非轮询链） | 用户动作同步阻塞（有界） |

结构事实（代码实证）：TBPollItem/TBMetricPopoverItem 的 `compute()` 在每 widget 专用串行队列执行，`loop()` 的下一周期仅在当前周期完成后调度（scheduleNextCycle 单飞守卫，WidgetKit.swift:682-712/:843-868）——compute() 内的同步网络调用直接拉长该 widget 的轮询节奏并冻结显示；每 widget 队列独立，不波及全局。RssUnread direct 模式按 feed 串行循环（RssUnread.swift:55-57），N 个死 feed 的阻塞线性叠加（每个最多 11s）。

### 1.2 无显式超时站点（默认 60s 错配）——7 处

| # | 站点 | 位置 | 刷新节奏 | 错配分类 |
|---|---|---|---|---|
| T1 | StockBarItem 腾讯分时 | StockBarItem.swift:155 | 交易时段 10s | 60s vs 10s：行情陈旧窗口最长 60s + 无单飞守卫时请求堆积 |
| T2 | StockBarItem 东财 quote | StockBarItem.swift:285 | 同上 | 同上 |
| T3 | StockBarItem 东财 trends | StockBarItem.swift:314 | 同上 | 同上 |
| T4 | CurrencyBarItem | CurrencyBarItem.swift:176 | 600s | 60s 陈旧窗口 |
| T5 | WeatherBarItem（openweather） | WeatherBarItem.swift:176 | 1800s | 60s 陈旧窗口 |
| T6 | YandexWeatherBarItem | YandexWeatherBarItem.swift:131 | 1800s | 60s 陈旧窗口 |
| T7 | LyricsTranslateBarItem | LyricsTranslateBarItem.swift:120 | 用户点击 | 用户等待最长 60s |

### 1.3 已有显式超时站点（治理合规，无需改动）——25 处

SecretsManager 8 处（10-15s）/ UsageBarItem 3×15s / OpenCodeGoUsageBarItem 15s / DeepseekBalanceBarItem 10s / ChinaWeatherProvider 2×10s / Lyrics 提供者 6 处（Migu 10 / QQMusic 10 / NetEase 10 / Kugou 10 / CoverCache 15 / Subtitle 15）/ ApiLatency directSession 5s / PackageTracker 8s / ApiTester 10s / TBNet get 默认 8s / TBNet postJSON 默认 20s。

### 1.4 错误面盘点

| 站点 | 失败时表现 | 分类 |
|---|---|---|
| ApiLatencyItem | 「超时/timeout」+ coral（apply 已区分） | ✅ 已有失败面 |
| PackageTrackerItem | 「查询失败」+ coral | ✅ 已有失败面 |
| RssUnreadItem（direct/provider） | **一律显示「0」**——"已读光"与"网络死"不可区分 | ❌ 真实缺口（本轮修复） |
| WeatherOutfit / DailyQuote / SlackUnread / BilibiliFeed / CiPipeline | 静默保留旧值 | ⚠️ 陈旧显示（登记遗留，不改动面） |
| TBPollItem compute 异常 | ⚠️ + coral（仅 ObjC 异常路径） | 已有兜底 |

---

## 二、真实问题与红→绿双跑实证

### 问题 1（阻塞面根因）：等待超时后孤儿 dataTask 长期占用连接 + 无条件读 result 的数据竞争

- 根因：6 处信号量同步调用均为「wait(timeout+1) 到期后不 cancel 任务、无条件返回 result」。
  - URLSession 的 `timeoutIntervalForRequest` 是**空闲**超时：服务器持续涓流数据（每 <8s 一个包）时请求自身永不超时，等待上界到期后孤儿任务继续占用连接直到服务器放弃（可长达数分钟）——修复前该任务不可见、不可取消；
  - 等待到期后仍读取 `result`：完成闭包可能随后写入，构成形式数据竞争（Swift 6 严格并发下为编译错误；当前实践中读写同一 box 无同步）。
- 修复（TBNet.syncFetch 统一硬化，WidgetKit.swift:889-907）：
  - `semaphore.wait` 返回 `.timedOut` 时 `task.cancel()`（URLSession 随即对 URLProtocol 调 stopLoading，连接立即释放）并返回合成 `NSURLErrorTimedOut`（调用方可上报）；
  - 仅在 wait 成功（happens-before 由信号量保证）时读 result——超时后晚到的完成写入无人读取的 box，数据竞争结构性消除；
  - 4 处调用点复用同一核心：TBNet.get/json/postJSON、ApiLatency.directGet（直连会话透传）、PackageTracker.formPost、ApiTester.performRequest（顺带修正 12s 等待 vs 10s 请求超时的错配，超时消息从空 body 变为可读错误）。
- 测试钩子：`TBNet.sessionOverride`（生产恒 nil = URLSession.shared；与 SettingsSync.itemsJSONPathOverride / SecretsManager.defaultsOverride 同型，第 42/43 轮先例）。
- 红→绿实证（断言未放宽）：
  - 红（测试基建 + 钩子就位、行为未修）：`testSyncFetchCancelsTaskWhenWaitExpires` —— XCTAssertGreaterThan(stopLoadingCount, 0) 实测 0 → failure；
  - 绿（修复落地）：同断言通过（2.005s，stopLoadingCount ≥ 1）。

### 问题 2（错误面）：RSS 未读角标抓取失败显示误导性「0」

- 根因：RSSProviderClient 各后端与 RSSDirectCounter.unreadCount 在任何失败（网络错误/401/响应形状异常）时返回 0——用户无法区分「已读光」与「网络断」。
- 修复（RssUnread.swift）：
  - `RSSDirectCounter.unreadCount` / `RSSProviderClient.unreadCount`（含 feedly/inoreader/miniflux/googleReader 四后端）返回 `Int?`：抓取失败（含 401、形状不符）→ nil，合法空 feed → 0；
  - RssUnreadItem 新增 `fetchFailed`（每周期复位）：direct 模式任一 feed 失败即置位并停止剩余抓取（单个死 feed 已使总数不可信）；provider 模式返回 nil 置位；
  - apply() 失败态优先：值「—」+ coral + 「获取失败/offline」（区别于 ⚠️ 异常兜底，语言无关）。
- 红→绿实证（断言未放宽）：
  - 红：`testRssDirectFetchFailureShowsFailureState` / `testRssProviderFetchFailureShowsFailureState` —— XCTAssertEqual(metric.value, "—") 实测 "0"、valueColor 非 coral → 2 用例 4 failures；
  - 绿：同断言通过（provider 用例 0.002s，桩立败零等待）。

### 问题 3（超时错配，审计论证修复）：7 处默认 60s 站点补显式超时

- 修复：全部补 `timeoutInterval = 15`（对齐仓内既有 10-15s 惯例；T1-T3 同时消除「60s 超时 × 10s 节奏」的在途堆积窗口）。行为修复以 grep 实证登记，机制层由问题 1 的契约测试钉住。
- 另修复 StockBarItem 无单飞守卫（StockBarItem.swift:106-112/134-141）：`isFetching` 守卫（timer 与 group.notify 均主线程，无锁安全），挂死端点不再每 tick 堆积重复请求；notify 先清守卫再判暂停，避免暂停中落地的 fetch 卡死恢复路径。

---

## 三、契约测试用例表（NetworkRobustnessContractTests.swift，新增 4 用例）

| 用例 | 契约 | 桩 | 红跑 | 绿跑 |
|---|---|---|---|---|
| testSyncFetchBoundedWaitOnTricklingEndpoint | 涓流端点下同步获取必须按 timeout+1s 上界返回 nil（轮询链停摆有界） | TricklingURLProtocol（每 0.4s 补 1 字节，请求自身永不超时） | pass（契约钉，非红） | pass 2.004s |
| testSyncFetchCancelsTaskWhenWaitExpires | 等待上界到期必须取消在途任务（URLProtocol.stopLoading 被调用） | 同上 | **fail**（stopLoading 实测 0） | pass 2.005s |
| testRssDirectFetchFailureShowsFailureState | direct 模式抓取失败显示「—」+ coral 而非「0」 | FailingURLProtocol（立败） | **fail**（value="0"、颜色不符） | pass |
| testRssProviderFetchFailureShowsFailureState | provider 模式抓取失败同样显示失败态 | FailingURLProtocol + SecretsManager.defaultsOverride 注入 key | **fail**（value="0"、颜色不符） | pass |

红跑：Executed 4 tests, with **5 failures**；绿跑：Executed 4 tests, with **0 failures**。断言前后逐字未放宽（XCTAssertEqual value/color、XCTAssertGreaterThan stopLoadingCount）。测试零真实网络（127.0.0.1 拒绝端口 + URLProtocol 桩），hosted 运行于宿主 App 进程。

---

## 四、全量实证表

| 项 | 结果 |
|---|---|
| 命令 | xcodebuild test（UnitTests, Debug），独立 /tmp/LyricsMTMR-dd-r44a-test（先清旧 /tmp/LyricsMTMR-dd-* 0 残留 + caffeinate -i 防显示器休眠） |
| 用例口径 | 462 基线 + 新增 4 = **466** |
| 结果 | **TEST SUCCEEDED —— Executed 466 tests, with 0 failures (0 unexpected) in 102.8s**（25 套件逐套件 0 失败，xcresulttool 实证） |
| 金丝雀 | StockMarketHoursTests 16/16 · WidgetLeakTests 30/30 · RegistryReconciliationTests 6/6 · ItemTypeDecodeRegistryTests 173/173 · WriteSideContractTests 6/6 · SecretsManagerContractTests 13/13 · **NetworkRobustnessContractTests 4/4（本轮新增）** 全绿 |
| 锚点巡检 | scripts/anchor-patrol.py 复跑 **PASS 67 / WARN 16 / INFO 5 / ERROR 0 退出码 0**（合计 88 项与第 43 轮基线同口径；StockBarItem 合入 +16 行触发锚点行号修正——iteration-plan.md:9、maintenance-notes.md:16-19、anchor-patrol.py 8 条定义；5 条 record 范围锚点 IP-152d/280a/280b/334a/334b 转记录性位移 WARN，全部已登记项） |
| 改动文件 | WidgetKit.swift / ApiLatency.swift / ApiTester.swift / PackageTracker.swift / RssUnread.swift / StockBarItem.swift / CurrencyBarItem.swift / WeatherBarItem.swift / YandexWeatherBarItem.swift / LyricsTranslateBarItem.swift / NetworkRobustnessContractTests.swift（新增，pbxproj 4 条目注册）/ project.pbxproj |

---

## 五、文档同步表

| 文档 | 动作 |
|---|---|
| 本验证报告 | 本分支根目录新建 |
| iteration-log.md | 第 44 轮「### 子任务记录」小节补建并追加本卡记录（标注「第 44 轮 / 子任务 A」），收口时父任务重组 |
| file-structure.zh.md | mindmap 第 7~43 轮 → 第 7~44 轮 + 本报告行登记（无重复行） |
| scripts/anchor-patrol.py | 锚点行号修正 8 条定义（TOD-1/2、MNT-1~8、IP-158/IP-281 known），修正后复跑确认；docs/iteration-plan.md:9 与 docs/maintenance-notes.md:16-19 引用行号同步修正 |

---

## 六、结论与遗留

- 结论：信号量同步调用在轮询链上为**真实但有界**的风险——等待上界保证不无限停摆，但每周期拉长节奏 + 冻结显示 + 涓流场景孤儿任务占用连接。本轮根因修复集中于信号量模式的真实缺陷（超时取消 + 数据竞争消除 + 错误面上报）与超时策略错配（7 处补显式超时 + 单飞守卫），全部行为可测，红→绿双跑实证未放宽断言。
- 遗留登记：
  1. **轮询链同步网络调用彻底异步化评估**（消除阻塞本身）：TBNet sync API 保持（调用点 15+，含 postJSON 用户动作语义），异步化重构改动面大，登记为后续轮候选；
  2. WeatherOutfit / DailyQuote / SlackUnread / BilibiliFeed / CiPipeline 失败静默保留旧值——沿用 try? 静默吞错的全仓既有风格（R42 登记），本轮仅治理与阻塞/超时直接相关的错误面，不扩大改动面；
  3. 内存修复真机冒烟 3 项挂账延续（第 8/17~44 轮同口径）；
  4. 锚点巡检 REGISTRY 报告登记行预期 +1（本报告登记后口径）。
- 约束遵守：仅本工作区（.worktrees/round44-A，r44/network）改动；未 push 远端（父任务收口统一推送）；未开新分支/新子任务/无 parents 依赖；未建 cron/自触发；未改 Info.plist 版本号（B 卡建议、父任务收口落地）；完成自查 git status 干净 + commit 已提交。
