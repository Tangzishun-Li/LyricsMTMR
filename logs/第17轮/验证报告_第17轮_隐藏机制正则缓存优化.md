# 验证报告_第17轮_隐藏机制正则缓存优化

- 轮次：第 17 轮（功能/优化迭代第 5 轮）· 子任务 B（实现/优化维度）
- 任务卡：t_9c0de9ca（分支 r17/feature，工作区 `.worktrees/round17-B`，基于 main@e231128）
- 落地目标：matchAppId 正则编译缓存（第 16 轮落地的隐藏机制 TECHNICAL_DEBT ③ 之性能细节跟进）
- 日期：2026-08-12

---

## 〇、结论速览

| 项 | 结论 |
|----|------|
| 问题 | 第 16 轮提取的纯函数 `TouchBarController.shouldShowItem(_:frontmostAppId:)` 对每个带 matchAppId 参数的 item、在每次评估（前台应用切换/主题切换触发全量评估，114 类型全集）时都执行 `try? NSRegularExpression(pattern:)` 重新编译正则——同一 regexString 反复编译，纯浪费 |
| 方案 | 新增有界、线程安全的 `MatchAppIdRegexCache`（按 regexString 缓存编译结果，编译一次复用）；`shouldShowItem` 经缓存取正则；顺带把两条调用路径（同步 createItems / 异步 reloadPresetAsync）循环内重复取值的 `frontmostApplicationIdentifier` 提出循环，每轮评估只取一次 |
| 等价性 | **行为严格等价**：无效正则仍降级「始终显示」并记日志（且不做负缓存，每次评估仍重新尝试编译——与缓存前行为逐字节一致）；nil 前台仍返回 true；匹配语义不变（NSRegularExpression + firstMatch 不变）；仅编译频次下降 |
| 实证 | 分支内 xcodebuild build **BUILD SUCCEEDED** + test **TEST SUCCEEDED —— 134 用例 0 失败 0 意外**（129 基线 + 新增 5 缓存单测，金丝雀锚点全绿） |

---

## 一、问题分析（改动前热点）

- 评估入口：`TouchBarController.shouldShowItem(_:frontmostAppId:)`（TouchBarController.swift:298-307，第 16 轮提取）。
- 热点：`guard let regex = try? NSRegularExpression(pattern: regexString)` 在**每次**评估、**每个**带 matchAppId 的 item 上执行。预设 item 全集 114 类型，每次前台应用切换 / 主题切换都会触发全量评估（同步路径 createItems 673 行 + 异步路径 reloadPresetAsync 894 行）。
- 浪费点：同一 preset 中不同 item 可共享同一 regexString（如多个 item 都配 `matchAppId: "Safari"`），且同一 item 在每次应用切换时反复评估——同一 pattern 被重复编译几十上百次。
- 附带重复：两条调用路径都在**循环体内**取 `frontmostApplicationIdentifier`（`NSWorkspace.shared.frontmostApplication?.bundleIdentifier`，每次取值是一次系统查询），114 个 item 就取 114 次；同步循环内该值不可能变化。

## 二、落地变更明细

### 1. 新增 `MatchAppIdRegexCache`（TouchBarController.swift 文件尾，未开新文件 → 零 pbxproj 注册）

```swift
final class MatchAppIdRegexCache {
    static let maxEntries = 128          // 容量封顶
    private var storage: [String: NSRegularExpression] = [:]
    private var insertionOrder: [String] = []   // FIFO 淘汰序
    private var compileAttempts = 0             // 诊断/测试计数
    private let lock = NSLock()                 // 线程安全
    func regex(for pattern: String) -> NSRegularExpression?
    var count / var compileCount / func reset() // 诊断/测试接口
}
```

形态选择（按任务要求「评估后选择合适形态」）：

| 考量点 | 决策 | 依据 |
|--------|------|------|
| 容量 | 封顶 128（`maxEntries`） | 预设全集 ≤114 item；128 可容纳一整份预设 + 第二份交替预设的余量。FIFO 淘汰最旧——交替多预设时最坏情况是重新编译（本就没缓存时的行为），内存有界 |
| 线程安全 | NSLock 全临界区串行化 | `shouldShowItem` 是静态纯函数，两条运行时调用路径都在主线程，但测试可从任意线程调用；锁代价（μs 级）远小于正则编译，且保证「并发下每个 pattern 恰好编译一次」可被单测断言 |
| 负缓存 | **不做**（无效正则不入缓存） | 无效正则降级「始终显示」并记日志是既有行为；负缓存会改变日志频率语义。无效 pattern 的编译失败路径极快（立即抛错），无性能顾虑 |
| 编译计数 | `compileAttempts` 每次实际编译 +1 | 仅诊断/测试用，不属于匹配语义；`reset()` 供测试隔离 |

### 2. `shouldShowItem` 接入缓存（:301 一处替换）

```swift
// 改前：guard let regex = try? NSRegularExpression(pattern: regexString)
// 改后：guard let regex = matchAppIdRegexCache.regex(for: regexString)
```

静态共享实例 `static let matchAppIdRegexCache = MatchAppIdRegexCache()`（挂 TouchBarController，与 shouldShowItem 同为静态、两条路径共用）。

### 3. 两条调用路径 frontmostAppId 提出循环（最小改动，非重构）

- 同步 `createItems()`（:673）：`let frontmostAppId = frontmostApplicationIdentifier` 提至 for 循环前，循环内 `Self.shouldShowItem(definition, frontmostAppId: frontmostAppId)`。
- 异步 `reloadPresetAsync` main.async 闭包内（:894）：同样提至 newDefs 循环前（`self.frontmostApplicationIdentifier` 只取一次）。
- 等价性：两条循环体内部全同步（无 await/异步跳转），循环期间前台应用不可能变化；取一次与取 114 次结果相同。

## 三、单测覆盖（BarItemVisibilityTests.swift 同文件新增 MatchAppIdRegexCacheTests 类，5 用例，免 pbxproj 注册）

| 用例 | 断言 |
|------|------|
| testRepeatedEvaluationsReuseCompiledRegex | 同一 regexString 两次评估（一匹配一不匹配）→ compileCount == 1（缓存命中复用），count == 1 |
| testDistinctPatternsCompiledSeparately | 两个不同 pattern → 各自编译一次（compileCount == 2，count == 2） |
| testInvalidRegexIsNotCachedAndStillShows | 无效正则 `[` 两次评估 → 均返回 true（降级显示），compileCount == 2（**不做负缓存**，每次仍重新尝试编译），count == 0 |
| testCacheBoundedByMaxEntries | 插入 maxEntries+10 个不同 pattern → count 封顶 128；被淘汰的最旧 pattern 再次使用重新编译（compileCount 递增） |
| testConcurrentEvaluationsCompileEachPatternOnce | 200 次并发评估 × 20 个 pattern（DispatchQueue.concurrentPerform）→ 全部匹配结果正确、compileCount == 20（锁串行化，每个 pattern 恰好编译一次）、count == 20 |

测试隔离：setUp/tearDown 均 `reset()`，不影响既有 11 个 BarItemVisibilityTests（不依赖缓存状态）。

## 四、等价性论证

| 行为 | 改前 | 改后 |
|------|------|------|
| 无 matchAppId 规则 | true | true（未触碰） |
| nil 前台应用 | true | true（未触碰） |
| 有效正则匹配 | firstMatch != nil | firstMatch != nil（同一 NSRegularExpression 语义，仅实例被缓存复用） |
| 有效正则不匹配 | false | false |
| 无效正则 | 降级 true + AppLog.error | 降级 true + AppLog.error（每次评估均重新尝试编译并记日志——无负缓存，日志频次不变） |
| 缓存淘汰后复用 | — | 重新编译，结果等价（编译是无状态纯操作） |
| 前台应用取值 | 每 item 取一次 | 每轮评估取一次（同步循环内值不可能变） |

## 五、构建与测试实证

- 构建：`xcodebuild build -project LyricsMTMR.xcodeproj -scheme MTMR -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r17b-test CODE_SIGNING_ALLOWED=NO` → **BUILD SUCCEEDED**
- 测试：`xcodebuild test -project LyricsMTMR.xcodeproj -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r17b-test` → **TEST SUCCEEDED —— 134 用例 0 失败 0 意外**（129 基线 + 新增 5 全过；BarItemVisibilityTests 11 全绿、BarItemFactoryTests 18 全绿、金丝雀锚点 testGoldenAnchors2026/2027/Makeup2026 全绿）
- 独立 derivedDataPath（/tmp 前缀），未污染工作区 .build；本轮不触发全量回归（父任务未安排回归卡，基线 129 用例）

## 六、风险点

1. **缓存为进程内静态共享**：预设文件在运行时被编辑时，缓存中的旧 pattern 编译结果仍有效（正则源串未变，编译结果与源串一一对应，无失效问题）；仅当用户切换预设时新 pattern 按需编译、旧 pattern 被 FIFO 淘汰。
2. **无效正则在缓存中无记录**：若用户修正预设中的正则，下一次评估即重新尝试编译并成功入缓存——不存在「修了还显示旧行为」的窗口。
3. `compileCount`/`count`/`reset()` 为诊断/测试接口，不属于匹配语义，未参与任何运行时判断。
4. 黑名单整条隐藏机制（第二层）与 dock 正则（BarItemFactory「Bad regex」降级）本轮未触碰。

---

## 七、交付物清单

| 交付物 | 路径 |
|--------|------|
| 验证报告（本报告） | 仓库根 `验证报告_第17轮_隐藏机制正则缓存优化.md` |
| 代码落地 | `LyricsMTMR/MTMR/Core/TouchBarController.swift`（+93/-4：MatchAppIdRegexCache + shouldShowItem 接入 + 双路径 frontmostAppId 提出循环） |
| 新增测试 | `LyricsMTMR/MTMRTests/BarItemVisibilityTests.swift`（同文件新增 MatchAppIdRegexCacheTests 5 用例，无需 pbxproj 注册） |
| 技术债标注 | `LyricsMTMR/TECHNICAL_DEBT.md` ③ 条目追加第 17 轮跟进说明 |
| 迭代记录 | `iteration-log.md` 第 17 轮 / 子任务 B 追加 |
| 文档登记 | `LyricsMTMR/docs/file-structure.zh.md` 报告行 + mindmap 轮次 |

约束遵守：仅本工作区与 r17/feature 分支改动；未 push 远端（父任务收口统一推送）；未开新分支/新子任务/无 parents 依赖；不触发全量回归（分支内 134 用例实证已附）；新增测试类在同一文件内（未新增测试文件，无需 pbxproj 注册）。
