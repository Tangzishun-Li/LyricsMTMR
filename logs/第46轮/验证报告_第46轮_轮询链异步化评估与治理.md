# 验证报告_第46轮_轮询链异步化评估与治理

- 轮次：第 46 轮（功能/优化迭代第 34 轮）/ 子任务 A（实现/优化）
- 维度：后端服务（轮询链同步网络调用：阻塞影响面 / 异步化评估 / 落地治理）
- 分支：r46/async-network（worktree .worktrees/round46-A，基于父分支预建头 5154ab7）
- 前置基线：父任务分解前全量回归 471 用例 0 失败实证通过（TEST SUCCEEDED，/tmp/LyricsMTMR-dd-r46reg-test，基线口径 471+）
- 历史铺垫：R44 A 卡登记遗留「轮询链同步网络调用彻底异步化评估」（TBNet sync API 保持、调用点 15+、异步化重构改动面大）；R45 A 卡再次登记延续未承接；本轮承接该主线（第 34 轮功能/优化迭代）。

---

## 一、全仓盘点：sync API 调用点与阻塞影响面（20 处 / 13 消费文件 + 定义文件）

### 1.1 调用点清单（grep 取证）

| # | 调用 | 位置 | 请求超时 | 调用上下文 | 影响分类 |
|---|---|---|---|---|---|
| 1 | `TBNet.get` | RssUnread.swift:242（RSSDirectCounter.unreadCount） | 10s | RssUnreadItem.compute — direct 模式 **N feed 循环** | 轮询链串行叠加（本轮治理） |
| 2 | `TBNet.json` | RssUnread.swift:171（feedly） | 8s 默认 | RssUnreadItem.compute — provider 模式 | 轮询链同步阻塞（单发） |
| 3 | `TBNet.json` | RssUnread.swift:179（inoreader） | 8s 默认 | 同上 | 轮询链同步阻塞（单发） |
| 4 | `TBNet.json` | RssUnread.swift:188（miniflux） | 8s 默认 | 同上 | 轮询链同步阻塞（单发） |
| 5 | `TBNet.json` | RssUnread.swift:200（googleReader） | 8s 默认 | 同上 | 轮询链同步阻塞（单发） |
| 6 | `TBNet.get` | ApiLatency.swift:52 | 5s | ApiLatencyItem.compute | 轮询链同步阻塞（单发） |
| 7 | `TBNet.syncFetch` | ApiLatency.swift:83（directGet） | 5s | ApiLatencyItem.compute（直连会话） | 轮询链同步阻塞（单发） |
| 8 | `TBNet.json` | WeatherOutfit.swift:33 | 8s 默认 | WeatherOutfitItem.compute | 轮询链同步阻塞（单发） |
| 9 | `TBNet.json` | DailyQuote.swift:33 | 8s 默认 | DailyQuoteItem.compute | 轮询链同步阻塞（单发） |
| 10 | `TBNet.json` | SlackUnread.swift:32 | 8s 默认 | SlackUnreadItem.compute | 轮询链同步阻塞（单发） |
| 11 | `TBNet.json` | BilibiliFeed.swift:44 | 8s 默认 | BilibiliFeedItem.compute | 轮询链同步阻塞（单发） |
| 12 | `TBNet.json` | CiPipeline.swift:33 | 8s 默认 | CiPipelineItem.compute | 轮询链同步阻塞（单发） |
| 13 | `TBNet.syncFetch` | PackageTracker.swift:85（formPost） | 8s | PackageTrackerItem.compute | 轮询链同步阻塞（单发） |
| 14 | `TBNet.postJSON` | WordLookup.swift:62（deepseek） | 20s 默认 | 用户点击 → global 队列 | 用户动作同步阻塞（有界，非轮询链） |
| 15 | `TBNet.json` | WordLookup.swift:71（dictionaryapi） | 8s 默认 | 同上 | 用户动作同步阻塞（有界，非轮询链） |
| 16 | `TBNet.postJSON` | HomekitScene.swift:67（HA） | 20s 默认 | 用户点击 → global 队列 | 用户动作同步阻塞（有界，非轮询链） |
| 17 | `TBNet.postJSON` | HomekitScene.swift:93（米家） | 20s 默认 | 同上 | 用户动作同步阻塞（有界，非轮询链） |
| 18 | `TBNet.syncFetch` | ApiTester.swift:86（performRequest） | 10s | 用户点击 → global 队列 | 用户动作同步阻塞（有界，非轮询链） |
| 19 | `TBNet.postJSON` | AiSelectedText.swift:91 | 20s 默认 | 用户点击 → global 队列 | 用户动作同步阻塞（有界，非轮询链） |
| 20 | `TBNet.json` | CitationGen.swift:105（CrossRef） | 8s 默认 | 用户点击 → global 队列 | 用户动作同步阻塞（有界，非轮询链） |

（第 1-13 处 = 8 个轮询 widget 的 compute()；第 14-20 处 = 6 个用户触发工具，均已先派发到 global 队列再同步等待，不阻塞主线程。）

### 1.2 阻塞影响面结构事实（代码实证）

- **每 widget 独立串行队列**：TBPollItem/TBMetricPopoverItem 的 compute() 在各自专用队列执行（WidgetKit.swift:571 `com.l lyricsmtmr.poll.<id>`），loop() 的下一周期仅在当前周期完成后调度（scheduleNextCycle 单飞守卫，WidgetKit.swift:682-712/:843-868）。**同步网络调用只拉长该 widget 自身的轮询节奏并冻结其显示，不阻塞其他 widget，不阻塞主线程（apply() 才回主线程）。**
- **刷新节奏拉长**：死端点上每周期拉长 = timeout+1s（R44 已统一硬化：等待超时 task.cancel() + 合成 NSURLErrorTimedOut + 成功才读）。R44 已补 7 处显式 timeoutInterval=15 + StockBarItem 单飞守卫。
- **唯一串行叠加点 = RssUnread direct 模式**：N 个 feed 在 for 循环内逐个 TBNet.get(10s)，N 个死 feed 单周期阻塞 = N × 11s 线性叠加（实测代码路径 RssUnread.swift:60-69 → 本轮 :59-99）。这是「轮询链阻塞」的真实放大面。
- 用户触发工具（WordLookup/HomekitScene/ApiTester/AiSelectedText/CitationGen）全部先 `DispatchQueue.global().async` 再同步等待，UI 无冻结，仅结果延迟，且等待有界（timeout+1s）。

---

## 二、异步化评估（②）

### 2.1 候选方案对比

| 方案 | 改动面 | 风险 | 收益 |
|---|---|---|---|
| A. 全量 async/await 化（TBNet 加 async 变体 + TBPollItem loop 支持 async compute + 20 调用点迁移） | 基类轮询循环（~40 个 TBPollItem 子类共享）+ 13 个 widget 文件 + 7 个工具文件 | 高：MTMRTryOrError ObjC 异常保护无法包 async 代码、loop 调度语义重写、Swift 版本/部署目标约束、回归面大 | 中低：单发阻塞已由 R44 有界化（≤11s），全量异步化只再消除「单发等待拉长节奏」，但轮询间隔本就远大于等待上界（多数 widget 60-1800s），收益边际 |
| B. 保持 sync API，仅治理真实放大面（RssUnread direct 并行扇出） | RssUnread.swift computeDirect（约 30 行）+ 契约测试 | 低：行为语义保持（任一失败→失败态；全成功→求和），URLProtocol 桩可测 | 高：N×11s 串行叠加 → 单 feed 上界 ~11s；这是轮询链唯一线性放大点 |
| C. 不动 | — | — | 遗留挂账继续（R44/R45 已两次登记） |

### 2.2 结论与依据

**不值得全量异步化（方案 A 否决），落地代表性试点（方案 B）。** 依据：

1. **阻塞已全局有界**：R44 硬化后每个 syncFetch 有 timeout+1s 上界，无无限停摆路径；调用点 20 处（13 消费文件）均有界。
2. **无全局/主线程阻塞**：每 widget 独立串行队列 + 用户触发工具先派发 global 队列——「轮询链阻塞」的实际放大面只有 RssUnread direct 的 N feed 串行叠加（N×11s），这是本轮治理对象。
3. **全量异步化改动面/收益比差**：需要重写 TBPollItem 基类轮询循环（影响 ~40 个子类）并迁移 20 处调用点，而收益仅是消除「单发等待拉长该 widget 节奏」（≤11s，多数 widget 间隔 60s+，感知微弱）；MTMRTryOrError 异常保护语义、调度单飞守卫、Swift 并发严格性均为额外风险。为改而改违背本轮原则。
4. **试点选择**：RssUnread direct 并行扇出——单一文件、语义可保持（任一失败→失败态「—」、全成功→求和）、URLProtocol 桩可测并发（maxConcurrent / 墙钟上界），是「消除轮询链阻塞」的代表性落地。

---

## 三、落地治理（③）：RssUnread direct 并行扇出

### 3.1 实现（RssUnread.swift:59-99）

- 串行 for 循环 → `DispatchGroup` + `DispatchQueue.global().async` 每 feed 一个并发任务；每任务内 `RSSDirectCounter.unreadCount` 保持同步 API（TBNet.get 10s 硬化上界不变）。
- 结果收集：`NSLock` 保护 `total`/`anyFailed`（多任务并发写），`group.wait(timeout: .now()+30)` 后读——每个 feed 自身有 ≤11s 硬上界，30s 仅为防御性安全网（防未来回归移除单发超时）。
- 语义保持：任一 feed 失败 → `fetchFailed = true` + `unread = 0`（apply 显示「—」+coral，与 R44/R45 失败面契约一致）；全部成功 → `unread = Σcount`（顺序无关）。
- API 兼容：TBNet sync API 零改动；RssUnreadItem 对外接口零改动；仅 computeDirect 内部实现变化。

### 3.2 契约测试（④，NetworkRobustnessContractTests 同文件扩展 +4 用例）

新增 `ParallelFeedURLProtocol` 桩（按 URL 路由成功 payload/即时失败，每请求延迟 delay，统计同时在途峰值 maxConcurrent）+ 4 用例：

| 用例 | 契约 | 红→绿实证 |
|---|---|---|
| testRssDirectParallelFetchesOverlap | N feed 必须同时在途（maxConcurrent ≥ 2）+ 合计正确（3 feed×1=3） | 红（串行 maxConcurrent=1）→ 绿 |
| testRssDirectParallelWallTimeBoundedBySingleFeed | 整轮 compute 由单 feed 上界界定（3×0.5s 延迟 < 1.2s，串行 ≥1.5s） | 红（串行 1.56s）→ 绿 |
| testRssDirectParallelAnyFailureShowsFailureState | 任一 feed 失败 → 「—」+coral（部分成功不得当成功） | 双绿（语义保持钉） |
| testRssDirectParallelSumsAllFeeds | 全成功 → 求和（1+2+1=4）+ 成功态 iconTint gold | 双绿（语义保持钉） |

- **红→绿双跑实证**：先跑 13 用例（9 既有 + 4 新增）→ **3 failures**（并行两契约 + 求和用例 1 处颜色断言笔误 sRGB 0.96 vs 1,0.8,0.35——修正断言为真实契约 iconTint=gold 后）；实现并行扇出后复跑 → **13/13 全绿，断言零放宽**。
- 零真实网络：全部经 URLProtocol 桩（127.0.0.1:9 拒绝端口 + 注入 sessionOverride，R44 同型钩子）。

---

## 四、全量回归实证（471 基线 + 4 新增 = 475）

| 项 | 值 |
|---|---|
| 命令 | xcodebuild test（UnitTests, Debug），独立 /tmp/LyricsMTMR-dd-r46a-test（先清旧 /tmp/LyricsMTMR-dd-* 0 残留 + caffeinate -i 防显示器休眠） |
| 用例口径 | 471 基线 + 新增 4 = **475** |
| 结果 | **TEST SUCCEEDED —— Executed 475 tests, with 0 failures (0 unexpected)**（25 套件逐套件 0 失败） |
| 金丝雀 | StockMarketHoursTests · WidgetLeakTests · RegistryReconciliationTests · ItemTypeDecodeRegistryTests · WriteSideContractTests · SecretsManagerContractTests · **NetworkRobustnessContractTests 13/13（9 既有 + 4 新增）** 全绿 |
| 锚点巡检 | scripts/anchor-patrol.py 复跑 **PASS 67 / WARN 16 / INFO 5 / ERROR 0 退出码 0**（改动文件 RssUnread.swift / NetworkRobustnessContractTests.swift 均非锚点文件，锚点行号零漂移；REGISTRY +1 = 本报告登记） |

---

## 五、文档同步表

| 文件 | 动作 |
|---|---|
| iteration-log.md | 「## 第 46 轮」+「### 父任务」预建头由父分支提供；补建「### 子任务记录」小节头并追加本记录，标注「第 46 轮 / 子任务 A」 |
| file-structure.zh.md | 本报告行登记（根目录报告区，无重复行） |
| 本报告 | 验证报告_第46轮_轮询链异步化评估与治理.md（分支根目录） |

---

## 六、结论

轮询链同步网络调用经全仓盘点（20 处 / 13 消费文件）确认：阻塞全局有界（R44 硬化）、无主线程/跨 widget 冻结，唯一线性放大面为 RssUnread direct 模式 N feed 串行叠加（N×11s）。评估结论：**不值得全量异步化**（基类轮询循环重写改动面大、收益边际），落地**代表性试点**——direct 模式并行扇出，单周期阻塞由 N×11s 收敛为 ≤11s，失败面/求和语义零变化，4 用例红→绿双跑实证未放宽断言。TBNet sync API 保持不动（调用点兼容），遗留「彻底异步化」登记更新为本轮评估闭环结论。
