# 第 9 轮回归报告 — main（含内存修复代码）全量构建 + 单测回归验证

- 任务：`t_d0232788`（第 9 轮 / 子任务 A·全量回归，本链第 3 轮）
- 验证对象：main = `59a0d24`（含内存修复代码：AppDelegate.swift +65 /
  UnifiedSettingsWindowController.swift +40，关窗=隐藏复用 + 闲置 GC + 内存压力释放）
- 工作区分支：`r9/regression`（预建 dir 工作区，基于 main@59a0d24）
- 执行时间：2026-08-12 14:33–14:36（+0800）
- 环境：macOS 15.7.7 / Xcode 16.4 (16F6) / arm64，Debug 配置，CODE_SIGNING_ALLOWED=NO

## 0. 回归背景（为何本轮必须全量回归）

第 8 轮收尾清理 merge（28e65b6）误将 main 工作区未提交的内存修复改动（关窗=隐藏复用+
闲置 GC，注释"内存修复 2026-08-12"）一并合入 main；第 8 轮收口时误判"无代码改动"未触发
回归。按回归规则（隔代父任务 + 代码改动实际入 main），本轮全量回归验证前两轮积累变更
（尤其内存修复代码）是否破坏主干。已核验：main@59a0d24 中两个修复文件与后续 PR #41
（2d2d681，2066fca merge）合入版本逐字节一致（git diff 0 行），回归对象即最终合入代码。

## 1. 执行命令（与官方 CI .github/workflows/build-test.yml 一致）

```sh
cd LyricsMTMR
xcodebuild build -project LyricsMTMR.xcodeproj -scheme MTMR      -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r9reg CODE_SIGNING_ALLOWED=NO
xcodebuild test  -project LyricsMTMR.xcodeproj -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r9reg CODE_SIGNING_ALLOWED=NO
```

> 使用独立 derivedDataPath `/tmp/LyricsMTMR-dd-r9reg`，避免与并行构建共享
> `/tmp/LyricsMTMR-dd` 的 build.db 锁冲突（第 7 轮曾遇 exit 65）。本轮无并行争抢。

## 2. 结果摘要

| 步骤 | 命令 | 结果 | exit | 耗时 (real) |
|---|---|---|---|---|
| 构建 | xcodebuild build, scheme=MTMR, Debug | **BUILD SUCCEEDED** | 0 | ~52s（冷 derived data，无并行争抢） |
| 测试 | xcodebuild test, scheme=UnitTests, Debug | **TEST SUCCEEDED** | 0 | ~97s（含测试 target 编译） |

## 3. 单测统计：60 用例，0 失败，0 意外

| 测试套件 | 用例数 | 结果 |
|---|---|---|
| AppleScriptDefinitionTests | 5 | ✅ 0 失败 |
| BackgroundColorTests | 2 | ✅ 0 失败 |
| MirrorFingerprintTests | 14 | ✅ 0 失败 |
| NetEaseLRUCacheTests | 6 | ✅ 0 失败 |
| ParseConfig | 7 | ✅ 0 失败 |
| SettingsTabCacheTests | 6 | ✅ 0 失败 |
| StockMarketHoursTests | 16 | ✅ 0 失败 |
| WidgetLeakTests | 4 | ✅ 0 失败 |
| **合计** | **60** | **0 失败 / 0 意外** |

已知易碎点核验（均通过，无时区/日历环境问题）：
- `testGoldenAnchors2026` ✅
- `testGoldenAnchors2027` ✅
- `testGoldenAnchorsMakeup2026` ✅

## 4. 与第 7 轮基线对比（main@b405839，60 用例 0 失败）

| 维度 | 第 7 轮（main@b405839） | 第 9 轮（main@59a0d24，含内存修复） | 变化 |
|---|---|---|---|
| 用例总数 | 60 | 60 | 持平 |
| 失败 / 意外 | 0 / 0 | 0 / 0 | 持平 |
| 构建耗时 | 293.37s（冷 + 并行争抢） | ~52s（冷，无并行） | 更快（环境因素） |
| 测试耗时 | 159.42s | ~97s | 更快（环境因素） |
| 构建告警 | 10 条 | 10 条（构成一致） | 持平 |
| 易碎点（3 金丝雀） | 全过 | 全过 | 持平 |

耗时差异均为环境因素（第 7 轮冷 derived data 叠加并行任务争抢 CPU），非代码变化；
内存修复代码未引入新告警（见 §5）。

## 5. 构建告警

10 条 warning，与第 7 轮基线构成一致：
- WeatherBarItem.swift 未使用变量/不可变值 ×4（:134/:200/:202/:210）
- onChange(of:perform:) 弃用提示 ×3（KeyBindingTabView.swift:712/:731、TouchBarSimulatorView.swift:191）
- NSImage? non-sendable ×2（LyricsEngine.swift:809/:994）
- appintentsmetadataprocessor 元数据跳过提示 ×1（非代码）

**内存修复涉及的两个文件（AppDelegate.swift / UnifiedSettingsWindowController.swift）零警告。**
均为既有代码风格/并发标注类，非本次回归引入，不阻断。

## 6. 内存修复代码验证结论

回归对象 = 关窗即隐藏复用（windowShouldClose → orderOut 隐藏而非销毁）+ 1h 闲置 GC
（settingsWindowIdleGCSeconds=3600，`scheduleSettingsWindowGC`）+ 内存压力立即释放
（`releaseSettingsWindowIfHidden`）+ DockVisibilityManager.track 幂等化（重注册先移除旧
observer，防隐藏复用场景 token 累积）。上述代码编译通过、零警告，未破坏任何单测
（设置窗口相关 SettingsTabCacheTests 6 用例全绿）。

**覆盖面说明（风险点）**：内存修复属运行时 UI 生命周期行为（NSWindow 隐藏/复用、
dispatch_after 定时释放、Dock 图标显隐联动），现有单测无直接覆盖（60 用例均为
配置解析/日历/缓存/指纹类）。建议真机/交互冒烟验证三项：① 连续开关设置窗口 8+
次后进程内存不再 ~10MB/轮线性增长；② 设置窗口隐藏 1h 后（或内存压力模拟）整树释放、
内存回落到基线；③ 复用路径下 Dock 图标随窗口显隐正确收起/显示。此项不影响本轮
回归通过结论，仅作为行为级验证建议挂账。

## 7. 结论

**通过。** main@59a0d24（含内存修复代码）主干构建与全部 60 项单元测试保持绿色，
与第 7 轮基线完全一致（60 用例 0 失败 0 意外），内存修复代码未破坏主干，前两轮
积累变更（第 8 轮误合入的内存修复 + 第 8 轮各文档变更）无回归。

本轮为只读回归，无代码改动、无修复提交；产出为本报告 + iteration-log.md 第 9 轮
子任务 A 记录（均提交于 r9/regression 分支）。
