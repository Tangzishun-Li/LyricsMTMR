# 验证报告_第28轮_闲置GC策略可测化

> 第 28 轮（功能/优化迭代第 16 轮）/ 子任务 A · 分支 `r28/gc-strategy`（基于 main@2905892）
> 任务：t_cc3c287b · 遗留④前半句闭环 —— 内存修复（t_5e363548，设置窗口复用 + 闲置 GC）的「闲置 GC 决策逻辑」提取为可单测纯策略 + 单测覆盖
> 日期：2026-08-13

## 一、任务定位

内存修复报告《内存修复报告_t5e363548_设置窗口复用.md》结论：关窗=隐藏复用（不销毁，重开零重建零增长）；闲置 1 小时（或系统内存压力）才整体释放。当前实现中「何时该释放」的决策逻辑（闲置超时 OR 内存压力 OR 窗口在屏不释放）内嵌在 AppDelegate 的 DispatchWorkItem / 内存压力处理器里，属运行时接线层，不可单测。窗口生命周期本身是运行时行为（3 项真机冒烟挂账不变，非本卡范围），但**决策逻辑是纯策略**——本卡将其提取为可单测组件，生产行为逐字节等价，零行为变更。

第 27 轮 B 卡已有同类先例：SettingsWindowVisibilityTracker 可单测状态机 + WeatherLocationSession.shouldStopForViewState 纯策略。

## 二、评估：提取边界声明（如实，不虚构可测性）

对 AppDelegate 现有 GC 相关代码逐段评估，结论如下：

| 现状代码（AppDelegate.swift） | 性质 | 处置 |
|---|---|---|
| `settingsWindowIdleGCSeconds=3600` 常量（:172） | **纯常量**——策略的唯一调优参数 | 提取为 `SettingsWindowGCStrategy.idleReleaseThreshold`，AppDelegate 保留同值别名（见 §三.2） |
| `scheduleSettingsWindowGC()` 中 DispatchWorkItem + `asyncAfter(3600s)`（:181-193） | **运行时接线**——GCD 调度、主队列、弱引用捕获、work item 生命周期，均依赖 Dispatch/AppKit 运行时 | 留在 AppDelegate；仅释放决策体改为调用策略 |
| 定时器触发体的 `unifiedSettingsController = nil` | **决策**（何时释放）+ **副作用**（释放强引用） | 决策提取为策略调用；副作用留在接线层 |
| `releaseSettingsWindowIfHidden()` 的 `window?.isVisible != true` 判断（:197-203） | **决策**（内存压力下是否立即释放） | 提取为策略调用（visible guard 进矩阵） |
| `openSettings` 开窗即 cancel 待执行 GC（:218-219） | **生命周期不变量**（重开 ⇔ 窗口可见 ⇔ 不应释放）——取消调度是接线动作，其语义（可见窗口绝不释放）由策略矩阵覆盖 | 留在接线层（不新增策略调用；策略的 visible guard 即该不变量在矩阵中的表达） |
| `applicationDidReceiveMemoryWarning`（:74-81） | **事件接线**（系统通知 → 清缓存 + 调 release 路径） | 留在接线层 |

**边界结论**：可提取为纯策略的 = 「释放决策矩阵」（visible guard / 内存压力短路 / 闲置阈值比较）+ 调优常量；必须留在 AppDelegate 运行时接线层的 = GCD 调度与取消、AppKit 可见性读取（`window.isVisible` 本身）、强引用副作用（置 nil）、系统通知观察。策略函数是纯函数（无状态、无副作用），因此单测覆盖决策矩阵即可等价覆盖全部三个决策入口（定时器路径、压力路径、重开取消路径的语义）。

## 三、方案设计与落地

### 3.1 新增纯策略组件

新文件 `LyricsMTMR/MTMR/Preferences/SettingsWindowGCStrategy.swift`（已注册进 LyricsMTMR target）：

```swift
enum SettingsWindowGCStrategy {
    static let idleReleaseThreshold: TimeInterval = 3600   // 1 h（调优设计值，含决策注释）

    static func shouldRelease(isWindowVisible: Bool, memoryPressure: Bool, idleElapsed: TimeInterval) -> Bool {
        if isWindowVisible { return false }   // 窗口在屏守卫：使用中的窗口绝不释放
        if memoryPressure { return true }     // 内存压力短路：隐藏即立即释放
        return idleElapsed >= idleReleaseThreshold   // 闲置阈值路径
    }
}
```

### 3.2 AppDelegate 改动（运行时接线层仅调用策略）

| 位置 | 改动 | 等价性 |
|---|---|---|
| AppDelegate.swift:172 | 常量改为向后兼容别名 `settingsWindowIdleGCSeconds = SettingsWindowGCStrategy.idleReleaseThreshold` | 值恒等（别名直接引用策略常量，不可能分叉）；内存修复报告 / 控制器注释对 `AppDelegate.settingsWindowIdleGCSeconds` 的引用保持字面成立 |
| AppDelegate.swift:181-200（scheduleSettingsWindowGC） | 定时器触发体：`unifiedSettingsController = nil` 改为 `if shouldRelease(isWindowVisible: 实际可见性, memoryPressure: false, idleElapsed: 阈值) { ... = nil }` | 可达状态空间内逐字节等价（论证见 §四） |
| AppDelegate.swift:207-217（releaseSettingsWindowIfHidden） | `window?.isVisible != true` 判断改为 `shouldRelease(isWindowVisible: 实际可见性, memoryPressure: true, idleElapsed: 0)` | 矩阵等价：visible→false（不释放）；!visible+pressure→true（释放）；controller 为 nil 时 isWindowVisible=false→true→置 nil 为无操作，与现状 `nil != true → 置 nil`（无操作）同结果 |

`openSettings`、`applicationDidReceiveMemoryWarning`、`onWindowHidden`/`onWindowWillClose` 回调接线零改动（仅注释保持原样）。

### 3.3 决策矩阵与现状对照

| 输入（可见性, 压力, 闲置时长） | 现状行为 | 策略输出 | 一致 |
|---|---|---|---|
| 隐藏, 无压力, < 3600s | 不释放（继续缓存） | false | ✅ |
| 隐藏, 无压力, = 3600s | 释放（定时器触发） | true | ✅ |
| 隐藏, 无压力, > 3600s | 释放 | true | ✅ |
| 隐藏, 压力, 任意时长 | 立即释放 | true（短路） | ✅ |
| 可见, 压力, 任意时长 | 不释放（releaseSettingsWindowIfHidden 守卫） | false（guard 优先） | ✅ |
| 可见, 无压力, 任意时长 | 不释放（重开即取消待执行 GC） | false | ✅ |

## 四、行为等价论证（逐字节）

1. **常量**：`settingsWindowIdleGCSeconds` 由字面量 3600 改为对策略常量的直接引用——两处 `deadline: .now() + Self.settingsWindowIdleGCSeconds`（:200）与传参（:193）取值不变，定时时长不变。
2. **压力路径**（:207-217）：原判断 `unifiedSettingsController?.window?.isVisible != true` ⇔ 新判断 `shouldRelease(isWindowVisible: unifiedSettingsController?.window?.isVisible == true, memoryPressure: true, idleElapsed: 0)`——矩阵第二行：可见→false（不释放），不可见/无窗口→true（置 nil）。逐输入同值。cancel 前置动作（:208-209）零改动。
3. **定时器路径**（:181-200）：原触发体无条件置 nil；新触发体先经策略。**可达状态空间内二者恒同**：调度只发生在 `onWindowHidden`（关窗隐藏）时；唯一能令窗口重新可见的入口 `openSettings` 必先 cancel 待执行 work item（:218-219 原逻辑未动），故定时器触发时窗口必处于隐藏态 → `isWindowVisible=false` → 策略返回 `idleElapsed(3600) >= 3600 = true` → 置 nil。即：**任何可达状态下新旧代码执行同一组副作用**。
4. **不可达状态差异（如实登记）**：若（理论上不可能地）窗口在定时器触发时可见——旧代码会释放一个正在使用的窗口（原设计缺陷），新代码经 visible guard 拒绝释放。该差异不可观测（无路径可达），属守卫加固而非行为变更；若未来引入新的开窗路径绕过 `openSettings`，新代码的行为是正确侧。
5. **幂等性**：释放副作用 = `unifiedSettingsController = nil`（赋 nil 天然幂等）；策略为纯函数，重复求值结果恒同。接线层 cancel-调度序列（schedule/cancel/释放）原样保留。

## 五、单测清单（9 用例，决策点全覆盖）

新文件 `LyricsMTMR/MTMRTests/SettingsWindowGCStrategyTests.swift`（add_files.py Tests: 前缀注册进 LyricsMTMRTests target）：

| 用例 | 决策点 | 断言 |
|---|---|---|
| testBelowThresholdDoesNotRelease（:22） | 未到闲置阈值不释放 | (false,false,3599)→false |
| testAtThresholdReleases（:34） | 超阈值释放（≥ 边界包含） | (false,false,3600)→true |
| testAboveThresholdReleases（:47） | 超阈值释放（asyncAfter 实际触发 ≥ 名义时长恒落此区间） | (false,false,7200)→true |
| testMemoryPressureReleasesImmediatelyWhenHidden（:61） | 内存压力立即释放（elapsed=0） | (false,true,0)→true |
| testMemoryPressureReleasesRegardlessOfIdleElapsed（:73） | 压力短路无视闲置时长 | (false,true,0.5)→true |
| testVisibleWindowNeverReleasesUnderMemoryPressure（:88） | 窗口在屏守卫（压力路径） | (true,true,0)→false |
| testVisibleWindowNeverReleasesAfterLongIdle（:100） | 窗口在屏守卫（闲置路径） | (true,false,86400)→false |
| testVisibleWindowNeverReleasesAtAnySignalCombination（:112） | 守卫全组合（含 3 组信号组合遍历） | (true,*)→false |
| testIdleReleaseThresholdPinnedToOneHour（:134） | 调优常量回归钉 | idleReleaseThreshold == 3600 |

不凑用例数：窗口生命周期调度（DispatchWorkItem/asyncAfter/通知）属运行时接线，按 §二 边界声明不在单测范围（与第 27 轮 B 卡「视图 onChange 接线经代码审查 + 策略单测等价覆盖」同构）；策略纯函数无状态，幂等性由接线层 nil 赋值天然保证，不设重复用例。

## 六、实证表（分支验证）

- 命令：先 `rm -rf /tmp/LyricsMTMR-dd-*` 清理旧目录，再 `xcodebuild test -project LyricsMTMR.xcodeproj -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r28a-test`（日志 /tmp/r28a-test.log）
- 结果：**TEST SUCCEEDED —— Executed 237 tests, 0 failures (0 unexpected) in 167.6s**（228 基线 + 新增 9）
- 金丝雀：StockMarketHoursTests 三锚点（testGoldenAnchors2026/2027/Makeup2026）全绿 + 全套件 15 用例 0 失败；WidgetLeakTests 8 用例全绿（无新泄漏）
- 新增套件：SettingsWindowGCStrategyTests 9/9 passed（套件级 passed 实测）
- 构建告警：无新增 warning（xcodebuild 输出无本项目新增告警）

## 七、遗留登记

- 闭环：遗留④前半句（闲置 GC 决策逻辑不可单测）——决策矩阵提取为 SettingsWindowGCStrategy 纯策略 + 9 用例全覆盖。后半句（真机冒烟 3 项：连续开关 8+ 次内存不线性增长 / 隐藏 1h 或内存压力后整树释放回基线 / 复用路径 Dock 图标显隐正确）仍挂账，属运行时行为，非本卡范围。
- 挂账延续：真机冒烟延续挂账不变（第 8/17~27 轮同口径，依赖 Touch Bar 真机）。
- 新增登记：无新增生产观察项。
- 不可达状态守卫差异（§四.4）如实登记，无需后续动作。

## 八、约束遵守

仅动本工作区（.worktrees/round28-A，分支 r28/gc-strategy）；未 push 远端（父任务收口统一推送）；未开新分支/新子任务；无 parents 依赖；未建 cron/自触发。完成自查 git status 干净 + commit 已提交（第 14 轮 B 卡漏提交教训）。
