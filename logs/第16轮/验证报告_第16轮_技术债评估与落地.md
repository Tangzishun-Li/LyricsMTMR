# 验证报告_第16轮_技术债评估与落地

- 轮次：第 16 轮（功能/优化迭代第 4 轮）· 子任务 B（代码质量与工程规范维度）
- 任务卡：t_fc7efdb5（分支 r16/techdebt，工作区 `.worktrees/round16-B`，基于 main@5d2c4fa）
- 落地目标：TECHNICAL_DEBT.md 置顶 TODO 剩余 3 条逐条调研评估 + 至少落地一条
- 日期：2026-08-12

---

## 〇、结论速览

| # | TODO | 结论 | 一句话依据 |
|---|------|------|-----------|
| ① | try view controllers on `NSCustomTouchBarItem` instead of subclassing item itself | **暂缓** | 全 widget 体系（6 Core + 90+ Widgets 类）直承 item 基类，工厂/测试/镜像窗三方联动依赖 item 具体类型，重构收益仅架构整洁、风险高 |
| ② | try move away from enums when parse preset – enums are hard to extend | **暂缓** | 4 处巨型 switch（各 98 case）依赖枚举编译期穷尽性安全网；113/114 是「Item 类型全集」口径而非枚举 case 数，注册表混合扩展点已存在 |
| ③ | find better way to hide bar items | **✅ 已落地** | 隐藏机制两层（matchAppId per-item + 黑名单整条隐藏）；提取纯函数 `shouldShowItem` + 修复异步主题切换绕过 matchAppId 的不一致 bug + 11 单测 |

---

## 一、① try view controllers on `NSCustomTouchBarItem`（暂缓）

### 现状调研（源码实证）

**继承体系**：全部 item 类直接继承 `NSCustomTouchBarItem` / `NSPopoverTouchBarItem`，没有「item 外壳 + VC 承载视图」的两层结构：

- Core 6 个 item 类：
  - 直承 `NSCustomTouchBarItem`：`BasicView`（BasicView.swift:42）、`CustomButtonTouchBarItem`（CustomButtonTouchBarItem.swift:23）、`ScrollViewItem`（ScrollViewItem.swift:3）、`SwipeItem`（SwipeItem.swift:12）
  - 经 `CustomButtonTouchBarItem` 间接承：`AppleScriptTouchBarItem`（AppleScriptTouchBarItem.swift:3）、`ShellScriptTouchBarItem`（ShellScriptTouchBarItem.swift:10）
- Widgets 90+ 类（grep 实测按直接父类分）：34 个 `TBPopoverItem` 子类 + 29 个 `TBPollItem` 子类 + 19 个 `CustomButtonTouchBarItem` 子类 + 12 个直承 `NSCustomTouchBarItem`/`NSPopoverTouchBarItem`（含两个基类 `TBPollItem` WidgetKit.swift:546、`TBPopoverItem` WidgetKit.swift:477，及 GroupBarItem/VolumeViewController/BrightnessViewController 等）

**下游依赖 item 具体类型**（改造成本的连带面）：

1. `BarItemFactory.createItem` 98-case 构造 switch（BarItemFactory.swift:54-280）逐 case `barItem = XxxBarItem(...)`，直接以具体类构造并返回，类型即契约。
2. `BarItemFactoryTests` 18 用例断言 `item is XxxBarItem` 具体类型（BarItemFactoryTests.swift）。
3. 镜像窗 `TouchBarMirrorWindowController.swift:378` 按 `as? NSCustomTouchBarItem` 访问 `.view` 取快照/文本。
4. `VolumeViewController`/`BrightnessViewController` 已是「伪 VC」命名——实为 `NSCustomTouchBarItem` 子类（System/VolumeViewController.swift:6、System/BrightnessViewController.swift:6），说明历史上从未引入 VC 承载模式。

### 评估

- **难度**：高。涉及全部 90+ widget 类的内部视图承载方式重写（`customizationLabel`/`view` 属性 → `viewController`），非机械替换。
- **影响面**：全 widget 体系 + 工厂 switch + 工厂测试 + 镜像窗 4 处联动；镜像窗按 `.view` 取快照的路径依赖 item 直承关系，改造后需同步调整访问链。
- **风险**：高。收益为架构整洁（VC 生命周期管理、视图复用），无任何功能增益；回归面 = 全 widget 渲染。
- **结论**：**暂缓**。
- **前置条件**（若未来要做）：按 widget 逐个迁移、保持 item 类型不变（仅内部改用 `viewController` 承载视图）；先做 1 个试点（VolumeViewController/BrightnessViewController 天然候选）验证后再推广；每批迁移后跑 BarItemFactoryTests 与镜像窗冒烟。

---

## 二、② try move away from enums when parse preset（暂缓）

### 现状调研（源码实证）

解析链路共 4 处巨型 switch 依赖枚举穷尽性（**每处均为 98 case**，grep 逐行实测）：

| 位置 | 行号 | case 数 |
|------|------|---------|
| `ItemType` 枚举声明 | ItemsParsing.swift:285-384 | 98 |
| `ItemTypeRaw` 字符串枚举 | ItemsParsing.swift:485-584 | 98 |
| decode switch（`ItemTypeRaw` → `ItemType`） | ItemsParsing.swift:589-981 | 98 |
| `identifierBase` 映射 switch | TouchBarController.swift:24-223 | 98 |
| 工厂构造 switch | BarItemFactory.swift:54-280 | 98 |

**口径澄清**：TECHNICAL_DEBT.md 旧文「ItemType 113 case」不准确——113/114 是 **Item 类型全集**口径（ItemTypeRaw 98 + SupportedTypesHolder 预定义 14 + 控制器注册 2 = 114，第 14/15 轮文档口径），**枚举/switch 的 case 数均为 98**。本轮评估以 98 为准。

**穷尽性是安全网（关键收益）**：新增 widget 时编译器强制 4 处 switch 全部补齐，漏一处即编译失败。注册链 6 处文档化于 docs/internal-apis.zh.md §2.3，第 15 轮 holidayCountdown 新增即受益于该机制。

**已有注册表扩展点**：`SupportedTypesHolder`（ItemsParsing.swift:82-283）已用 `[String: ParametersDecoder]` 字典为预定义类型提供字符串键扩展（14 个：escape/delete/brightnessUp/.../displaySleep），证明「注册表」模式在本代码库可行，但**它不经过 ItemTypeRaw 解码 switch**，与主解析路径并行。

### 评估

- **难度**：中。若只迁 decode 分支 → 注册表，需重写 `init(from:)` 98 分支的调用方式，并处理关联值解码差异。
- **影响面**：解析层全部 + 依赖 decode 语义的 18 个工厂测试 + ElementPaletteView 注册。
- **风险**：中高。**核心风险 = 失去编译期穷尽检查**：字典驱动下漏注册某 type 不会编译报错，而是运行时静默降级/失败，需另立测试兜底（如「注册表键集 == ItemTypeRaw 全集」对账测试）；这是当前安全网换便利的净损失。
- **结论**：**暂缓**。当前 widget 增速（每轮 1 个）远未到枚举成为瓶颈的程度；decode 98 分支虽是样板代码，但编译器保证其与枚举同步。
- **前置条件**（若未来 widget 增速显著）：将 decode 分支逐步迁入注册表（保留枚举为编译期枢纽、注册表作扩展槽），并补「注册表键集对账测试」补回穷尽性；不宜整体推翻枚举架构。

---

## 三、③ find better way to hide bar items（✅ 已落地）

### 现状调研（源码实证）：隐藏机制实为两层

第 15 轮侦察「TouchBarController 无 hidden 逻辑」**不准确**，源码实测隐藏机制有两层：

1. **per-item 条件创建**：`matchAppId` 通用参数（preset 中每 item 可配正则），仅当前台应用 bundle id 匹配时才创建该 item。原实现内联在 `createItems()`（同步路径，TouchBarController.swift:665 起）与 `reloadPresetAsync`（异步主题切换路径）中——**异步路径此前完全没有该过滤**。
2. **整条 Touch Bar 黑名单隐藏**：`blacklistAppIdentifiers`（来自 `AppSettings.blacklistedAppIds`，TouchBarController.swift:246/364）→ 前台应用在黑名单内时 `dismissTouchBar()` 整条隐藏：`updateActiveApp`（:489）与 `presentTouchBarWithCurrentItems`（:771）。该机制与 per-item 隐藏正交，已覆盖两条路径，本轮不动。

### 落地变更明细

**1. 提取纯函数 `TouchBarController.shouldShowItem(_:frontmostAppId:)`**（TouchBarController.swift:298-307，新增）

```swift
static func shouldShowItem(_ definition: BarItemDefinition, frontmostAppId: String?) -> Bool
```

- 无 AppKit/控制器状态依赖 → 可直接单测；
- 规则：无 `matchAppId` 参数 → 恒显示；无前台应用信息 → 显示（保持历史语义）；正则无效 → 降级显示 + AppLog 告警（保持历史行为）；否则按正则首匹配判定。

**2. `createItems()` 同步路径改用该函数**（TouchBarController.swift:673，语义等价）

原内联逻辑（git diff 实证逐分支对照）：

| 分支 | 原逻辑 | shouldShowItem | 等价 |
|------|--------|----------------|------|
| 无 `matchAppId` 参数 | show=true | return true | ✅ |
| 无前台应用（nil） | show=true | return true | ✅ |
| 正则无效 | 告警 + show=true | 告警 + return true | ✅ |
| 正则匹配 | show=true | return true | ✅ |
| 正则不匹配 | show=false | return false | ✅ |

**3. `reloadPresetAsync` 异步路径补上同一过滤**（TouchBarController.swift:894，**行为修复**）

- 修复前：异步主题切换重建 items 时跳过 `matchAppId` 过滤 → 配置了 matchAppId 的 item 在主题切换后「复活」，与同步路径行为不一致；
- 修复后：`guard Self.shouldShowItem(def, frontmostAppId: self.frontmostApplicationIdentifier) else { continue }`，两路径一致。

**4. 新增 `BarItemVisibilityTests`（MTMRTests/BarItemVisibilityTests.swift，11 用例）**

| 分组 | 用例 | 断言要点 |
|------|------|---------|
| 无规则恒显示（2） | testNoMatchAppIdShowsWithNilFrontmostApp / WithAnyApp | 无 matchAppId 时无论前台应用与否均显示 |
| 匹配显示（3） | ExactMatch / SubstringMatch / RegexPattern | 完全匹配、子串正则、`^com\.apple\..*` 均显示 |
| 不匹配隐藏（2） | MismatchHides / SubstringMismatchHides | 不匹配/子串不匹配均隐藏 |
| 无前台应用（1） | WithNilFrontmostAppShows | 保持历史语义：无法判定 → 显示 |
| 无效正则（1） | InvalidRegexShowsAndDoesNotCrash | 降级显示、不崩溃 |
| 解码链路（2） | DecodesFromJSON / NoMatchAppIdLeavesParamAbsent | matchAppId 经 JSON 解码往返后判定仍正确；未配置时参数键缺席 |

### 构建与测试实证

- 构建+测试：`xcodebuild test -project LyricsMTMR.xcodeproj -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r16b-test` → **TEST SUCCEEDED —— 129 用例 0 失败 0 意外**（118 基线 + 新增 11 全过，xcresult 实证）
- 金丝雀锚点：全绿（testGoldenAnchors2026/2027/Makeup2026，xcresult 3 个全过）
- 独立 derivedDataPath（/tmp 前缀），未污染工作区 .build

### 风险点

1. **行为变化仅一处**：异步主题切换路径新增 matchAppId 过滤——这是修复（对齐同步路径语义），非回归；若用户依赖「主题切换后 matchAppId item 强制显示」的旧怪癖，行为将改变（符合文档语义）。
2. 黑名单整条隐藏机制（第二层）本轮未触碰，行为不变。
3. 纯函数提取保持静态方法挂载于 TouchBarController（避免新文件/新注册），与 BarItemFactory 的既有模式一致；如需更细粒度解耦，可后续随工厂迁移。

---

## 四、交付物清单

| 交付物 | 路径 |
|--------|------|
| 验证报告（本报告） | 仓库根 `验证报告_第16轮_技术债评估与落地.md` |
| TECHNICAL_DEBT.md 三条状态标注 | `LyricsMTMR/TECHNICAL_DEBT.md`（① ② 已评估-暂缓，③ 已落地） |
| 代码落地 | `LyricsMTMR/MTMR/Core/TouchBarController.swift`（+43/-19：shouldShowItem + 双路径接入） |
| 新增测试 | `LyricsMTMR/MTMRTests/BarItemVisibilityTests.swift`（11 用例，pbxproj 4 处注册 ID CA8F2B94/95 2FC7000000D189D9） |
| 迭代记录 | `iteration-log.md` 第 16 轮 / 子任务 B 追加 |
| 文档登记 | `LyricsMTMR/docs/file-structure.zh.md` 报告行 + mindmap 轮次 |

约束遵守：仅本工作区与 r16/techdebt 分支改动；未 push 远端（父任务收口统一推送）；未开新分支/新子任务/无 parents 依赖；不触发全量回归（129 用例分支实证已附）；新增测试文件 pbxproj 手工注册（add_files.py 锚点过期，第 15 轮遗留问题）。
