# 验证报告_第23轮_全局隐藏态注入与重建覆盖

- 轮次：第 23 轮（功能/优化迭代第 11 轮）子任务 A
- 分支：`r23/feature`（基于 main@8b15f98）
- 承接：第 22 轮 A 卡登记遗留 ② —— 隐藏期重建 item 的 init 单次 fetch 为配置变更事件请求，严格零请求需全局隐藏态注入 init（当时超出范围如实记录）
- 结论：**落地完成** —— 全局隐藏态记录/查询 + 重建覆盖 + init 单次 fetch 隐藏态注入，分支验证 **192 用例 0 失败 0 意外**（186 基线 + 新增 6，金丝雀锚点全绿）

## 一、实证（步骤①，问题成立）

第 18~22 轮「隐藏期零空转」治理主线已覆盖：Timer 16 项（第 19/20 轮）+ 自循环 2 项 + AudioSpectrum 采集链（第 21 轮）+ NSBackgroundActivityScheduler 类 4 widget 门控 + 定位服务暂停（第 22 轮）。但**重建路径**存在两类缺口：

1. **TouchBarController 无全局隐藏态查询接口**：`grep -rn isBarHidden` 实证全库零命中（本轮改动前）。`dismissTouchBar`→`setPollingPaused(true)`（TouchBarController.swift:762）只对现存 items/swipeItems 广播，**不落状态**；`presentTouchBar`→`setPollingPaused(false)` 同理。配置变更（refresh/slot switch/editor apply）→ `prepareTouchBar()` → `createItems()` 全量重建时，新 item 初始为「未暂停」：
   - `TBPollItem`/`TBMetricPopoverItem` 子类 init 即 `scheduleNextCycle()`（WidgetKit.swift:572/711）——首个 cycle（含初始 fetch）在 +interval（≥0.4s）后触发；
   - 4 个 scheduler widget（Currency/Weather/Yandex/UpNext）init 内**同步**调用 `updateCurrency()`/`updateWeather()`/`updateView()`（CurrencyBarItem.swift:115、WeatherBarItem.swift:123、YandexWeatherBarItem.swift:76、UpNextScrubberTouchBarItem.swift:79）——初始网络请求/EventKit 查询**在 init 内立即发出**，早于任何后续广播；
   - 组容器子 item（ExpandableCardItem:305 经 `createItem` 递归创建、GroupBarItem 浮层子 item）**不在 items/swipeItems 字典内**，广播体系根本到不了。
   - 若重建恰发生在 bar 隐藏期间（黑名单 app 激活 / exitTouchbar），新 item 的轮询/请求立即空转，直到下一次 present/dismiss 广播才被纠正——第 22 轮 A 卡已承认「初始 fetch 执行一次」为取舍，本卡收口。

2. **init 单次 fetch 无隐藏态感知**：scheduler widget 的 init fetch 是配置变更事件触发的单次请求，但严格零请求语义要求 init 本身可感知全局隐藏态（当时超范围）。

## 二、方案（步骤②）

三条正交改动，互不破坏既有 `TBPollPausable.setPaused` 广播协议：

### 1. 全局隐藏态注册表 `TouchBarVisibilityState`（TBPausableTimer.swift:86-118，新增）
- 进程级单例 `static let shared`，NSLock 保护读写（可被 background 队列读、主线程写）；
- **present/dismiss 驱动**：`TouchBarController.presentTouchBar` 落 `setBarHidden(false)`（:751）、`dismissTouchBar` 落 `setBarHidden(true)`（:764）——先落状态再广播，state 与 broadcast 同栈同步；
- **初始态 = 可见（false）**，语义处理：应用启动未 present 前 bar 尚不存在、无「隐藏」可继承，且 launch 路径（`reloadStandardConfig`→`updateActiveApp`）立即落定真实状态；取 false 保证与第 18~22 轮「新 item 默认未暂停」逐字节一致（基线 186 零改动实证）；
- 重复广播幂等：`setBarHidden` 重复写同值无害（注册表无变更检测需求——变更检测在消费侧 gate 上）。

### 2. 重建覆盖（controller 侧，TouchBarController.swift:709-719）
`createItems()` 重建循环结束后：`if TouchBarVisibilityState.shared.isBarHidden { setPollingPaused(true) }` —— 对全部新 item 立即应用暂停。语义上为**兜底**：已按全局态播种的 item 本就暂停（广播幂等 no-op），该 guard 保证任何未播种 item（未来新增 widget、组容器子 item 的父级）在任何调用路径下都被覆盖，不依赖「重建后必有 present/dismiss 广播」这一现行调用结构巧合。异步路径（`reloadPresetAsync`）结尾的 `presentTouchBarWithCurrentItems()` 已有 present/dismiss 广播收尾，两路径合拢。

### 3. init 隐藏态注入（item 侧）
- **TBPollItem / TBMetricPopoverItem**（WidgetKit.swift:575-582 / 762-769）：init 播种 `_isPaused = TouchBarVisibilityState.shared.isBarHidden`；若播种暂停则置 `_needsInitialRefresh = true`，首个 cycle 永不调度（初始 fetch 跳过）。`setPaused(false)`（恢复广播）时若 `_needsInitialRefresh` 走新增 `runImmediateCycle()`（:669-688）——**零延迟补刷**被跳过的初始 fetch（queue.async 立即执行，复用 loop() 全链路：compute→apply→scheduleNextCycle 续正常节奏），否则走原 `scheduleNextCycle()` 逐字节不变。补刷 hop 内复查 flags 且**仅在实际运行时消费 `_needsInitialRefresh`**——快速 resume→repause 的陈旧补刷被丢弃、后续 resume 重试（同 round-21/22 主线程 hop + 状态复查模式）。
- **4 个 scheduler widget**：`pollGate`/`locationPauseGate` 改 `TBPauseGate(startPaused: TouchBarVisibilityState.shared.isBarHidden)`（TBPauseGate 新增 `init(startPaused: Bool = false)`，TBPausableTimer.swift:48-52，默认 false 兼容全部既有构造点）——隐藏期重建时 gate 初始即暂停，init 内同步 fetch 被各更新方法顶部 `guard !pollGate.isPaused` 拦截（零网络/零 EventKit 查询），恢复广播 `setPaused(false)` 走既有「gate 变更→主线程 hop→立即补刷」路径；Weather/Yandex 的 `startLocationUpdates()` 另加 `if !locationPauseGate.isPaused` 守卫——隐藏期重建**不启动定位**（GPS 不亮、隐私指示灯不亮），恢复广播统一重启 + 补刷。

## 三、变更明细

| 文件 | 变更 |
|---|---|
| `LyricsMTMR/MTMR/Widgets/TBPausableTimer.swift` | 新增 `TouchBarVisibilityState`（NSLock 单例，默认可见）；`TBPauseGate` 新增 `init(startPaused: Bool = false)` |
| `LyricsMTMR/MTMR/Core/TouchBarController.swift` | `presentTouchBar` 落隐藏态 false；`dismissTouchBar` 落隐藏态 true；`createItems()` 尾部重建覆盖 guard（隐藏态→`setPollingPaused(true)`） |
| `LyricsMTMR/MTMR/Widgets/WidgetKit.swift` | `TBPollItem`/`TBMetricPopoverItem`：init 播种隐藏态 + `_needsInitialRefresh`；`setPaused` 恢复时按需走 `runImmediateCycle()`（新增，零延迟补刷） |
| `LyricsMTMR/MTMR/Widgets/Life/CurrencyBarItem.swift` | `pollGate` 播种全局隐藏态 |
| `LyricsMTMR/MTMR/Widgets/Life/WeatherBarItem.swift` | `pollGate`/`locationPauseGate` 播种；init `startLocationUpdates` 加 gate 守卫 |
| `LyricsMTMR/MTMR/Widgets/Life/YandexWeatherBarItem.swift` | 同 WeatherBarItem |
| `LyricsMTMR/MTMR/Widgets/Media/UpNextScrubberTouchBarItem.swift` | `pollGate` 播种全局隐藏态 |
| `LyricsMTMR/MTMRTests/GlobalHiddenStateTests.swift`（新增，add_files.py 注册 4 落点） | 第 23 轮单测 6 用例 |

## 四、等价性论证

- **可见期（未隐藏）逐字节等价**：初始态 false → init 播种为 no-op；`createItems` guard 不触发；`runImmediateCycle` 仅在 `_needsInitialRefresh`（=隐藏态播种）时启用，正常 pause/resume 走原 `scheduleNextCycle()`；
- **广播协议不破坏**：`setPaused(true)` 对已播种暂停 item 为幂等 no-op；重复 present 广播（`changed=false`）零副作用；`TBPauseGate` 新 init 参数默认 false，其余 ~30 处构造点零改动；
- **隐藏期行为差异（全部为严格改进）**：① 隐藏中重建 → 新 item 初始即暂停，init fetch 零请求（scheduler widget）/ 首个 cycle 零触发（TBPoll 家族，含组容器子 item——广播体系此前到不了它们）；② 恢复 → 被跳过的初始 fetch 零延迟补刷一次（此前 scheduler widget 为同栈广播后补刷、TBPoll 家族为 +interval 后补刷；本次统一为立即），随后正常节奏；③ 隐藏中重建 → 天气类不启动定位（GPS/隐私灯不亮）。
- **异步路径**（reloadPresetAsync）：item 在 main 块内创建时按当时全局态播种；块尾 `presentTouchBarWithCurrentItems` 的 present/dismiss 广播与播种叠加，双向收敛，无中间窗口（创建与广播同栈同步）。

## 五、单测清单（MTMRTests/GlobalHiddenStateTests.swift，新增 6 用例，沿用既有 Round N 文件模式 + add_files.py 注册）

| 用例 | 覆盖 |
|---|---|
| `testVisibilityStateDefaultsVisibleAndTracksFlips` | 初始态可见 / dismiss 翻转 true / present 翻转 false / 重复写幂等 |
| `testPollItemCreatedWhileHiddenSkipsInitialFetchAndResumeCatchUp` | 隐藏中重建 TBPollItem：≥3 interval 零 compute（初始 fetch 跳过）；恢复→立即补刷 1 次 + 正常节奏续跑（0.4s interval 达 2 次） |
| `testMetricPopoverItemCreatedWhileHiddenSkipsInitialFetch` | 同上（TBMetricPopoverItem 同构） |
| `testPollItemCreatedWhileVisibleStartsPollingNormally` | 可见期重建不受影响（正常起跑） |
| `testSchedulerWidgetsCreatedWhileHiddenFirstResumeCatchUp` | 隐藏中重建 Currency/Weather/UpNext：UpNext 真实 updateView guard 可观测跳过 init 加载（queryCount=0，Currency/Weather 计数双打绕过 base guard 记 1——如实注释）；隐藏期 pollTick 全门控；恢复后各 catch-up 恰 1 次、无多余 |
| `testHiddenInitItemResumeBroadcastIdempotentAndRapidPauseDropsStaleCatchUp` | 恢复广播幂等（重复 setPaused(false) 不双补刷）；快速 resume→repause 陈旧补刷丢弃（≤1 次，不双算）；dismiss 再停轮询 |

说明：`TouchBarController` init 为 private 单例（测试不可实例化），`createItems` 尾部 guard 的**可观测语义**（对新 item 广播 setPaused(true)）与 item 侧播种/广播幂等测试等价，controller 侧 3 行 guard 经代码审查 + item 级等价测试覆盖（报告中如实记录该覆盖边界）。

## 六、风险点 / 遗留

- **无新增遗留**。登记口径更新：第 22 轮遗留 ②（隐藏期重建 init fetch）本轮闭环；真机冒烟延续挂账（隐藏期零请求/恢复补刷观感、隐私指示灯熄灭/恢复观感，第 8/17/18/19/20/21/22 轮同口径，依赖 Touch Bar 真机）。
- 环境说明：本机无 Mac Development 证书，项目惯例以 Debug + CODE_SIGNING_ALLOWED=NO 为准（第 21/22 轮同口径）。
- 本轮不触发全量回归（第 22 轮收口整体 186 实证，隔代规则预计第 24 轮分解前触发，届时基线 192）。

## 七、分支验证

- `xcodebuild build`（MTMR, Debug, CODE_SIGNING_ALLOWED=NO，独立 derivedDataPath `/tmp/LyricsMTMR-dd-r23a-build`）**BUILD SUCCEEDED**
- `xcodebuild test`（UnitTests, Debug，`/tmp/LyricsMTMR-dd-r23a-test3`）**TEST SUCCEEDED —— 192 用例 0 失败 0 意外**（186 基线 + 新增 6 全过；金丝雀锚点 testGoldenAnchors2026/2027/Makeup2026 全绿；WidgetLeakTests 8 用例全绿无新泄漏；GlobalHiddenStateTests 6 用例全绿）
- 完成自查：git status 干净 + commit 已提交（第 14 轮 B 卡漏提交教训）；未 push 远端（父任务收口统一推送）；未开新分支/新子任务/无 parents 依赖。
