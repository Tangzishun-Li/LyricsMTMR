# 验证报告_第18轮_黑名单隐藏暂停轮询

- 轮次：第 18 轮（功能/优化迭代第 6 轮）· 子任务 B（实现/优化维度）
- 任务卡：t_ebbd96e5（分支 r18/optimize，工作区 `.worktrees/round18-B`，基于 main@7690ac7）
- 落地目标：黑名单/exitTouchbar 隐藏期间暂停 widget 轮询（TBPollItem / TBMetricPopoverItem 空转浪费）
- 日期：2026-08-12

---

## 〇、结论速览

| 项 | 结论 |
|----|------|
| 问题 | 整条 Touch Bar 隐藏（黑名单 app 命中 / exitTouchbar 按钮 / resetControlStrip）只调 `dismissTouchBar()` 隐藏 UI，**不销毁 items**——controller 的 `items` 字典仍强持有全部 item，TBPollItem/TBMetricPopoverItem 的轮询循环（每 interval ≥ 0.4s 一次 `compute()`，可能含网络请求/进程调用/文件 IO）在隐藏期间持续空转，纯浪费 |
| 方案 | TBPollItem/TBMetricPopoverItem 增加线程安全 pause/resume（标志位在循环调度处检查，暂停时既不 compute 也不调度下一 cycle，恢复时重新调度恰好一次）；TouchBarController 在 `dismissTouchBar()` 广播暂停、`presentTouchBar()` 广播恢复（经新协议 `TBPollPausable`） |
| 等价性 | 可见行为严格等价：新建 item 默认未暂停，与改动前完全一致；暂停只发生在整条 bar 隐藏期间（此前轮询对用户不可见），恢复后循环从下一个 interval 继续；`_isCancelled` 取消语义不变（仅访问改为锁保护，顺带消除原有 data race） |
| 实证 | 分支内 xcodebuild build **BUILD SUCCEEDED** + test **TEST SUCCEEDED —— 139 用例 0 失败 0 意外**（134 基线 + 新增 5 单测全过，金丝雀锚点全绿，WidgetLeakTests 4 用例仍绿无新泄漏） |

---

## 一、实证结论（步骤①：问题是否成立）

逐条阅读 `TouchBarController.swift` 与 `WidgetKit.swift` 的持有关系与生命周期后确认：**黑名单隐藏期间轮询确实继续，问题成立**。

| # | 实证点 | 源码证据 | 结论 |
|---|--------|----------|------|
| 1 | `items` 的持有关系 | `var items: [NSTouchBarItem.Identifier: NSTouchBarItem]`（TouchBarController.swift:238）为 controller 强持有字典；TBPollItem/TBMetricPopoverItem 实例创建后存入其中（createItems :701 / reloadPresetAsync :931） | items 对象随 controller 存活 |
| 2 | `dismissTouchBar()` 做什么 | :745-750 仅 `minimizeSystemModal(touchBar)` + `updateControlStripPresence()`，**不动 items** | 隐藏 UI 不销毁 items |
| 3 | `presentTouchBarWithCurrentItems()` 是否重建 items | :755-786 仅重建 basicView 布局（leftItems/scrollArea/rightItems 都从现有 `items` 字典取），不调 createItems | 不重建，items 复用 |
| 4 | 谁真正重建 items | `createItems()`（:671-709，先 `discardCurrentItems()` + `items = [:]`）仅在 `prepareTouchBar()`（:420-478）内调用；而 `prepareTouchBar()` 只在 app **切换**（updateActiveApp :538/:544 非 fast-path 分支）或主题切换时触发 | 同 app 恢复/黑名单路径不重建 |
| 5 | 黑名单路径 | updateActiveApp :495-496 `blacklistAppIdentifiers.contains(appId) → dismissTouchBar()` | 只隐藏，轮询继续 |
| 6 | 同 app 恢复 fast path | :534-537 `!appDidChange && touchBarIsBuilt() → presentTouchBar(); return` | items 原样复用，恢复显示 |
| 7 | exitTouchbar 自定义 item | :337 closure → `dismissTouchBar()` | 同黑名单路径 |
| 8 | 轮询循环自身 | TBPollItem :573-601 / TBMetricPopoverItem :649-674：`queue.asyncAfter` 自调度，仅 `deinit`（_isCancelled=true）停止；item 存活期间每 interval 执行一次 `compute()`（子类含 TBNet.get 网络请求、Process 进程调用、文件 IO 等） | 隐藏期间空转确认 |

**结论**：用户停留在黑名单 app（或点了 exitTouchbar）期间——可能持续数小时——所有 TBPollItem/TBMetricPopoverItem 子类（34 个 TBPollItem 子类 + 4 个 TBMetricPopoverItem 子类）继续以各自 interval 执行 compute()，网络/进程/IO 全部浪费。

## 二、方案设计（步骤②）

### 2.1 item 侧：暂停/恢复标志（在循环调度处检查）

两个基类各自实现（与现有「两处独立 loop」的代码风格一致），语义相同：

- **标志位**（NSLock 保护，全部访问走锁）：`_isPaused`、`_isCancelled`、`_cycleScheduled`。
  - 循环跑在自定义串行 queue 上，而 pause/resume 来自主线程（TouchBarController.dismiss/present 均在主线程），deinit 可在任意线程——三者并发访问同一组标志，锁是必须的（任务书明确要求「标志位访问需同步」；顺带消除了原代码 `_isCancelled` 裸读写的 data race）。
- **暂停语义**：cycle 开头检查 `isPaused` → 跳过 compute 直接 return（不调度下一个）；cycle 执行中被 pause → compute 已完成但 `scheduleNextCycle()` 检查 `_isPaused` → 不调度。**至多完成一次在途 compute，然后彻底停止**（连 asyncAfter 调度都不再发生，零空转）。
- **恢复语义**：`setPaused(false)` 时若状态有变化则调用 `scheduleNextCycle()` 重启循环。
- **防双循环**：`_cycleScheduled` 标志保证任意时刻至多一个在途 cycle。竞态场景（pause 已通过检查的 cycle 正在调度 → resume 又调度）下，第二个调度看到 `_cycleScheduled == true` 直接跳过——**绝无双 compute 循环**。重复 pause/resume 幂等（状态无变化不触发调度）。

### 2.2 controller 侧：dismiss/present 广播

- 新协议 `TBPollPausable`（WidgetKit.swift，两个基类均 conform）暴露 `setPaused(_:)`。
- `dismissTouchBar()` 末尾广播 `setPollingPaused(true)`；`presentTouchBar()` 末尾广播 `setPollingPaused(false)`。
- `setPollingPaused(_:)` 遍历 `items` 与 `swipeItems` 中所有 `TBPollPausable` item 广播（items 为空时无操作，无害）。
- 覆盖路径：黑名单命中（:495-496）、exitTouchbar（:337）、`presentTouchBarWithCurrentItems` 的黑名单兜底（:781-782，经 dismiss/present 间接广播）、同 app 恢复 fast path（:534-537，经 presentTouchBar 恢复）、app 切换重建（新 items 默认未暂停，present 广播幂等）、resetControlStrip（dismiss + updateActiveApp）。
- 新建 item 默认未暂停 → 首次出现即开始轮询，与改动前一致；overlay 展开（showOverlay → presentSystemModal）不经过 dismiss/present，行为不受影响。

## 三、变更明细

| 文件 | 变更 |
|------|------|
| `LyricsMTMR/MTMR/Widgets/WidgetKit.swift` | ① 新增协议 `TBPollPausable`；② `TBPollItem` 增加 `flagLock`/`_isPaused`/`_cycleScheduled` + `setPaused(_:)`/`scheduleNextCycle()`，loop 开头暂停检查，调度改走 `scheduleNextCycle()`，`_isCancelled` 访问改锁保护；③ `TBMetricPopoverItem` 同构改造（保持其独立 loop 风格） |
| `LyricsMTMR/MTMR/Core/TouchBarController.swift` | `dismissTouchBar()` 广播暂停、`presentTouchBar()` 广播恢复；新增私有 `setPollingPaused(_:)`（遍历 items/swipeItems 按 `TBPollPausable` 广播） |
| `LyricsMTMR/MTMRTests/PollingPauseTests.swift` | 新增 5 单测（`Scripts/add_files.py Tests:` 一键注册，pbxproj 4 处落点：C1FE/C1FF 前缀 + MTMRTests 分组 + 单测目标 phase） |

## 四、等价性论证

| 场景 | 改动前 | 改动后 | 等价性 |
|------|--------|--------|--------|
| item 新建（bar 显示） | init 即循环，首 cycle 在 1×interval 后 | 同（scheduleNextCycle 首次调度，interval 不变） | ✅ |
| bar 正常显示期间 | 每 interval compute+apply | 同（未暂停，调度路径等价——asyncAfter 一次、间隔同 interval） | ✅ 逐字节等价 |
| 黑名单/exitTouchbar 隐藏 | 每 interval 继续 compute（不可见浪费） | 暂停：不 compute 不调度 | 行为改进（原为空转） |
| 恢复显示 | 循环照旧 | setPaused(false) 后从下一 interval 恢复 | ✅ 恢复后节奏一致 |
| 主题切换重建 items | discardCurrentItems + 释放旧 items（deinit 置 cancelled） | 同（cancelled 语义不变，仅访问走锁） | ✅ |
| deinit | 不再调度（weak self + cancelled） | 同（weak self 不变 + cancelled 锁保护） | ✅ |
| compute 抛错 | MTMRTryOrError 捕获，显示 ⚠️，循环继续 | 同（compute 调用方式未改） | ✅ |
| overlay 展开 | 轮询继续（可见状态，合理） | 同（不经 dismiss/present） | ✅ |
| 无效参数/宽高/标识 | 不变 | 不变（init 签名未动） | ✅ |

**行为差异仅一处**：整条 bar 隐藏期间轮询暂停（正是本次优化目标）；无任何可见行为回退。

## 五、单测清单（步骤③）

文件 `LyricsMTMR/MTMRTests/PollingPauseTests.swift`（add_files.py Tests: 注册，pbxproj 4 处落点实证），最小 interval 0.4s + 短等待控制时长（5 用例合计 ~11.6s）：

| 用例 | 验证点 | 耗时 |
|------|--------|------|
| testPollItemPauseStopsComputeAndResumeRestarts | TBPollItem：pause 后等 ≥2 interval compute 数不增长；resume 后恢复增长 | 2.23s |
| testMetricPopoverItemPauseStopsCompute | TBMetricPopoverItem：同语义（两类基类都覆盖） | 2.23s |
| testPauseBeforeFirstCycleNeverComputes | init 后立即 pause（首 cycle 前）：等 3 interval 从未 compute（0 次）；resume 后启动 | 1.67s |
| testRapidPauseResumeDoesNotDoubleSchedule | 5 次快速 pause/resume 后仍暂停（无在途 cycle 泄漏）；resume 后首次 compute 延迟 ≥0.3s（≈1 interval，双调度会立即触发）+ 之后节奏不翻倍（1.3s 内 ≤4 次） | 3.84s |
| testDeinitStopsScheduling | 释放后 weak 引用为 nil（无泄漏）；等 1.2s 仍 nil（在途 cycle 不会复活已释放对象） | 1.67s |

## 六、验证记录

- xcodebuild build（MTMR, Debug, CODE_SIGNING_ALLOWED=NO，独立 derivedDataPath /tmp/LyricsMTMR-dd-r18b）：**BUILD SUCCEEDED**。
- xcodebuild test（UnitTests, Debug，同 derivedDataPath）：**TEST SUCCEEDED —— 139 用例 0 失败 0 意外**（134 基线 + 5 新增全过；金丝雀锚点 testGoldenAnchors2026/2027/Makeup2026 全绿；WidgetLeakTests 4 用例仍绿——未引入新泄漏）。
- 本轮不触发全量回归（父任务已实证 main@7690ac7 134 用例 0 失败）。

## 七、风险点

1. **时序单测 flaky 风险**：pause/resume 断言依赖真实时钟（0.4s interval）。已用「先等 0.3s 让在途 cycle 落地再快照」「等待窗口 ≥2 interval」等手法留足余量；CI 机器负载极端时理论上有超时风险（waitUntil 兜底 2s 超时 + 最终重查一次）。
2. **双循环竞态**：`_cycleScheduled` 单在途标志已从设计上排除；testRapidPauseResumeDoesNotDoubleSchedule 专门覆盖。
3. **deinit 与锁**：deinit 在最后一个强引用释放时发生；在途 cycle 的 `[weak self]` 不会阻止释放，释放后锁访问不再发生（closure 已因 nil 退出）。无死锁路径（锁从不嵌套持有）。
4. **真机行为**：广播依赖 dismiss/present 路径覆盖完整（已枚举 7 类路径）；无 Touch Bar 真机冒烟，隐藏/恢复的视觉效果留待用户验证。
