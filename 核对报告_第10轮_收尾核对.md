# 核对报告：第10轮 · 收尾核对 — 遗留问题清单复核 + D1 注释陈旧项处置

> 核对日期：2026-08-12。执行：merge-agent（子任务 B，分支 r10/check，基于 main@487415e）。
> 性质：第 10 轮（纯维护轮）收尾核对——遗留 6 项逐一复核 + GitHub 状态复核 +
> 归档一致性抽查 + D1 处置（本轮唯一允许的代码改动，已修正并附构建冒烟证据）。

---

## 一、结论速览

1. **GitHub 状态复核：4/4 符合预期。** issue #1 OPEN + 1 条评论（= 第 8 轮回复，无新动态）；
   issue #40 OPEN（backlog）；open PR 0；origin/main = 487415e（487415ee1c0295d0f9eda948600b5a1142e16e80）。
2. **「2d2d681 文档并入 main 已闭环」属实**：`git merge-base --is-ancestor 2d2d681 main` = YES，
   `git log --oneline main | grep -c 2d2d681` = 1，提交在 main 历史中。
3. **遗留 6 项全部复核**：① issue #1 待用户真机验证（继续跟踪）；② ITER-15 待用户 4 问（继续跟踪）；
   ③ ITER-14 第 4 次核验由子任务 A 覆盖（已确认其卡存在 t_216edb49 running）；④ README 补 MediaRemote
   说明仍为建议状态（未实施，继续跟踪）；⑤ D1 本轮已修正（见第四节）；⑥ 内存修复真机冒烟 3 项
   挂账（待用户真机执行，继续跟踪）。
4. **归档一致性抽查：1 处漂移已就地修正。** file-structure.zh.md 目录树缺登记
   `回归报告_第9轮_t_d0232788.md`（第 9 轮预登记 3 份时漏了回归报告），已补登记。
   backup/ 17 份、docs/ 5 文件、根目录 9 份报告与 backup/ 无重复，均与登记一致。
5. **D1 处置：修正注释（代码改动）**，`UnifiedSettingsWindowController.swift:113-116`
   「默认 10 分钟」→「默认 1 小时（时长见 AppDelegate.settingsWindowIdleGCSeconds = 3600）」；
   xcodebuild build 冒烟 **BUILD SUCCEEDED** + make test **TEST SUCCEEDED（60 用例 0 失败）**。

---

## 二、GitHub 状态复核（gh CLI + git ls-remote，2026-08-12）

| 对象 | 预期 | 实测 | 结论 |
|---|---|---|---|
| issue #1「Why Apple why?」 | OPEN + 1 条评论=第 8 轮回复 | OPEN；1 条评论（Tangzishun-Li，内容为 macOS 15.4+ MediaRemote 权限收紧说明=第 8 轮回复），无新动态 | ✅ 符合 |
| issue #40「[Feature Request] Per-app bar switching」 | OPEN（backlog） | OPEN；0 评论 | ✅ 符合 |
| open PR | 0 | 0（gh pr list --state open 返回空） | ✅ 符合 |
| origin/main | 487415e | `git ls-remote origin main` = 487415ee1c0295d0f9eda948600b5a1142e16e80 | ✅ 符合 |
| 2d2d681 文档并入 main 已闭环 | 提交在 main 历史 | merge-base --is-ancestor = YES；main log 含 2d2d681；提交信息「fix(settings): 关窗即隐藏复用 + 闲置GC」 | ✅ 闭环属实 |

---

## 三、第 9 轮遗留问题 6 项逐一复核

| # | 遗留项（第 9 轮定义） | 本轮复核结果 | 状态分类 |
|---|---|---|---|
| ① | issue #1 保持 OPEN，待用户 macOS 15.7 真机验证后关闭 | OPEN + 1 条第 8 轮回复，无新动态；第 8 轮已承诺「验证后关闭」；backlog #40 已承接未实现项 | 🔄 待用户确认（真机验证） |
| ② | ITER-15 镜像窗事件驱动刷新：第一决策门=使用场景 4 问（是否常驻 / 用途 / 快照实时性 / 电量敏感度） | 评估报告（第 8 轮）§5.3 4 问清单在案；iteration-plan 维持「可选项」（:238/:393/:406），未实施未关闭 | 🔄 待用户确认（4 问答复） |
| ③ | ITER-14（2026-11 国办 2027 节假日通知核对）第 4 次核验 | 子任务 A（t_216edb49，review-agent，running）覆盖；本任务仅确认其存在 ✅ 已在板面确认 | ✅ 已闭环（由子任务 A 承接） |
| ④ | README/FAQ 补「macOS 15.4+ 音乐信息获取机制与已知风险」说明 | README.md 现状：无 FAQ 章节、无 15.4+ 风险段落（仅第 49 行功能列表提及 MediaRemote 集成）——建议仍未实施 | 🔄 继续跟踪（建议状态，待用户拍板） |
| ⑤ | D1 低危注释陈旧（10 分钟 vs 1 小时） | **本轮已处置：修正注释**（见第四节），非「留待下次」 | ✅ 已闭环（本轮修正） |
| ⑥ | 内存修复真机交互冒烟 3 项：① 连续开关 8+ 次内存不再 ~10MB/轮增长；② 隐藏 1h 或内存压力后整树释放回基线；③ 复用路径 Dock 图标显隐正确 | 内存修复报告「五、遗留与建议」3 项在案；无真机交互冒烟记录（本轮为维护轮，无真机执行条件） | 🔄 挂账（待用户真机执行） |

**分类汇总：已闭环 2 项（③、⑤）；待用户确认 2 项（①、②）；继续跟踪 2 项（④、⑥）。**

---

## 四、D1 处置：注释陈旧修正（本轮唯一代码改动）

### 4.1 问题复述

`LyricsMTMR/MTMR/Preferences/UnifiedSettingsWindowController.swift:113-114`（onWindowHidden
文档注释）：「侧挂一个闲置 GC（**默认 10 分钟**未再开则整体释放，内存回到基线）」，
而实际实现 `AppDelegate.swift:172` `static let settingsWindowIdleGCSeconds: TimeInterval = 3600`（**1 小时**），
且 `scheduleSettingsWindowGC()`（:190）`deadline: .now() + Self.settingsWindowIdleGCSeconds` 按 3600s 调度。
修复报告与提交信息均写 1h → 注释为唯一陈旧点，实现正确。

### 4.2 处置决策：修正注释（改代码文件）

理由：
1. 纯注释修正，零行为变化（改动仅 2 行注释文本），风险可忽略；
2. 该陈旧已连续两轮被记录（第 9 轮子任务 C 差异清单 D1 + 第 9 轮子任务 B 遗留观察③），
   且父任务将「D1 处置」列为第 10 轮子任务 B 的明确交付项，本轮具备处置授权；
3. 注释误导后续维护者（以为 10 分钟释放，实际 1 小时），属文档/代码一致性义务范畴。

### 4.3 修改内容（diff 摘要）

```diff
     /// 方案：关窗只 orderOut 隐藏（树保留 → 重开零重建、零增长）；AppDelegate
-    /// 侧挂一个闲置 GC（默认 10 分钟未再开则整体释放，内存回到基线），系统内存
+    /// 侧挂一个闲置 GC（默认 1 小时未再开则整体释放，内存回到基线；时长见
+    /// AppDelegate.settingsWindowIdleGCSeconds = 3600），系统内存
     /// 压力时立即释放。隐藏期间 Deck 动画已被 OPT-2 的 isVisible 暂停，不会出现
     /// 优化前的幽灵渲染问题。
```

### 4.4 构建冒烟证据（独立 derivedDataPath）

| 项 | 命令 | 结果 |
|---|---|---|
| 构建冒烟 | `xcodebuild build -project LyricsMTMR.xcodeproj -scheme MTMR -configuration Debug -derivedDataPath /tmp/r10b-derived` | **BUILD SUCCEEDED** |
| 回归佐证 | `make test`（UnitTests, Debug，worktree 内 .build/DerivedData） | **TEST SUCCEEDED**：Executed 60 tests, 0 failures (0 unexpected)，8 套件全绿 |

注：xcodebuild build 已含编译阶段，注释改动仅影响文档注释节点，不产生新告警。

---

## 五、归档状态核对（与子任务 A 交叉验证，独立执行）

| 核对项 | 登记声明 | 实测 | 结论 |
|---|---|---|---|
| backup/ 计数 | 17 份（backup-note.md + file-structure.zh.md） | `ls backup/` = 17 个 .md | ✅ 一致 |
| backup/ 清单 | pre-opt-20260812-0114 存档点文件集 | 17 个文件与第 8 轮收尾记录吻合（含 优化计划_OPT任务清单.md） | ✅ 一致 |
| docs/ 文件 | 5 个：backup-note / iteration-plan / maintenance-notes / optimization-plan / memory-rendering-audit | `ls docs/` = 5 个同名文件 | ✅ 一致 |
| 根目录报告清单 | file-structure.zh.md 目录树登记 8 份（第 7~9 轮） | 根目录实际 9 份报告 | ⚠️ 漂移 1 处：**回归报告_第9轮_t_d0232788.md 未登记**（第 9 轮预登记 3 份时漏掉回归报告；已就地补登记） |
| 根目录 vs backup/ 重复 | 第 8 轮已清理 | 逐文件比对：无同名重复（backup/ 每份均无根目录同名文件） | ✅ 无重复 |
| iteration-log.md | 第 9 轮父记录 + A/B/C 子记录并列 | 文件尾部含第 9 轮完整记录（父 + 3 子任务） | ✅ 一致 |

**漂移处置**：file-structure.zh.md 目录树补一行登记回归报告_第9轮（纯文档，与历轮核验子任务就地修正惯例一致）。

---

## 六、交付与变更清单

| 项 | 内容 | 位置 |
|---|---|---|
| 核对报告（本文件） | 第 10 轮收尾核对报告 | 本分支根目录 `核对报告_第10轮_收尾核对.md` |
| D1 注释修正 | UnifiedSettingsWindowController.swift:113-116（10 分钟→1 小时 + 指向常量） | `LyricsMTMR/MTMR/Preferences/UnifiedSettingsWindowController.swift` |
| file-structure 漂移修正 | 目录树补登记 回归报告_第9轮_t_d0232788.md | `LyricsMTMR/docs/file-structure.zh.md` |
| iteration-log 追加 | 第 10 轮 / 子任务 B 记录（见 iteration-log.md 尾部） | `iteration-log.md` |
| 构建冒烟记录 | BUILD SUCCEEDED + TEST SUCCEEDED（60/60） | 见第四节 |

**分支状态**：r10/check 基于 main@487415e，以上改动未 push（父任务收口统一合并，与历轮协议一致）。

---

## 七、风险点与需配合项

- **需用户配合（挂账 4 项）**：① issue #1 在 macOS 15.7 真机验证后关闭；② ITER-15 使用场景
  4 问答复（决定「事件驱动+轮询兜底」是否实施）；④ README 补 MediaRemote 风险说明拍板；
  ⑥ 内存修复真机交互冒烟 3 项执行。
- **风险（低）**：D1 注释修正为纯文档性改动，构建+测试双通过，无行为风险；file-structure
  补登记与子任务 A 的文档一致性核验可能交叉（各自独立执行，父任务合并时若有冲突统一解决）。
- **无阻塞项**：本任务无 parents 依赖，全部交付物已在分支上就绪。
