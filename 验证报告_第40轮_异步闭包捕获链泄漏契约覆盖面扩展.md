# 验证报告_第40轮_异步闭包捕获链泄漏契约覆盖面扩展

- 轮次：第 40 轮（功能/优化迭代第 28 轮）/ 子任务 A
- 分支：r40/leak-closures（基于 main@4afbbe6，未 push）
- 任务：异步闭包捕获链泄漏契约覆盖面扩展 — 非 timer/observer 的 block 捕获防回归网（内存修复主线续篇三）
- 执行人：default（实现/优化）

## 一、审计范围与方法（自选）

全仓 grep 实证（URLSession/NSURLSession dataTask completion、DispatchQueue.async/asyncAfter、DispatchWorkItem、perform(after:)、CoreAudio block listener、系统级 block 回调注册）：

**审计结论：活代码 27 处 URLSession dataTask completion + 181 处 DispatchQueue.async（自捕获子集逐点核查）+ 39 处 asyncAfter + 34 处 DispatchWorkItem + 2 处 CoreAudio property-listener block；perform(after:) 与 NSURLSession 全仓 0 处。** 按对象生命周期与捕获强度分类（与前两轮同口径：单例生命周期=app 豁免、无对象捕获豁免、widget/窗口级需契约）：

| # | 类别 | 处数 | 判定 |
|---|------|------|------|
| 1 | URLSession completion 弱捕获（widget 级） | 3 | ✅ 在位（YandexWeatherBarItem:135 / CurrencyBarItem:176 / LyricsTranslateBarItem:120） |
| 2 | URLSession completion 强捕获（widget 级） | 1 | ❌→✅ **WeatherBarItem.swift:178 本轮修复**（陈旧回调风险） |
| 3 | URLSession completion 零 self 捕获（闭包仅用局部/参数） | 17 | 豁免（SecretsManager×8 单例+零 self、UsageBarItem×3、StockBarItem×3、ChinaWeatherProvider×2、OpenCodeGo:209、Deepseek:78、ApiLatency:83、ApiTester:86、WidgetKit:887/915 TBNet 静态、PackageTracker:85 信号量同步） |
| 4 | DispatchQueue.async 弱捕获（widget/窗口级） | ~30 | ✅ 在位（GitStatus×8、TBPausableTimer×5、BluetoothToggle×4、YandexWeather×3、AudioSpectrum×3、TouchBarController×3 单例、Currency×2、Weather×2、HomekitScene×2、WidgetKit×2、其余单点） |
| 5 | DispatchQueue.async 外层弱内层强（guard let self 解包，单跳有界） | 8 | 豁免（解包后主队列单跳即释放；OpenCodeGo:534/562、Deepseek:56、Currency:186、MusicBarItem:234、AudioSpectrum:206/308、WeatherLocationSession:169、LyricsTranslate:121、StockBarItem:46、WeatherBarItem:201/235/241） |
| 6 | DispatchQueue.async 一次性/阻塞执行有界（执行期持有 self 属必要） | 6 | 豁免（AppleScriptTouchBarItem init 链:22/:24/:30/:39 + refresh:66/:77、ShellScript:35、SwipeItem:48、MusicBarItem:332；队列串行、执行完即释放） |
| 7 | 单例豁免（app 生命周期） | 3 文件 | 豁免（TouchBarController:939、LyricsEngine:558/:571、MediaRemoteAdapter:70/:169/:175 属单例持有） |
| 8 | SwiftUI 值类型（struct 捕获=值拷贝零保留） | 16 处文件 | 豁免（全部 Preferences TabView saveDebounced/asyncAfter、DockTabView、RSSTabView:426、ServicesTabView×3、LyricsTabView:649/:691、ExpenseTabView:231、AITabView:237） |
| 9 | asyncAfter 弱捕获（自循环 hop） | 13 | ✅ 在位（AppleScript:70、ShellScript:94/:112、LyricsEngine:548/:1094、OpenCodeGo:723、WidgetKit:645/:810、ExpandableCard:106、DailyQuote:93、CPUBarItem:72、AiSelectedText:122、LyricsTranslate:200、MusicBarItem:236、AppDelegate:199） |
| 10 | DispatchWorkItem 实例存储 + 弱闭包 | 3 | ✅ 在位（AppDelegate:183 settingsWindowGCWorkItem、RibbonEditorView:777 liveSyncWorkItem、LyricsEngine:1090 lineCheckTimer） |
| 11 | DispatchWorkItem 静态存储/值类型 | 12 | 豁免（各 TabView saveWork 为 struct 值捕获 + 0.5s 一次性 debounce） |
| 12 | **CoreAudio block listener（系统级 block 回调注册）** | **2 处/1 类** | ❌→✅ **VolumeViewController.swift:37/:57 真实泄漏，本轮修复**（weak 闭包 + deinit 成对移除 + block 恒等存储） |

archive/ 死代码不参与编译不计入；测试/报告文件中的引用不计入活代码。KVO/perform(after:)/NSURLSession 全仓 0 命中。

## 二、真实泄漏发现与修复（红→绿实证）

**发现 1：VolumeViewController 的 CoreAudio property-listener block 强捕获 self + 永不移除（真实永久泄漏）**

- 根因：init 中 `AudioObjectAddPropertyListenerBlock(audioId, ..., audioRouteChanged)` 与 `AudioObjectAddPropertyListenerBlock(defaultDeviceID, ..., audioObjectPropertyListenerBlock)` 直接传实例方法引用——方法引用默认**强捕获 self**；block 注册进 CoreAudio（kAudioObjectSystemObject 系统对象 + 默认输出设备，进程生命周期持有），而 `deinit` 只做 `sliderItem.unbind`，**从不移除监听** → 每个构造实例被系统对象永久保留，bar 每次重建（config 变更/主题切换等频繁触发 reloadStandardConfig）均累积一份残留 = 进程生命周期泄漏。
- 修复：① 两个监听改为 `{ [weak self] ... }` 弱闭包（第 8/38/39 轮既定模式）；② block 存属性（routeChangeBlock/volumeChangeBlock）保证 add/remove 恒等（`AudioObjectRemovePropertyListenerBlock` 要求同一 block 对象）；③ `deinit` 成对移除两个监听（`removeAudioRouteChangedListener()` + `removeLastAudioVolumeChangeListener()`），修复后 deinit 可实际执行、无陈旧回调窗口。
- 红绿双跑实证（未放宽任何断言）：
  1. **RED**：保留原代码 + 仅跑 3 个新测试 → `testVolumeViewControllerDoesNotLeak` **FAILED**（XCTAssertNil 捕获存活实例 0x600000b800e0；Executed 3 tests, with 1 failure；独立 derivedDataPath /tmp/LyricsMTMR-dd-r40a-red）；
  2. **GREEN**：应用修复 → 3 个新测试 **Executed 3 tests, with 0 failures**（独立 derivedDataPath /tmp/LyricsMTMR-dd-r40a-green）。

**发现 2：WeatherBarItem:178 的 URLSession dataTask completion 强捕获 self（陈旧回调风险，非永久环）**

- 根因：`URLSession.shared.dataTask(with: urlRequest) { data, _, error in ... self.setWeather(...) }` 无 `[weak self]`——widget 被丢弃后 completion 仍持有 self 直至请求结束（慢网/悬挂可达分钟级），且到期仍执行 `self.setWeather`（对已丢弃 widget 的陈旧回调）。第 22 轮测试注释声称这些 widget 的初始网络请求均为 weak-self completions，本处与 CurrencyBarItem/YandexWeatherBarItem 不同、是漏网强捕获。
- 修复：`[weak self]` + `guard let self = self` 弱闭包（有界保留消除，行为不变）。
- 红绿说明：该路径在测试条件下（location==nil）不触发网络请求（构造零副作用策略），无注入点可确定性钉住请求在途状态，故不新增独立测试——构造路径 dealloc 由既有 testWeatherBarItemDoesNotLeak 钉住；修复以代码审计论证登记（不虚构红绿）。

## 三、用例清单（新增 3 用例）

全部追加于现有文件 MTMRTests/WidgetLeakTests.swift（hosted TEST_HOST 运行，同一文件追加无需 add_files.py 注册），沿用 autoreleasepool + weak var + letRunLoopSpin + XCTAssertNil 模式：

| # | 测试函数 | 构造方式（最小参数 + 无副作用策略） | 钉住的契约 |
|---|---------|----------------------------------|-----------|
| 1 | testVolumeViewControllerDoesNotLeak | VolumeViewController(identifier:)（仅注册 CoreAudio 监听 + 读音量属性——零权限弹窗零硬件激活） | **修复点**：CoreAudio block listener 弱捕获 + deinit 移除（红→绿实证） |
| 2 | testShellScriptTouchBarItemDoesNotLeak | NoExecShellScriptItem(identifier:source: EmptySource(), interval: 3600)（execute() 覆写为空——零进程 spawn） | asyncAfter 自循环 hop 弱捕获（interval 取 3600，若 hop 改强捕获测试即红） |
| 3 | testAppleScriptTouchBarItemDoesNotLeak | NoExecAppleScriptItem(identifier:source: EmptySource(appleScript: NSAppleScript(source:"return 1")), interval: 3600, alternativeImages: [:])（仅编译不执行——零 Apple Events/自动化弹窗） | 同上（appleScript 非 nil 走编译+自循环路径，execute() 覆写为空） |

## 四、实证表（443 用例逐套件）

命令：`caffeinate -i xcodebuild test -project LyricsMTMR.xcodeproj -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r40a-test`（先清理旧 /tmp/LyricsMTMR-dd-*；独立 derivedDataPath 防并行构建冲突；caffeinate 防显示器休眠——第 28 轮 CoreDisplay 教训；红/绿双跑另用 -r40a-red / -r40a-green 独立路径）。

| 套件 | 结果 |
|------|------|
| 全量（All tests） | **TEST SUCCEEDED — Executed 443 tests, with 0 failures (0 unexpected)** |
| WidgetLeakTests | Executed 30 tests, 0 failures（27 基线 + 3 新增） |
| RED（原代码 + 3 新测试） | Executed 3 tests, **with 1 failure**（VolumeViewController 泄漏捕获） |
| GREEN（修复 + 3 新测试） | Executed 3 tests, with 0 failures |

任务预算：440 基线 + 新增 3 = **443**，实测 443 —— **零偏差**。

## 五、文档同步表

| 文件 | 改动 |
|------|------|
| MTMR/Widgets/System/VolumeViewController.swift | 泄漏修复 1 处（2 个 CoreAudio property-listener block 方法引用强捕获 self → 弱闭包 + block 恒等存储 + deinit 成对移除） |
| MTMR/Widgets/Life/WeatherBarItem.swift | 陈旧回调强捕获修复 1 处（dataTask completion `[weak self]`） |
| MTMRTests/WidgetLeakTests.swift | +3 用例（27→30）+ round-40 MARK 注释节 + 2 测试子类（NoExecShellScriptItem/NoExecAppleScriptItem）+ EmptySource 桩 |
| iteration-log.md | 末尾追加本记录（先建「## 第 40 轮（功能/优化迭代第 28 轮）」+「### 子任务记录」小节头——第 33/35 轮教训，父任务预建于父分支、子卡基于 main 看不到故本卡自建，标注「第 40 轮 / 子任务 A」） |
| file-structure.zh.md | mindmap 第 7~39 轮 → 第 7~40 轮 + 报告行登记（无重复行） |
| scripts/anchor-patrol.py | 零改动（本轮改动文件无锚点：VolumeViewController.swift/WeatherBarItem.swift/WidgetLeakTests.swift 均不在锚点表），复跑确认 |

锚点巡检复跑：**PASS 72 / WARN 11 / INFO 5 / ERROR 0 退出码 0**（连续第十四轮 0 ERROR）。

## 六、结论与遗留登记

- 结论：异步闭包捕获链（无资源句柄的 block 捕获）泄漏契约网织密——全仓 27 处 URLSession completion + 181 处 DispatchQueue.async + 39 处 asyncAfter + 34 处 DispatchWorkItem + 2 处 CoreAudio block listener 逐一审计分类（widget 级强捕获 2 处全部修复，弱捕获在位 46 处、零 self/单例/值类型/有界一次性豁免 42 处）；**发现并根因修复真实永久泄漏 1 处**（VolumeViewController CoreAudio property-listener block 强捕获保留环 + 永不移除，红→绿双跑实证未放宽断言）+ 陈旧回调强捕获 1 处（WeatherBarItem dataTask，审计论证登记）；新增契约测试 3 用例（27→30，含全仓仅有的 2 个 asyncAfter 自循环 widget 契约钉），443 用例 0 失败。
- 遗留登记：
  1. WeatherBarItem dataTask 路径无网络注入点，红绿不可确定性钉住（location==nil 零副作用构造不触发请求）——修复以审计论证登记，构造路径 dealloc 由既有测试钉住；其余 URLSession completion 零 self 捕获 17 处 + 弱捕获 3 处经代码审计确认在位（与 YandexWeather/Currency/LyricsTranslate 同构），低风险不重复铺测试。
  2. VolumeViewController 修复后 deinit 移除双监听（block 恒等存储保证 remove 命中），零陈旧回调窗口；audioRouteChanged 内部 remove+re-add 链路随 block 句柄化同步保持恒等。
  3. 一次性/阻塞执行有界捕获（AppleScript/ShellScript init 链、SwipeItem execute、MusicBarItem 内联）评估为执行期持有 self 属必要（脚本执行需实例状态），零永久保留，不改动。
  4. 内存修复真机冒烟 3 项挂账延续（第 8/17~40 轮同口径）。
  5. 本轮零新增生产观察项；未 push（父任务收口统一推送）；未开新分支/新子任务/无 parents 依赖；未建 cron/自触发；未改 Info.plist 版本号（B 卡建议、父任务收口落地）。
- 完成自查：git status 干净（仅预期改动）+ commit 已提交（第 14 轮 B 卡漏提交教训）。
