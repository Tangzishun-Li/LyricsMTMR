# 第 12 轮回归报告 — main@bfcecd0 全量构建 + 单测回归验证（隔代触发）

- 任务：`t_33fa28c8`（第 12 轮 / 子任务 A · 年度维护核验 + 全量回归，本链第 4 次全量回归）
- 验证对象：main = `bfcecd0`（第 11 轮收口合并点；工作区分支 r12/review 基于此，工作树干净）
- 工作区分支：`r12/review`（预建 dir 工作区 .worktrees/round12-A，未 push）
- 执行时间：2026-08-12 15:53–15:57（+0800）
- 环境：macOS 15.7.7 / Xcode 16.4 (16F6) / arm64（MacBook Pro），Debug 配置，CODE_SIGNING_ALLOWED=NO

## 0. 回归背景（隔代触发规则）

第 9 轮全量回归（60 用例基线，main@487415e 含内存修复代码）之后已隔第 10、11 两代：
- 第 10 轮：年度维护核验（第 4 次）+ 收尾核对（含 D1 注释修正：`UnifiedSettingsWindowController.swift`
  闲置 GC 注释「10 分钟 → 1 小时」，随 main@0727066 入主干）+ 仓库卫生；
- 第 11 轮：年度维护核验（第 5 次）+ 收尾核对 + 仓库卫生，**纯文档维护，零代码改动**。

按回归规则（隔代父任务触发），本轮全量回归验证第 10 轮 D1 注释改动 + 第 11 轮纯文档维护后
主干仍健康。

## 1. 执行命令（与官方 CI .github/workflows/build-test.yml 一致，build/test 并行、独立 derivedDataPath）

```sh
cd LyricsMTMR
xcodebuild build -project LyricsMTMR.xcodeproj -scheme MTMR      -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-build CODE_SIGNING_ALLOWED=NO
xcodebuild test  -project LyricsMTMR.xcodeproj -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-test  CODE_SIGNING_ALLOWED=NO
```

> 两腿并行执行，各用独立 derivedDataPath（`-dd-build` / `-dd-test`），避免 build.db 锁冲突
> （第 7 轮曾遇 exit 65）。

## 2. 结果摘要

| 步骤 | 命令 | 结果 | exit | 耗时 (real) |
|---|---|---|---|---|
| 构建 | xcodebuild build, scheme=MTMR, Debug | **BUILD SUCCEEDED** | 0 | ~172s（冷 derived data，与 test 并行争抢） |
| 测试 | xcodebuild test, scheme=UnitTests, Debug | **TEST SUCCEEDED** | 0 | ~99s（含测试 target 编译，与 build 并行） |

## 3. 单测统计：60 用例，0 失败，0 意外（xcresult 实测）

| 测试套件 | 用例数 | 结果 |
|---|---|---|
| AppleScriptDefinitionTests | 5 | ✅ 0 失败 |
| BackgroundColorTests | 2 | ✅ 0 失败 |
| MirrorFingerprintTests | 14 | ✅ 0 失败 |
| NetEaseLRUCacheTests | 6 | ✅ 0 失败 |
| ParseConfig | 7 | ✅ 0 失败 |
| SettingsTabCacheTests | 6 | ✅ 0 失败 |
| StockMarketHoursTests | 16 | ✅ 0 失败 |
| WidgetLeakTests | 4 | ✅ 0 失败 |
| **合计** | **60** | **0 失败 / 0 意外** |

（`xcresulttool get test-results summary` 实证：passedTests=60，failedTests=0，skippedTests=0，
result=Passed；`get test-results tests` 逐用例复核 8 套件全部 Passed。）

已知易碎点核验（均通过，无时区/日历环境问题）：
- `testGoldenAnchors2026` ✅
- `testGoldenAnchors2027` ✅
- `testGoldenAnchorsMakeup2026` ✅

## 4. 与第 9 轮基线对比（main@487415e，60 用例 0 失败）

| 维度 | 第 9 轮（main@487415e） | 第 12 轮（main@bfcecd0） | 变化 |
|---|---|---|---|
| 用例总数 | 60 | 60 | 持平 |
| 失败 / 意外 | 0 / 0 | 0 / 0 | 持平 |
| 套件构成 | 8 套件 | 8 套件（逐一相同） | 持平 |
| 易碎点（3 金丝雀） | 全过 | 全过 | 持平 |

## 5. 构建告警

冷构建完整告警清单（/tmp/build_r12_cold.log 实测）：**9 条代码 warning + 2 条非代码提示**，
与第 9 轮基线构成一致：
- WeatherBarItem.swift 未使用变量/不可变值 ×4（:134/:200/:202/:210）
- onChange(of:perform:) 弃用提示 ×3（KeyBindingTabView.swift:712/:731、TouchBarSimulatorView.swift:191）
- NSImage? non-sendable ×2（LyricsEngine.swift:809/:994）
- appintentsmetadataprocessor 元数据跳过提示 ×1（非代码，同第 9 轮）
- 「Using the first of multiple matching destinations」×1（并行构建环境噪音，非代码，无影响）

D1 注释修正涉及文件 `UnifiedSettingsWindowController.swift` 零警告。均为既有代码风格/并发标注类，
非本次回归引入，不阻断。

## 6. 回归验证结论（第 10/11 轮变更）

- **第 10 轮 D1 注释修正**（UnifiedSettingsWindowController.swift:113-116「默认 1 小时
  （settingsWindowIdleGCSeconds = 3600）」）：编译通过、零警告，未破坏任何单测；
- **第 11 轮纯文档维护**（核验/核对/清理 3 报告 + iteration-log + file-structure 登记）：
  无代码变更，主干不受影响；
- 第 8 轮合入的内存修复代码（关窗隐藏复用 + 闲置 GC + 内存压力释放）在主干持续稳定，
  60 用例（含 WidgetLeakTests 4 用例）全绿。

## 7. 结论

**通过。** main@bfcecd0 主干构建与全部 60 项单元测试保持绿色（BUILD SUCCEEDED +
TEST SUCCEEDED，60 用例 0 失败 0 意外），与第 9 轮基线完全一致；第 10 轮 D1 注释修正与
第 11 轮纯文档维护未破坏主干，无回归。

本轮无代码改动、无修复提交；产出为本报告 + 核验报告（第 12 轮 / 子任务 A）+ iteration-log 记录
（均提交于 r12/review 分支）。
