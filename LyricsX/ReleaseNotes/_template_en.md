<!--
Release notes template. Copy to ReleaseNotes/{VERSION}_en.md when
starting a new release, replace {VERSION}, then fill only the sections
that apply and delete the rest.

Conventions:
- Title is the version tag, e.g. `# v1.8.5`. Do NOT include the product
  name (do not write `LyricsX 1.8.5`).
- The body must not contain `LyricsX {VERSION}` either; refer to the
  product as "the app" when needed.
- Bullets: `- **Short title** — One- or two-line description.` Wrap
  long lines with two-space indent so continuations remain attached to
  the bullet.
-->

# v{VERSION}

{Optional one-paragraph summary of what this release is about. Delete
this paragraph entirely if there is nothing to contextualize.}

## New Features

- **{Feature title}** — {description}

## Fixes

- **{Fix title}** — {description}

## Performance & stability

- **{Improvement title}** — {description}

## Acknowledgements

Thanks to contributors whose work ships in this release:

- **{Library or area}**
  - @{github-handle} (#{PR}) — {short description}

## System Requirements

- Minimum requirement: **macOS {version}**
