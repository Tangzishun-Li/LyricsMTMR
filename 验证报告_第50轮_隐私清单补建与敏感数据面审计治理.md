# 验证报告_第50轮_隐私清单补建与敏感数据面审计治理

- **轮次**：第 50 轮（功能/优化迭代第 38 轮）· 子任务 A（实现/优化）
- **维度**：安全与合规 —— 隐私清单补建与敏感数据面审计治理（R43 SecretsManager 后隔 6 轮）
- **分支**：r50/privacy ｜ **任务**：t_3821da31 ｜ **基线**：500 用例 0 失败（父任务分解前已实证，/tmp/LyricsMTMR-dd-r50reg-test，101.5s）

## 一、背景与目标

项目缺失 `PrivacyInfo.xcprivacy` 隐私清单（macOS 14+/Xcode 15 起合规面，memory「分发合规」空白面的前置项）。本轮完成：

1. 隐私相关 API 使用面盘点分类（6 类）；
2. 补建 `MTMR/PrivacyInfo.xcprivacy` 并注册进 project.pbxproj（四条目）；
3. 敏感数据面（日志/打印）复检治理（R43 续面）；
4. 新增 `PrivacyManifestContractTests` 13 用例（红→绿双跑）；
5. 全量回归 513 用例 0 失败实证。

## 二、隐私 API 使用面盘点分类

| 类别 | 使用面（grep 实证） | 是否需要清单声明 | 结论 |
|---|---|---|---|
| UserDefaults | 17 文件（AppSettings/WidgetKit/SettingsSync 等） | 是（required-reason） | 声明 `NSPrivacyAccessedAPICategoryUserDefaults` → **CA92.1** |
| 文件时间戳 | 2 文件：`GitStatus.swift:402` attributesOfItem（.git/HEAD 元数据）、`ExpenseTracker.swift:72` modificationDate（自有数据文件） | 是（required-reason） | 声明 `NSPrivacyAccessedAPICategoryFileTimestamp` → **C617.1** |
| 活动键盘 | 2 文件：`KeyBindingTabView.swift:1229` addLocalMonitorForEvents(.keyDown)（键绑定录制）、`Preferences/Editor/VirtualKeyboardView.swift:214` addLocalMonitorForEvents([.keyDown,.keyUp,.flagsChanged])（虚拟键盘编辑预览） | 是（required-reason） | 声明 `NSPrivacyAccessedAPICategoryActiveKeyboard` → **3B52.1**（键绑定录制语义） |
| 系统启动时间 | 全仓 0 命中（无 systemUptime/bootTime/mach_absolute_time） | 否（未使用） | 不声明（契约测试反向锚定） |
| 磁盘空间 | 全仓 0 命中（无 volumeAvailableCapacity/attributesOfFileSystemForPath） | 否（未使用） | 不声明（契约测试反向锚定） |
| 剪贴板 | 6 文件（KeyBindingTabView/RSSTabView/TouchBarPreviewView/WidgetKit/QuickReplyBarItem/ClipboardHistory） | 非 required-reason API | 不声明 API 类别；ClipboardHistory 本地持久化剪贴板内容 → 收集面 `OtherUserContent` |
| 定位 | 3 文件（WeatherLocationSession/YandexWeatherBarItem/WeatherBarItem） | 非 required-reason API | 坐标随天气请求出设备 → 收集面 `Location` |
| 麦克风 | 2 文件（NoiseMeter/AudioSpectrumBarItem） | 非 required-reason API | 仅内存实时处理（dB/频谱），不存储不传输 → 收集面**不声明** AudioData（诚实口径） |
| Keychain | 2 文件（SecretsManager/ServicesTabView） | 非 required-reason API | 不声明（R43 决策门状态保持） |
| 网络 URLSession | 8+ 文件（天气/RSS/汇率/GitHub 等） | 非 required-reason API | 不声明；均为用户功能请求 |

其他说明：

- `KeyPress.swift` 使用 CGEvent **post**（合成按键输出，非读取）——不属于 ActiveKeyboard 读取面，不触发声明；
- `RibbonEditorView.swift:666` 的 `uptimeNanoseconds` 为 `ContinuousClock` 单调时钟差值（进程内计时），非系统启动时间 API；
- `ServerMonitor.swift` 的 `uptime` 为 **SSH 远端命令字符串**（读远程服务器负载），非本机系统启动时间 API；
- 无广告追踪 SDK / 无第三方统计 → `NSPrivacyTracking = false`、`NSPrivacyTrackingDomains = []`；
- LyricsRendering 为内部 framework（不对外分发），按 Apple 规范无需自带清单（第三方分发 framework 才需要）；
- Info.plist 已含全套 usage description（麦克风/定位/相机等），与 entitlements（audio-input/network.client）一致，无需增补。

## 三、PrivacyInfo.xcprivacy 补建

路径：`LyricsMTMR/MTMR/PrivacyInfo.xcprivacy`（与 Info.plist 同组，主 target 可识别处）。

声明内容（plutil -lint OK）：

- `NSPrivacyAccessedAPITypes`（3 项必申，按实际语义）：
  - UserDefaults → `CA92.1`（读写应用自身偏好，17 文件使用面）；
  - FileTimestamp → `C617.1`（GitStatus/ExpenseTracker 文件元数据）；
  - ActiveKeyboard → `3B52.1`（键绑定录制 + 虚拟键盘按键监视）；
- `NSPrivacyCollectedDataTypes`（2 项诚实声明，均 Linked=false / Tracking=false / Purpose=AppFunctionality）：
  - `Location`（天气定位坐标随请求出设备）；
  - `OtherUserContent`（ClipboardHistory 本地持久化剪贴板内容）；
- `NSPrivacyTracking` = false；`NSPrivacyTrackingDomains` = []。

未声明项论证（防过度声明）：SystemBootTime/DiskSpace 全仓 0 命中（见上表）；麦克风仅内存处理不落盘不传输（Apple「收集」定义=存储或传输）。

## 四、pbxproj 注册

四条目齐备（参考 ChinaCityCodes.json 既有资源注册模式，UUID 采用 add_files.py 同款确定性方案 sha1("PrivacyInfo.xcprivacy")）：

| 条目 | UUID | 实证 |
|---|---|---|
| PBXBuildFile | `C0FF26B2A637F7DC4079D5AB`（in Resources） | grep 命中 |
| PBXFileReference | `C0FE26B2A637F7DC4079D5AB`（lastKnownFileType = text.xml） | grep 命中 |
| MTMR group children | `C0FE26B2A637F7DC4079D5AB /* PrivacyInfo.xcprivacy */,` | grep 命中 |
| 主 target Resources phase | `C0FF26B2A637F7DC4079D5AB /* PrivacyInfo.xcprivacy in Resources */,` | grep 命中（B082B24D… 应用 target 的 Resources phase） |

测试文件经 `python3 Scripts/add_files.py Tests:PrivacyManifestContractTests.swift` 注册（C1FE/C1FF 测试 UUID 前缀，4 条目）。注册后 `plutil -lint project.pbxproj` OK。

## 五、敏感数据面复检（R43 续面）

- 全仓 print/NSLog 共 29 处逐一复检：无密钥/token/密码/cookie 输出（R43 结论「日志 0 泄漏」保持）；
- 风险点复核：`YandexWeatherBarItem.swift:180` `print(getWeatherUrl())` 仅含坐标（lat/lon）与 lang，无 API 密钥（R43 曾核 :177，行号后移 3 行内容不变）；
- `detectHardcodedKeys` snippet 掩码（R43 修复面）复检在位；
- 本轮敏感日志治理结论：**0 新发现，无需处理**（治理面已由 R43 收口）。

## 六、契约测试：PrivacyManifestContractTests（13 用例）

文件：`LyricsMTMR/MTMRTests/PrivacyManifestContractTests.swift`（手写，`#filePath` 解析仓库相对路径直读源码/manifest/pbxproj，不触碰真实用户数据）。

| 分组 | 用例 | 锚定契约 |
|---|---|---|
| 存在性/合法性 | testPrivacyManifestFileExists / testPrivacyManifestIsValidPlist | 文件存在 + 合法 plist |
| 必申 API | testUserDefaultsCategoryDeclaredWithCA92 / testFileTimestampCategoryDeclaredWithC617 / testActiveKeyboardCategoryDeclaredWith3B52 | 三类 + 精确 reason code |
| 收集面/追踪 | testTrackingFalseAndNoTrackingDomains / testCollectedDataTypesLocationDeclared / testCollectedDataTypesOtherUserContentDeclared | Tracking=false、两收集项字段级断言 |
| pbxproj 注册 | testManifestRegisteredInPbxproj | 四条目文本断言 + 防误入 Sources |
| 声明↔代码双向 | testUserDefaultsUsageSiteExists / testFileTimestampUsageSitesExist / testActiveKeyboardUsageSitesExist | 每条声明对应真实 API 使用点（防过度声明） |
| 反向锚定 | testUnusedRequiredReasonAPIsNotDeclared | SystemBootTime/DiskSpace 不得声明（防漏声明的镜像：防过度声明） |

红→绿双跑（-only-testing:LyricsMTMRTests/PrivacyManifestContractTests，同 /tmp/LyricsMTMR-dd-r50a-contract）：

- **红**：临时移除 manifest 中 ActiveKeyboard 声明块（红态=漏声明）→ **13 tests, 1 failure**（testActiveKeyboardCategoryDeclaredWith3B52 精确失败，其余 12 全绿）；恢复后文件与备份 sha256 一致；
- **绿**：**13 tests, 0 failures** —— 同一断言未放宽（红态失败用例即绿态断言本身，非新断言）。

## 七、全量回归

```
cd LyricsMTMR && caffeinate -i xcodebuild test -project LyricsMTMR.xcodeproj \
  -scheme UnitTests -configuration Debug -derivedDataPath /tmp/LyricsMTMR-dd-r50a-full
```

**TEST SUCCEEDED —— Executed 513 tests, 0 failures（0 unexpected）**，101.0s（500 基线 + 13 新增零偏差）。金丝雀套件随全量 0 失败（SecretsManagerContractTests 13 / UserDefaultsContractTests 6 / NetworkRobustnessContractTests 13 / ThemeStateMachineContractTests 19 / WriteSideContractTests 6 等）。先清旧 /tmp/LyricsMTMR-dd-* 防磁盘写满（第 22 轮教训），caffeinate -i 防休眠。

## 八、锚点巡检

`python3 scripts/anchor-patrol.py`：**PASS 67 / WARN 16 / INFO 5 / ERROR 0 退出码 0**（连续第二十九轮 0 ERROR）。改动文件（新增 PrivacyInfo.xcprivacy / PrivacyManifestContractTests.swift / pbxproj 四条目 / 本报告 / iteration-log / file-structure）均非锚点行号修正对象；REGISTRY 登记本卡报告 1 行（file-structure.zh.md，无重复行，live 检查通过）。

## 九、交付物

- `LyricsMTMR/MTMR/PrivacyInfo.xcprivacy`（新增，1806B）
- `LyricsMTMR/MTMRTests/PrivacyManifestContractTests.swift`（新增，13 用例）
- `LyricsMTMR/LyricsMTMR.xcodeproj/project.pbxproj`（+5 行：资源四条目 + 测试四条目）
- 本报告（分支根目录）+ iteration-log.md 第 50 轮「### 子任务记录」追加 + file-structure.zh.md 登记（mindmap 第 7~50 轮 + 报告行 1 行）

## 十、遗留登记

1. PrivacyInfo.xcprivacy 生效面为 macOS 14+/Xcode 15+ 构建产物（低版本系统运行时静默忽略，无损）；未公证/未开 App Sandbox 的「分发合规」空白面继续挂账（父任务既定口径）；
2. NSPrivacyCollectedDataTypes 为诚实口径当前值；若未来新增分析类 SDK/遥测，须同步补声明（契约测试锚定字段级，新声明会被 live 检查覆盖）；
3. 其余各轮挂账延续（issue #1 真机验证 / ITER-15 决策门 / ITER-14 2026-11 时间驱动 / 真机冒烟系列 / GPS 灯观感 / t_aeb0b769 建议 / R47 A 卡 3 项等），本轮**新增发现 0 项**。

## 十一、约束自检

- ✅ 未 push 远端；未开新分支/新子任务；无 parents 依赖；未建 cron/自触发
- ✅ 未改 Info.plist 版本号（B 卡建议、父任务收口落地）
- ✅ 未触碰 SecretsManager/Keychain 决策门（R43/R45 状态）；未改无前缀键行为（R47 A 卡论证不动）
- ✅ 红→绿双跑断言未放宽；完成自查 git status 干净 + commit 已提交（第 14 轮 B 卡漏提交教训）
- ✅ 未虚构：全部实证（grep 命中 / plutil / xcodebuild 日志 / anchor-patrol 输出）
